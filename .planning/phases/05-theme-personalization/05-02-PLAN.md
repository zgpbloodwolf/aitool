---
phase: 05-theme-personalization
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/renderer/src/components/ChatPanel.vue
  - src/renderer/src/components/ConfirmDialog.vue
  - src/renderer/src/components/UpdatePrompt.vue
  - src/renderer/src/components/SettingsDrawer.vue
autonomous: true
requirements: [UX-06]

must_haves:
  truths:
    - "ChatPanel.vue 中零硬编码 hex 颜色值（#开头的颜色）"
    - "ConfirmDialog.vue 中零硬编码 hex 颜色值"
    - "UpdatePrompt.vue 中零硬编码 hex 颜色值"
    - "SettingsDrawer.vue 中零硬编码 hex 颜色值"
    - "所有 rgba() 值迁移为 CSS 变量引用"
  artifacts:
    - path: "src/renderer/src/components/ChatPanel.vue"
      provides: "纯 CSS 变量驱动的样式"
      not_contains: "#181825"
    - path: "src/renderer/src/components/ConfirmDialog.vue"
      provides: "纯 CSS 变量驱动的样式"
      not_contains: "#eba0ac"
    - path: "src/renderer/src/components/UpdatePrompt.vue"
      provides: "纯 CSS 变量驱动的样式"
      not_contains: "rgba(243, 139, 168"
  key_links:
    - from: "ChatPanel.vue scoped CSS"
      to: "main.css CSS 变量"
      via: "var(--xxx) 引用"
      pattern: "var\\(--"
---

<objective>
将 ChatPanel.vue、ConfirmDialog.vue、UpdatePrompt.vue、SettingsDrawer.vue 中的所有硬编码颜色值迁移为 CSS 变量引用，使主题切换对所有 UI 元素生效。

Purpose: 没有 CSS 变量迁移，切换亮色主题后大量组件仍显示暗色硬编码值。这是 D-03 的核心要求。
Output: 四个组件中零硬编码颜色，全部使用 var(--xxx) 引用。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-theme-personalization/05-CONTEXT.md
@.planning/phases/05-theme-personalization/05-RESEARCH.md
@.planning/phases/05-theme-personalization/05-01-SUMMARY.md

<interfaces>
<!-- Plan 01 创建的 CSS 变量 — 迁移目标引用 -->

```css
/* 颜色变量 */
--bg-primary, --bg-secondary, --bg-tertiary
--text-primary, --text-secondary, --text-muted
--accent, --accent-hover, --border
--success, --error, --warning

/* 辅助变量（Plan 01 新增） */
--bg-hover                  /* 悬停背景 */
--border-hover              /* 悬停边框 */
--bg-overlay                /* 半透明覆盖 (0.95 opacity) */
--bg-overlay-light          /* 半透明覆盖 (0.8 opacity) */
--error-hover               /* error 按钮悬停 */
--warning-hover             /* warning 按钮悬停 */
--status-info-bg            /* info 状态背景 */
--status-info-border        /* info 状态边框 */
--status-info-text          /* info 状态文字 */
--status-warning-bg         /* warning 状态背景 */
--status-warning-border     /* warning 状态边框 */
--status-warning-text       /* warning 状态文字 */
--status-error-bg           /* error 状态背景 */
--status-error-border       /* error 状态边框 */
--status-error-text         /* error 状态文字 */
--overlay-backdrop          /* 半透明遮罩 */
--dialog-backdrop           /* 对话框遮罩 */
--shadow-color              /* 阴影 */
--delete-hover-bg           /* 删除按钮悬停背景 */
--success-bg-subtle         /* success 低透明度背景 */
--success-border-subtle     /* success 低透明度边框 */
--error-bg-subtle           /* error 低透明度背景 */
--error-border-subtle       /* error 低透明度边框 */
--warning-bright            /* 明亮警告色 (#f9e2af / #df8e1d) */
```

<!-- 硬编码颜色映射表（来自 RESEARCH.md 审计） -->

```
ChatPanel.vue 映射:
  #181825  → var(--bg-secondary)
  #1e1e2e  → var(--bg-primary)
  #313244  → var(--bg-tertiary)
  #45475a  → var(--border)
  #cdd6f4  → var(--text-primary)
  #a6adc8  → var(--text-secondary)
  #89b4fa  → var(--accent)
  #f38ba8  → var(--error)
  #f9e2af  → var(--warning-bright)
  #6c7086  → var(--text-muted)
  rgba(24, 24, 37, 0.95) → var(--bg-overlay)
  rgba(24, 24, 37, 0.8)  → var(--bg-overlay-light) [如出现]
  rgba(0, 0, 0, 0.4)     → var(--overlay-backdrop)
  rgba(137, 180, 250, 0.15) → var(--status-info-bg)
  rgba(137, 180, 250, 0.3)  → var(--status-info-border)
  rgba(249, 226, 175, 0.15) → var(--status-warning-bg)
  rgba(249, 226, 175, 0.3)  → var(--status-warning-border)
  rgba(243, 139, 168, 0.15) → var(--status-error-bg)
  rgba(243, 139, 168, 0.3)  → var(--status-error-border)
  rgba(243, 139, 168, 0.1)  → var(--delete-hover-bg)

ConfirmDialog.vue 映射:
  #eba0ac  → var(--error-hover)
  #ffcba0  → var(--warning-hover)
  rgba(0, 0, 0, 0.5) → var(--dialog-backdrop)

SettingsDrawer.vue 映射:
  rgba(0, 0, 0, 0.4) → var(--overlay-backdrop)
  rgba(0, 0, 0, 0.3) → var(--shadow-color) [用于 box-shadow]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 迁移 ChatPanel.vue 硬编码颜色到 CSS 变量</name>
  <files>src/renderer/src/components/ChatPanel.vue</files>
  <behavior>
    - 所有 scoped CSS 中的硬编码 hex 值 (#181825, #1e1e2e, #313244, #45475a, #cdd6f4, #a6adc8, #89b4fa, #f38ba8, #f9e2af, #6c7086) 替换为对应的 var(--xxx)
    - 所有 rgba() 值替换为对应的 var(--xxx) 辅助变量
    - `rgba(0, 0, 0, 0.4)` 替换为 `var(--overlay-backdrop)`
  </behavior>
  <read_first>
    src/renderer/src/components/ChatPanel.vue
    src/renderer/src/assets/main.css
  </read_first>
  <action>
    在 ChatPanel.vue 的 `<style scoped>` 部分，逐个替换硬编码颜色。

    **映射关系（按 CSS 选择器分组）：**

    1. `.tab-bar` (第 918-922 行)：
       - `background: #181825` → `background: var(--bg-secondary)`
       - `border-bottom: 1px solid #313244` → `border-bottom: 1px solid var(--bg-tertiary)`

    2. `.tab-item` (第 935-948 行)：
       - `color: #a6adc8` → `color: var(--text-secondary)`

    3. `.tab-item:hover` (第 950-953 行)：
       - `background: #1e1e2e` → `background: var(--bg-primary)`
       - `color: #cdd6f4` → `color: var(--text-primary)`

    4. `.tab-item.active` (第 955-959 行)：
       - `background: #1e1e2e` → `background: var(--bg-primary)`
       - `color: #cdd6f4` → `color: var(--text-primary)`
       - `border-bottom: 2px solid #89b4fa` → `border-bottom: 2px solid var(--accent)`

    5. `.tab-close:hover` (第 1004-1007 行)：
       - `color: #f38ba8` → `color: var(--error)`

    6. `.tab-add` (第 1009-1022 行)：
       - `color: #a6adc8` → `color: var(--text-secondary)`

    7. `.tab-history` (第 1024-1037 行)：
       - `color: #a6adc8` → `color: var(--text-secondary)`

    8. `.tab-history:hover` (第 1039-1042 行)：
       - `background: #1e1e2e` → `background: var(--bg-primary)`
       - `color: #cdd6f4` → `color: var(--text-primary)`

    9. `.tab-history.active` (第 1044-1047 行)：
       - `background: #313244` → `background: var(--bg-tertiary)`
       - `color: #89b4fa` → `color: var(--accent)`

    10. `.tab-add:hover` (第 1049-1052 行)：
        - `background: #1e1e2e` → `background: var(--bg-primary)`
        - `color: #cdd6f4` → `color: var(--text-primary)`

    11. `.webview-iframe` (第 1061-1069 行)：
        - `background: #1e1e2e` → `background: var(--bg-primary)`

    12. `.unresponsive-overlay` (第 1077-1091 行)：
        - `background: rgba(24, 24, 37, 0.95)` → `background: var(--bg-overlay)`
        - `border: 1px solid #313244` → `border: 1px solid var(--bg-tertiary)`

    13. `.unresponsive-text` (第 1093-1098 行)：
        - `color: #f38ba8` → `color: var(--error)`

    14. `.status-overlay.info` (第 1115-1119 行)：
        - `background: rgba(137, 180, 250, 0.15)` → `background: var(--status-info-bg)`
        - `color: #89b4fa` → `color: var(--status-info-text)`
        - `border-top: 1px solid rgba(137, 180, 250, 0.3)` → `border-top: 1px solid var(--status-info-border)`

    15. `.status-overlay.warning` (第 1121-1125 行)：
        - `background: rgba(249, 226, 175, 0.15)` → `background: var(--status-warning-bg)`
        - `color: #f9e2af` → `color: var(--status-warning-text)`
        - `border-top: 1px solid rgba(249, 226, 175, 0.3)` → `border-top: 1px solid var(--status-warning-border)`

    16. `.status-overlay.error` (第 1127-1131 行)：
        - `background: rgba(243, 139, 168, 0.15)` → `background: var(--status-error-bg)`
        - `color: #f38ba8` → `color: var(--status-error-text)`
        - `border-top: 1px solid rgba(243, 139, 168, 0.3)` → `border-top: 1px solid var(--status-error-border)`

    17. `.info .status-dot` (第 1141-1143 行)：
        - `background: #89b4fa` → `background: var(--accent)`

    18. `.warning .status-dot` (第 1144-1146 行)：
        - `background: #f9e2af` → `background: var(--warning-bright)`

    19. `.error .status-dot` (第 1147-1149 行)：
        - `background: #f38ba8` → `background: var(--error)`

    20. `.session-history-overlay` (第 1178-1188 行)：
        - `background: rgba(0, 0, 0, 0.4)` → `background: var(--overlay-backdrop)`

    21. `.session-history-panel` (第 1190-1198 行)：
        - `background: #181825` → `background: var(--bg-secondary)`
        - `border-left: 1px solid #313244` → `border-left: 1px solid var(--bg-tertiary)`

    22. `.session-header` (第 1200-1207 行)：
        - `border-bottom: 1px solid #313244` → `border-bottom: 1px solid var(--bg-tertiary)`

    23. `.session-title` (第 1209-1212 行)：
        - `color: #cdd6f4` → `color: var(--text-primary)`

    24. `.session-close` (第 1214-1221 行)：
        - `color: #6c7086` → `color: var(--text-muted)`

    25. `.session-close:hover` (第 1223-1225 行)：
        - `color: #f38ba8` → `color: var(--error)`

    26. `.session-loading`, `.session-empty` (第 1227-1233 行)：
        - `color: #6c7086` → `color: var(--text-muted)`

    27. `.session-list::-webkit-scrollbar-thumb` (第 1247-1253 行)：
        - `background: #45475a` → `background: var(--border)`

    28. `.session-item:hover` (第 1261-1263 行)：
        - `background: #1e1e2e` → `background: var(--bg-primary)`

    29. `.session-summary` (第 1265-1278 行)：
        - `color: #cdd6f4` → `color: var(--text-primary)`

    30. `.session-meta` (第 1280-1285 行)：
        - `color: #6c7086` → `color: var(--text-muted)`

    31. `.session-branch` (第 1287-1298 行)：
        - `background: #313244` → `background: var(--bg-tertiary)`
        - `color: #a6adc8` → `color: var(--text-secondary)`

    32. `.session-delete` (第 1300-1313 行)：
        - `color: #6c7086` → `color: var(--text-muted)`

    33. `.session-delete:hover` (第 1319-1322 行)：
        - `color: #f38ba8` → `color: var(--error)`
        - `background: rgba(243, 139, 168, 0.1)` → `background: var(--delete-hover-bg)`

    注意：`.tab-item.drag-over` 中 `var(--accent, #89b4fa)` 已使用 CSS 变量，无需修改（fallback 值保留）。同样 `.tab-status-dot` 系列也已使用变量。
  </action>
  <verify>
    <automated>cd E:/work/projects/new-aitools && grep -cP '#[0-9a-fA-F]{6}' src/renderer/src/components/ChatPanel.vue | xargs -I{} sh -c 'test {} -le 0 && echo "PASS: zero hardcoded hex" || echo "FAIL: {} hardcoded hex remain"'</automated>
  </verify>
  <done>
    ChatPanel.vue 的 `<style scoped>` 中零硬编码 hex 颜色值和零主题相关 rgba() 值，全部使用 CSS 变量引用。
  </done>
</task>

<task type="auto">
  <name>Task 2: 迁移 ConfirmDialog.vue、UpdatePrompt.vue、SettingsDrawer.vue 硬编码颜色</name>
  <files>
    src/renderer/src/components/ConfirmDialog.vue
    src/renderer/src/components/UpdatePrompt.vue
    src/renderer/src/components/SettingsDrawer.vue
  </files>
  <read_first>
    src/renderer/src/components/ConfirmDialog.vue
    src/renderer/src/components/UpdatePrompt.vue
    src/renderer/src/components/SettingsDrawer.vue
    src/renderer/src/assets/main.css
  </read_first>
  <action>
    **1. ConfirmDialog.vue — 替换 3 处硬编码颜色**

    - `.btn-confirm.danger:hover` (约第 127-129 行)：
      `background: #eba0ac` → `background: var(--error-hover)`
    - `.btn-confirm.warning:hover` (约第 131-133 行)：
      `background: #ffcba0` → `background: var(--warning-hover)`
    - `.dialog-overlay` (约第 53-56 行)：
      `background: rgba(0, 0, 0, 0.5)` → `background: var(--dialog-backdrop)`

    **2. UpdatePrompt.vue — 替换 rgba 值**

    读取 UpdatePrompt.vue，找到所有硬编码颜色：
    - `rgba(0, 0, 0, 0.5)` → `var(--dialog-backdrop)` （遮罩背景）
    - `rgba(243, 139, 168, 0.1)` → `var(--error-bg-subtle)` （error 背景色）
    - `rgba(243, 139, 168, 0.3)` → `var(--error-border-subtle)` （error 边框色）
    - `rgba(166, 227, 161, 0.1)` → `var(--success-bg-subtle)` （success 背景色）
    - `rgba(166, 227, 161, 0.3)` → `var(--success-border-subtle)` （success 边框色）
    - 检查是否还有其他硬编码 hex 值，如有也一并替换

    **3. SettingsDrawer.vue — 替换 2 处 rgba 值**

    - `.settings-backdrop` (约第 213-220 行)：
      `background: rgba(0, 0, 0, 0.4)` → `background: var(--overlay-backdrop)`
    - `.settings-drawer` (约第 225-235 行)：
      `box-shadow: -4px 0 16px rgba(0, 0, 0, 0.3)` → `box-shadow: -4px 0 16px var(--shadow-color)`
  </action>
  <verify>
    <automated>cd E:/work/projects/new-aitools && echo "=== ConfirmDialog ===" && grep -cP '#[0-9a-fA-F]{6}' src/renderer/src/components/ConfirmDialog.vue && echo "=== UpdatePrompt rgba ===" && grep -cP 'rgba\([0-9]' src/renderer/src/components/UpdatePrompt.vue && echo "=== SettingsDrawer rgba ===" && grep -cP 'rgba\([0-9]' src/renderer/src/components/SettingsDrawer.vue</automated>
  </verify>
  <done>
    - ConfirmDialog.vue 中零硬编码 hex 和零硬编码 rgba（#eba0ac, #ffcba0, rgba(0,0,0,0.5) 已替换）
    - UpdatePrompt.vue 中零主题相关 rgba 值（error/success 的 rgba 替换为 CSS 变量）
    - SettingsDrawer.vue 中零硬编码 rgba（rgba(0,0,0,0.4) 和 rgba(0,0,0,0.3) 已替换）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增信任边界 | 本计划仅修改 CSS 样式，不涉及数据流或安全边界变更 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | — | CSS 变量引用 | accept | 纯样式变更，无安全影响 |
</threat_model>

<verification>
1. `grep -P '#[0-9a-fA-F]{6}' src/renderer/src/components/ChatPanel.vue` 返回 0 结果（排除注释中的 hex）
2. `grep -P '#[0-9a-fA-F]{6}' src/renderer/src/components/ConfirmDialog.vue` 返回 0 结果
3. 切换到亮色主题后，所有组件颜色跟随变化，无残留暗色块
4. `pnpm dev` 编译无错误
</verification>

<success_criteria>
1. ChatPanel.vue 中所有硬编码颜色（~30 处 hex + ~10 处 rgba）替换为 CSS 变量
2. ConfirmDialog.vue 中 2 处 hex hover 颜色 + 1 处 rgba 替换为 CSS 变量
3. UpdatePrompt.vue 中 5 处 rgba 替换为 CSS 变量
4. SettingsDrawer.vue 中 2 处 rgba 替换为 CSS 变量
5. 无编译错误
</success_criteria>

<output>
After completion, create `.planning/phases/05-theme-personalization/05-02-SUMMARY.md`
</output>

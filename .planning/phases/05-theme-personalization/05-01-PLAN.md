---
phase: 05-theme-personalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/assets/main.css
  - src/renderer/src/stores/settings.ts
  - src/renderer/index.html
  - src/renderer/src/main.ts
  - src/preload/index.ts
  - src/renderer/src/components/SettingsDrawer.vue
autonomous: true
requirements: [UX-06]

must_haves:
  truths:
    - "main.css 包含 [data-theme='dark'] 和 [data-theme='light'] 两套完整 CSS 变量值"
    - "settings store 包含 theme 字段且支持 update({ theme }) 持久化到 localStorage"
    - "index.html 的 <html> 元素在页面加载时即带有正确的 data-theme 属性"
    - "SettingsDrawer 包含主题切换区域，三个选项可点击切换主题"
    - "preload 暴露 onThemeSystemChanged 和 updateTheme 两个 IPC 桥接方法"
  artifacts:
    - path: "src/renderer/src/assets/main.css"
      provides: "双主题 CSS 变量定义"
      contains: "[data-theme=\"light\"]"
    - path: "src/renderer/src/stores/settings.ts"
      provides: "theme: ThemeMode 字段"
      contains: "theme:"
    - path: "src/renderer/index.html"
      provides: "data-theme 初始属性"
      contains: "data-theme"
    - path: "src/renderer/src/main.ts"
      provides: "主题初始化和 IPC 监听逻辑"
      contains: "applyTheme"
    - path: "src/preload/index.ts"
      provides: "主题 IPC 桥接"
      contains: "onThemeSystemChanged"
    - path: "src/renderer/src/components/SettingsDrawer.vue"
      provides: "主题切换 UI"
      contains: "主题"
  key_links:
    - from: "src/renderer/src/main.ts"
      to: "src/renderer/src/stores/settings.ts"
      via: "读取 settings.theme 调用 applyTheme"
      pattern: "useSettingsStore"
    - from: "src/renderer/src/components/SettingsDrawer.vue"
      to: "src/renderer/src/stores/settings.ts"
      via: "调用 settingsStore.update({ theme: value })"
      pattern: "update.*theme"
    - from: "src/renderer/src/main.ts"
      to: "src/preload/index.ts"
      via: "window.api.onThemeSystemChanged 监听系统主题变化"
      pattern: "onThemeSystemChanged"
---

<objective>
建立主题系统基础设施层：CSS 变量双主题定义、设置 store 扩展、HTML 入口 data-theme 属性、渲染进程初始化逻辑、preload IPC 桥接、以及设置面板的主题切换 UI。

Purpose: 这是整个主题系统的根基——后续的硬编码颜色迁移和主进程同步都依赖本计划创建的 CSS 变量、store 字段和 IPC 通道。
Output: 完整可用的主题切换基础设施，用户可在设置面板选择暗色/亮色/跟随系统，页面立即响应。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-theme-personalization/05-CONTEXT.md
@.planning/phases/05-theme-personalization/05-RESEARCH.md

<interfaces>
<!-- 现有 CSS 变量（main.css）— 需要为每对变量添加 light 主题值 -->
```css
:root {
  --bg-primary: #1e1e2e;
  --bg-secondary: #181825;
  --bg-tertiary: #313244;
  --text-primary: #cdd6f4;
  --text-secondary: #a6adc8;
  --text-muted: #6c7086;
  --accent: #89b4fa;
  --accent-hover: #74c7ec;
  --border: #45475a;
  --success: #a6e3a1;
  --error: #f38ba8;
  --warning: #fab387;
  --sidebar-width: 260px;
  --titlebar-height: 36px;
  --statusbar-height: 24px;
  --terminal-default-height: 200px;
}
```

<!-- settings.ts SettingsState 接口 — 需要新增 theme 字段 -->
```typescript
export interface SettingsState {
  soundEnabled: boolean;
  soundVolume: number;
  soundComplete: boolean;
  soundPermission: boolean;
  soundError: boolean;
  zoomFactor: number;
  notifyEnabled: boolean;
  notifyComplete: boolean;
  notifyPermission: boolean;
  notifyPlan: boolean;
  notifyReply: boolean;
  notifyError: boolean;
  skippedVersion: string | null;
  closeBehavior: CloseBehavior;
}
// DEFAULTS 对象中需要新增 theme: 'dark'
// settings ref 聚合对象中需要新增 theme 字段
```

<!-- preload/index.ts api 对象 — 需要新增主题 IPC 桥接 -->
```typescript
// 新增两个方法:
// onThemeSystemChanged: 监听 'theme:system-changed' IPC 通道
// updateTheme: 发送 'theme:update' IPC 通道
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS 变量双主题定义 + 设置 store 扩展 + index.html + preload IPC 桥接</name>
  <files>
    src/renderer/src/assets/main.css
    src/renderer/src/stores/settings.ts
    src/renderer/index.html
    src/preload/index.ts
  </files>
  <read_first>
    src/renderer/src/assets/main.css
    src/renderer/src/stores/settings.ts
    src/renderer/index.html
    src/preload/index.ts
  </read_first>
  <action>
    **1. main.css — 扩展双主题 CSS 变量 (per D-01, D-05)**

    将现有 `:root { ... }` 改为 `:root, [data-theme="dark"] { ... }`（保持暗色默认）。
    在其后新增 `[data-theme="light"]` 选择器块，提供 Catppuccin Latte 配色值（per D-01）：

    ```css
    :root,
    [data-theme="dark"] {
      /* 保持所有现有变量值不变 */
      --bg-primary: #1e1e2e;
      --bg-secondary: #181825;
      --bg-tertiary: #313244;
      --text-primary: #cdd6f4;
      --text-secondary: #a6adc8;
      --text-muted: #6c7086;
      --accent: #89b4fa;
      --accent-hover: #74c7ec;
      --border: #45475a;
      --success: #a6e3a1;
      --error: #f38ba8;
      --warning: #fab387;

      /* 新增辅助变量（per D-03, 用于后续硬编码颜色迁移） */
      --bg-hover: #45475a;
      --border-hover: #585b70;
      --bg-overlay: rgba(30, 30, 46, 0.95);
      --bg-overlay-light: rgba(30, 30, 46, 0.8);
      --error-hover: #eba0ac;
      --warning-hover: #ffcba0;
      --status-info-bg: rgba(137, 180, 250, 0.15);
      --status-info-border: rgba(137, 180, 250, 0.3);
      --status-info-text: #89b4fa;
      --status-warning-bg: rgba(249, 226, 175, 0.15);
      --status-warning-border: rgba(249, 226, 175, 0.3);
      --status-warning-text: #f9e2af;
      --status-error-bg: rgba(243, 139, 168, 0.15);
      --status-error-border: rgba(243, 139, 168, 0.3);
      --status-error-text: #f38ba8;
      --overlay-backdrop: rgba(0, 0, 0, 0.4);
      --dialog-backdrop: rgba(0, 0, 0, 0.5);
      --shadow-color: rgba(0, 0, 0, 0.3);
      --delete-hover-bg: rgba(243, 139, 168, 0.1);
      --success-bg-subtle: rgba(166, 227, 161, 0.1);
      --success-border-subtle: rgba(166, 227, 161, 0.3);
      --error-bg-subtle: rgba(243, 139, 168, 0.1);
      --error-border-subtle: rgba(243, 139, 168, 0.3);
      --warning-bright: #f9e2af;

      /* 非颜色变量 */
      --sidebar-width: 260px;
      --titlebar-height: 36px;
      --statusbar-height: 24px;
      --terminal-default-height: 200px;
    }

    [data-theme="light"] {
      --bg-primary: #eff1f5;
      --bg-secondary: #e6e9ef;
      --bg-tertiary: #ccd0da;
      --text-primary: #4c4f69;
      --text-secondary: #5c5f77;
      --text-muted: #9ca0b0;
      --accent: #1e66f5;
      --accent-hover: #209fb5;
      --border: #bcc0cc;
      --success: #40a02b;
      --error: #d20f39;
      --warning: #fe640b;

      /* 辅助变量 Latte 值 */
      --bg-hover: #bcc0cc;
      --border-hover: #acb0be;
      --bg-overlay: rgba(239, 241, 245, 0.95);
      --bg-overlay-light: rgba(239, 241, 245, 0.8);
      --error-hover: #e64553;
      --warning-hover: #e64553;
      --status-info-bg: rgba(30, 102, 245, 0.15);
      --status-info-border: rgba(30, 102, 245, 0.3);
      --status-info-text: #1e66f5;
      --status-warning-bg: rgba(254, 100, 11, 0.15);
      --status-warning-border: rgba(254, 100, 11, 0.3);
      --status-warning-text: #fe640b;
      --status-error-bg: rgba(210, 15, 57, 0.15);
      --status-error-border: rgba(210, 15, 57, 0.3);
      --status-error-text: #d20f39;
      --overlay-backdrop: rgba(0, 0, 0, 0.25);
      --dialog-backdrop: rgba(0, 0, 0, 0.35);
      --shadow-color: rgba(0, 0, 0, 0.15);
      --delete-hover-bg: rgba(210, 15, 57, 0.1);
      --success-bg-subtle: rgba(64, 160, 43, 0.1);
      --success-border-subtle: rgba(64, 160, 43, 0.3);
      --error-bg-subtle: rgba(210, 15, 57, 0.1);
      --error-border-subtle: rgba(210, 15, 57, 0.3);
      --warning-bright: #df8e1d;
    }
    ```

    **2. settings.ts — 新增 theme 字段 (per D-06)**

    - 在文件顶部添加类型导出：`export type ThemeMode = 'dark' | 'light' | 'system'`
    - 在 `SettingsState` 接口中新增：`theme: ThemeMode`
    - 在 `DEFAULTS` 对象中新增：`theme: 'dark' as ThemeMode`
    - 在 `initial` 解构后新增 `const theme = ref<ThemeMode>(initial.theme)`
    - 在 `settings` ref 聚合对象中新增：`theme: initial.theme`
    - 在 `return` 中新增 `theme` ref 和 `currentTheme` 计算属性

    **3. index.html — 添加 data-theme 属性 (per D-05)**

    将 `<html>` 改为 `<html data-theme="dark">`，确保首次加载即使用暗色主题（匹配默认设置）。

    **4. preload/index.ts — 新增主题 IPC 桥接 (per D-02, D-04)**

    在 `api` 对象中（建议放在 `onNotificationPlaySound` 之后）新增两个方法：

    ```typescript
    // 主题 IPC — 主进程系统主题变化通知渲染进程
    onThemeSystemChanged: (callback: (resolvedTheme: 'dark' | 'light') => void): (() => void) => {
      const handler = (_event: Electron.IpcRendererEvent, theme: string): void =>
        callback(theme as 'dark' | 'light')
      ipcRenderer.on('theme:system-changed', handler)
      return () => ipcRenderer.removeListener('theme:system-changed', handler)
    },
    // 主题 IPC — 渲染进程通知主进程主题变更
    updateTheme: (mode: 'dark' | 'light' | 'system', resolved?: 'dark' | 'light'): void =>
      ipcRenderer.send('theme:update', mode, resolved),
    ```
  </action>
  <verify>
    <automated>cd E:/work/projects/new-aitools && grep -c '\[data-theme="light"\]' src/renderer/src/assets/main.css && grep -c 'ThemeMode' src/renderer/src/stores/settings.ts && grep -c 'data-theme' src/renderer/index.html && grep -c 'onThemeSystemChanged' src/preload/index.ts</automated>
  </verify>
  <done>
    - main.css 包含 `[data-theme="dark"]` 和 `[data-theme="light"]` 两套完整变量值
    - main.css 新增了 `--bg-hover`, `--bg-overlay`, `--error-hover`, `--status-*-bg/border/text` 等辅助变量
    - settings.ts 导出 `ThemeMode` 类型，`SettingsState` 包含 `theme` 字段，默认值 `'dark'`
    - index.html 的 `<html>` 元素包含 `data-theme="dark"` 属性
    - preload/index.ts 包含 `onThemeSystemChanged` 和 `updateTheme` 两个 IPC 桥接方法
  </done>
</task>

<task type="auto">
  <name>Task 2: 渲染进程主题初始化逻辑 + SettingsDrawer 主题切换 UI</name>
  <files>
    src/renderer/src/main.ts
    src/renderer/src/components/SettingsDrawer.vue
  </files>
  <read_first>
    src/renderer/src/main.ts
    src/renderer/src/components/SettingsDrawer.vue
    src/renderer/src/stores/settings.ts
    src/preload/index.ts
  </read_first>
  <action>
    **1. main.ts — 添加主题初始化和系统主题变化监听 (per D-04, D-05)**

    在 `app.mount('#app')` 之后添加主题初始化逻辑：

    ```typescript
    // 主题初始化 — 读取设置，应用 data-theme 属性
    import { useSettingsStore } from './stores/settings'

    // 需要在 app.mount 之后使用 store（Pinia 已安装）
    ```

    具体做法：在 `app.mount('#app')` 之后，导入并使用 settings store：

    ```typescript
    // 应用主题 — 在 mount 之后执行，确保 Pinia 已初始化
    function applyTheme(mode: string): void {
      let resolved: 'dark' | 'light'
      if (mode === 'system') {
        resolved = window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light'
      } else {
        resolved = mode as 'dark' | 'light'
      }
      document.documentElement.setAttribute('data-theme', resolved)
      // 通知主进程当前 resolved 主题（用于 webview/通知窗口同步）
      window.api?.updateTheme(mode as 'dark' | 'light' | 'system', resolved)
    }

    // 初始化主题
    const settingsStore = useSettingsStore()
    applyTheme(settingsStore.settings.theme)

    // 监听设置中 theme 变化，自动应用
    import { watch } from 'vue'
    watch(() => settingsStore.settings.theme, (newTheme) => {
      applyTheme(newTheme)
    })

    // 监听主进程发来的系统主题变化通知 (per D-04)
    window.api?.onThemeSystemChanged((resolvedTheme) => {
      // 仅在用户选择 "跟随系统" 模式时响应
      if (settingsStore.settings.theme === 'system') {
        document.documentElement.setAttribute('data-theme', resolvedTheme)
        // 通知主进程更新 resolved 值
        window.api?.updateTheme('system', resolvedTheme)
      }
    })
    ```

    注意：`import { watch } from 'vue'` 已在文件中通过其他 import 引入。需要在文件顶部添加 `useSettingsStore` 的 import。同时将 `import { useSettingsStore } from './stores/settings'` 添加到已有的 import 语句中。`watch` 也需要在 import 中添加（当前未从 vue 导入）。

    实际上查看 main.ts 内容，目前只有：
    ```typescript
    import { createApp } from 'vue'
    import { createPinia } from 'pinia'
    import App from './App.vue'
    import './assets/main.css'

    const app = createApp(App)
    app.use(createPinia())
    app.mount('#app')
    ```
    
    需要修改为：
    ```typescript
    import { createApp, watch } from 'vue'
    import { createPinia } from 'pinia'
    import App from './App.vue'
    import './assets/main.css'
    import { useSettingsStore } from './stores/settings'

    const app = createApp(App)
    app.use(createPinia())
    app.mount('#app')

    // 主题系统初始化 (per D-05)
    function applyTheme(mode: string): void {
      let resolved: 'dark' | 'light'
      if (mode === 'system') {
        resolved = window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light'
      } else {
        resolved = mode as 'dark' | 'light'
      }
      document.documentElement.setAttribute('data-theme', resolved)
      window.api?.updateTheme(mode as 'dark' | 'light' | 'system', resolved)
    }

    const settingsStore = useSettingsStore()
    applyTheme(settingsStore.settings.theme)

    watch(() => settingsStore.settings.theme, (newTheme) => {
      applyTheme(newTheme)
    })

    window.api?.onThemeSystemChanged((resolvedTheme) => {
      if (settingsStore.settings.theme === 'system') {
        document.documentElement.setAttribute('data-theme', resolvedTheme)
        window.api?.updateTheme('system', resolvedTheme)
      }
    })
    ```

    **2. SettingsDrawer.vue — 添加主题切换区域 (per D-07)**

    在 `<script setup>` 中添加：
    ```typescript
    import { useSettingsStore, type ThemeMode } from '../stores/settings'

    // 主题选项定义
    const themeOptions: { value: ThemeMode; label: string }[] = [
      { value: 'dark', label: '暗色' },
      { value: 'light', label: '亮色' },
      { value: 'system', label: '跟随系统' }
    ]

    /** 更新主题设置 */
    function updateTheme(value: ThemeMode): void {
      settingsStore.update({ theme: value })
    }
    ```
    注意：`useSettingsStore` 已在现有代码中导入，只需添加 `type ThemeMode` 和 `themeOptions` 和 `updateTheme` 函数。

    在 `<template>` 的 `<div class="drawer-body">` 内部，在"声音提示" section 之前，添加主题选择 section：

    ```html
    <!-- 主题 (D-07) -->
    <section class="settings-section">
      <h3 class="section-title">主题</h3>
      <div
        v-for="opt in themeOptions"
        :key="opt.value"
        class="setting-row radio-row"
        @click="updateTheme(opt.value)"
      >
        <input
          type="radio"
          name="theme-mode"
          :value="opt.value"
          :checked="settingsStore.settings.theme === opt.value"
          class="radio-input"
          @click.stop="updateTheme(opt.value)"
        />
        <span class="setting-label">{{ opt.label }}</span>
      </div>
    </section>
    ```
    这个 section 使用了现有的 `setting-row`、`radio-row`、`radio-input`、`setting-label` CSS 类，无需添加新样式。
  </action>
  <verify>
    <automated>cd E:/work/projects/new-aitools && grep -c 'applyTheme' src/renderer/src/main.ts && grep -c 'onThemeSystemChanged' src/renderer/src/main.ts && grep -c 'themeOptions' src/renderer/src/components/SettingsDrawer.vue && grep -c 'updateTheme' src/renderer/src/components/SettingsDrawer.vue</automated>
  </verify>
  <done>
    - main.ts 包含 `applyTheme` 函数，根据 mode ('dark'/'light'/'system') 设置 `data-theme` 属性
    - main.ts 监听 settings.theme 变化自动调用 applyTheme
    - main.ts 监听 `onThemeSystemChanged` IPC，在 'system' 模式下响应系统主题变化
    - main.ts 通过 `updateTheme` IPC 通知主进程当前 resolved 主题
    - SettingsDrawer.vue 在"声音提示"之前显示"主题"区域
    - 主题区域包含三个单选按钮：暗色/亮色/跟随系统
    - 点击选项更新 settings store，触发自动主题切换
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 渲染进程 -> 主进程 IPC | theme:update 通道接受用户选择的 theme mode，需验证值合法性 |
| 主进程 -> 渲染进程 IPC | theme:system-changed 通道传递 resolved theme，需验证值为 'dark'/'light' |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | theme:update IPC | accept | 主题值仅影响 UI 外观，无安全敏感数据；主进程需验证值为合法枚举 |
| T-05-02 | Information Disclosure | localStorage theme | accept | theme 偏好不是敏感信息，localStorage 中已存储其他设置 |
</threat_model>

<verification>
1. 应用启动后检查 `<html>` 元素有 `data-theme="dark"` 属性
2. 在设置面板切换到"亮色"，页面颜色立即变化为 Catppuccin Latte 配色
3. 切换回"暗色"，页面恢复 Catppuccin Mocha 配色
4. 切换到"跟随系统"，页面根据 Windows 当前明暗设置自动选择
5. localStorage 中 `aitools-settings` 的 `theme` 字段正确持久化
6. 重启应用后主题设置保持
</verification>

<success_criteria>
1. main.css 包含完整的 `[data-theme="light"]` 变量块（所有颜色变量 + 辅助变量）
2. settings.ts 包含 `ThemeMode` 类型导出和 `theme` 字段
3. 切换主题后 `<html>` 元素的 `data-theme` 属性立即更新
4. 设置面板的主题区域有三个可点击选项
5. 无编译错误，`pnpm dev` 正常启动
</success_criteria>

<output>
After completion, create `.planning/phases/05-theme-personalization/05-01-SUMMARY.md`
</output>

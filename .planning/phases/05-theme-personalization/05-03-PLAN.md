---
phase: 05-theme-personalization
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/main/index.ts
  - src/main/claude/webview-server.ts
  - src/main/ipc/claude-webview.ts
  - src/main/notification/notification-manager.ts
  - src/notification/index.html
autonomous: true
requirements: [UX-06]

must_haves:
  truths:
    - "主进程监听 nativeTheme.on('updated')，检测到系统主题变化时通过 IPC 通知渲染进程"
    - "主进程创建 BrowserWindow 时 backgroundColor 根据当前主题动态设置"
    - "webview-server.ts 的 generateHostHtml 接收 theme 参数，生成对应颜色的 CSS"
    - "notification-manager.ts 创建通知时传递当前主题参数给通知窗口"
    - "notification/index.html 根据 URL 参数中的 theme 值应用对应颜色"
  artifacts:
    - path: "src/main/index.ts"
      provides: "nativeTheme 监听和 BrowserWindow 背景色动态化"
      contains: "nativeTheme"
    - path: "src/main/claude/webview-server.ts"
      provides: "参数化主题的 webview HTML 生成"
      contains: "generateHostHtml"
    - path: "src/main/notification/notification-manager.ts"
      provides: "传递主题参数给通知窗口"
      contains: "theme"
    - path: "src/notification/index.html"
      provides: "主题感知的通知 UI"
      contains: "params.get('theme')"
  key_links:
    - from: "src/main/index.ts"
      to: "src/renderer/src/main.ts"
      via: "theme:system-changed IPC 通道"
      pattern: "theme:system-changed"
    - from: "src/main/notification/notification-manager.ts"
      to: "src/notification/index.html"
      via: "URL 查询参数 theme=dark|light"
      pattern: "theme.*params"
---

<objective>
实现主进程侧的主题同步：nativeTheme 系统主题检测与通知、BrowserWindow 背景色动态化、webview HTML 参数化主题生成、通知窗口主题参数传递。

Purpose: 主进程管理独立的 BrowserWindow（主窗口、webview、通知），每个窗口需要知道当前主题来正确渲染背景和颜色。
Output: 主进程完全感知主题状态，所有独立窗口跟随主题变化。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-theme-personalization/05-CONTEXT.md
@.planning/phases/05-theme-personalization/05-RESEARCH.md
@.planning/phases/05-theme-personalization/05-01-SUMMARY.md

<interfaces>
<!-- preload/index.ts 暴露的 IPC 桥接（Plan 01 已创建） -->
```typescript
// 渲染进程 -> 主进程
updateTheme: (mode: 'dark' | 'light' | 'system', resolved?: 'dark' | 'light'): void =>
  ipcRenderer.send('theme:update', mode, resolved),

// 主进程 -> 渲染进程
onThemeSystemChanged: (callback: (resolvedTheme: 'dark' | 'light') => void): (() => void) => {
  ipcRenderer.on('theme:system-changed', handler)
  ...
}
```

<!-- webview-server.ts generateHostHtml 当前签名 -->
```typescript
function generateHostHtml(_extensionPath: string): string
// 需要改为接受 theme 参数
```

<!-- notification-manager.ts 创建通知的 URLSearchParams -->
```typescript
const params = new URLSearchParams({
  id,
  type: options.type,
  title: options.title,
  body: options.body,
  toolName: options.toolName || ''
})
// 需要新增 theme 参数
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 主进程 nativeTheme 监听 + BrowserWindow 背景动态化 + theme:update IPC 注册</name>
  <files>
    src/main/index.ts
    src/main/ipc/claude-webview.ts
  </files>
  <read_first>
    src/main/index.ts
    src/main/ipc/claude-webview.ts
  </read_first>
  <action>
    **1. src/main/index.ts — 添加 nativeTheme 导入和主题状态管理 (per D-04)**

    在文件顶部 import 中添加 `nativeTheme`：
    ```typescript
    import { app, shell, BrowserWindow, ipcMain, dialog, nativeTheme } from 'electron'
    ```

    在 `app.whenReady().then(...)` 回调中，`const mainWindow = createWindow()` 之后，添加：

    ```typescript
    // D-04: 系统主题检测 — 监听 Windows 明暗设置变化
    let currentResolvedTheme: 'dark' | 'light' = nativeTheme.shouldUseDarkColors ? 'dark' : 'light'

    nativeTheme.on('updated', () => {
      const resolved = nativeTheme.shouldUseDarkColors ? 'dark' : 'light'
      currentResolvedTheme = resolved
      mainWindow.webContents.send('theme:system-changed', resolved)
      // 同步更新窗口背景色
      mainWindow.setBackgroundColor(resolved === 'dark' ? '#1e1e2e' : '#eff1f5')
    })
    ```

    修改 `createWindow()` 函数中的 `backgroundColor` 使用变量：

    将第 54 行的 `backgroundColor: '#1e1e2e'` 改为动态读取：
    ```typescript
    backgroundColor: nativeTheme.shouldUseDarkColors ? '#1e1e2e' : '#eff1f5',
    ```

    但注意：`createWindow()` 在 `nativeTheme` 监听器设置之前调用，所以需要在 `createWindow` 函数内部读取初始值。由于 `nativeTheme` 在 `createWindow` 之前就已可用（它是 Electron 全局 API），直接在 `createWindow` 中使用 `nativeTheme.shouldUseDarkColors` 即可。

    添加 `theme:update` IPC 监听：
    ```typescript
    // 渲染进程通知主进程主题变更 — 存储当前 resolved 主题
    ipcMain.on('theme:update', (_event, _mode: string, resolved?: string) => {
      if (resolved === 'dark' || resolved === 'light') {
        currentResolvedTheme = resolved
        mainWindow.setBackgroundColor(resolved === 'dark' ? '#1e1e2e' : '#eff1f5')
      }
    })
    ```

    导出 `currentResolvedTheme` 的获取函数（供其他模块使用），在文件末尾 `app.on('window-all-closed', ...)` 之前添加：
    ```typescript
    /** 获取当前 resolved 主题（供通知管理器等模块使用） */
    export function getCurrentTheme(): 'dark' | 'light' {
      return currentResolvedTheme
    }
    ```

    **2. src/main/ipc/claude-webview.ts — 检查是否需要修改**

    读取 `claude-webview.ts`，确认 `startWebviewServer` 调用位置。当前 `webview-server.ts` 的 `startWebviewServer(extensionPath)` 不接受主题参数。但 server 是单例（只启动一次），主题变化需要通过不同方式处理。

    **策略调整：** 由于 webview server 只启动一次且为全局单例，`generateHostHtml` 的主题参数化需要在 server 层面存储主题值。具体在 Task 2 的 webview-server.ts 修改中处理。此文件可能不需要修改。

    如果 `claude-webview.ts` 中有其他需要感知主题的地方，添加必要的修改。否则保持不变。
  </action>
  <verify>
    <automated>cd E:/work/projects/new-aitools && grep -c 'nativeTheme' src/main/index.ts && grep -c 'theme:system-changed' src/main/index.ts && grep -c 'theme:update' src/main/index.ts && grep -c 'getCurrentTheme' src/main/index.ts</automated>
  </verify>
  <done>
    - src/main/index.ts 导入 nativeTheme
    - 注册 `nativeTheme.on('updated')` 监听器
    - 系统主题变化时通过 `theme:system-changed` IPC 通知渲染进程
    - 系统主题变化时同步更新 BrowserWindow 背景色
    - `createWindow()` 的 backgroundColor 根据初始主题设置
    - 注册 `theme:update` IPC 监听器，存储 resolved 主题
    - 导出 `getCurrentTheme()` 函数
  </done>
</task>

<task type="auto">
  <name>Task 2: webview-server.ts 参数化主题 + notification 主题参数传递</name>
  <files>
    src/main/claude/webview-server.ts
    src/main/notification/notification-manager.ts
    src/notification/index.html
  </files>
  <read_first>
    src/main/claude/webview-server.ts
    src/main/notification/notification-manager.ts
    src/notification/index.html
    src/main/index.ts
  </read_first>
  <action>
    **1. webview-server.ts — 参数化 generateHostHtml 接受主题 (per D-02)**

    webview server 是全局单例，主题需要在模块层面存储：

    在文件顶部（server 变量附近）添加：
    ```typescript
    /** 当前 resolved 主题，用于 webview HTML 生成 */
    let currentTheme: 'dark' | 'light' = 'dark'

    /** 更新 webview 主题 — 由主进程调用 */
    export function setWebviewTheme(theme: 'dark' | 'light'): void {
      currentTheme = theme
    }
    ```

    修改 `generateHostHtml` 函数签名和内部实现：

    将 `function generateHostHtml(_extensionPath: string): string` 改为 `function generateHostHtml(_extensionPath: string): string`（签名不变，但内部使用 `currentTheme` 变量）。

    在 `generateHostHtml` 的 HTML 字符串中，将所有硬编码的暗色 CSS 值替换为根据 `currentTheme` 动态生成的值。

    具体修改：在 `body` 的 `style` 中和 `:root` CSS 变量块中：

    **body style（第 259 行附近）：**
    ```javascript
    body { background: ${currentTheme === 'dark' ? '#1e1e2e' : '#eff1f5'}; color: ${currentTheme === 'dark' ? '#cdd6f4' : '#4c4f69'}; font-family: ... }
    ```

    **:root VSCode 变量（第 262-296 行）— 使用条件对象：**
    在 `generateHostHtml` 函数开头定义颜色映射：
    ```typescript
    const colors = currentTheme === 'dark' ? {
      bg: '#1e1e2e', fg: '#cdd6f4', sidebar: '#181825', activity: '#11111b',
      input: { bg: '#313244', fg: '#cdd6f4', border: '#45475a', placeholder: '#6c7086' },
      focus: '#89b4fa', badge: { bg: '#89b4fa', fg: '#1e1e2e' },
      button: { bg: '#89b4fa', fg: '#1e1e2e', hover: '#74c7ec' },
      scrollbar: { bg: '#45475a80', hover: '#585b7080' },
      list: { hover: '#313244', activeBg: '#45475a', activeFg: '#cdd6f4' },
      dropdown: { bg: '#313244', border: '#45475a', fg: '#cdd6f4' },
      menu: { bg: '#313244', fg: '#cdd6f4', border: '#45475a', selBg: '#45475a', selFg: '#cdd6f4' },
      notif: { bg: '#313244', fg: '#cdd6f4', border: '#45475a' },
      titleFg: '#cdd6f4', titleInactiveFg: '#6c7086'
    } : {
      bg: '#eff1f5', fg: '#4c4f69', sidebar: '#e6e9ef', activity: '#dce0e8',
      input: { bg: '#ccd0da', fg: '#4c4f69', border: '#bcc0cc', placeholder: '#9ca0b0' },
      focus: '#1e66f5', badge: { bg: '#1e66f5', fg: '#eff1f5' },
      button: { bg: '#1e66f5', fg: '#eff1f5', hover: '#209fb5' },
      scrollbar: { bg: '#bcc0cc80', hover: '#acb0be80' },
      list: { hover: '#ccd0da', activeBg: '#bcc0cc', activeFg: '#4c4f69' },
      dropdown: { bg: '#ccd0da', border: '#bcc0cc', fg: '#4c4f69' },
      menu: { bg: '#ccd0da', fg: '#4c4f69', border: '#bcc0cc', selBg: '#bcc0cc', selFg: '#4c4f69' },
      notif: { bg: '#ccd0da', fg: '#4c4f69', border: '#bcc0cc' },
      titleFg: '#4c4f69', titleInactiveFg: '#9ca0b0'
    }
    ```

    然后在 HTML 模板字符串中使用 `${colors.bg}` 等替换硬编码值。

    注意：webview 内的 badge 脚本（context usage badge）也使用硬编码颜色（`#f38ba8`, `#f9e2af`, `#89b4fa`），需要同样参数化：
    ```javascript
    // 在 badge 脚本中使用模板变量
    badge.style.color = pct > 90 ? '${colors.error || "#f38ba8"}' : pct > 70 ? '${colors.warning || "#f9e2af"}' : '${colors.focus}';
    ```

    等等 — 这里 `colors` 对象没有 error/warning。需要在 colors 中添加：
    - dark: `error: '#f38ba8'`, `warning: '#f9e2af'`
    - light: `error: '#d20f39'`, `warning: '#fe640b'`

    并在 badge 脚本中引用。

    同时需要在 `src/main/index.ts` 的 `theme:update` 监听器中调用 `setWebviewTheme`：
    ```typescript
    import { setWebviewTheme } from './claude/webview-server'
    ```
    在 `theme:update` handler 中添加：
    ```typescript
    setWebviewTheme(resolved)
    ```

    **2. notification-manager.ts — 传递主题参数 (per D-02)**

    在文件顶部导入：
    ```typescript
    import { getCurrentTheme } from '../index'
    ```

    在 `show` 方法中创建 URLSearchParams 时添加 theme 参数：
    ```typescript
    const params = new URLSearchParams({
      id,
      type: options.type,
      title: options.title,
      body: options.body,
      toolName: options.toolName || '',
      theme: getCurrentTheme()  // 新增：传递当前主题
    })
    ```

    **3. notification/index.html — 根据 theme 参数应用颜色 (per D-02)**

    在 `<script>` 标签中，在现有 `var params = ...` 之后添加：
    ```javascript
    var theme = params.get('theme') || 'dark'
    var colors = theme === 'light' ? {
      bg: '#eff1f5', border: '#bcc0cc', text: '#4c4f69', subtext: '#5c5f77',
      accent: '#1e66f5', success: '#40a02b', error: '#d20f39',
      inputBg: '#e6e9ef', hover: '#acb0be', accentHover: '#209fb5',
      successHover: '#6cc644', errorHover: '#e64553', btnText: '#eff1f5'
    } : {
      bg: '#1e1e2e', border: '#45475a', text: '#cdd6f4', subtext: '#a6adc8',
      accent: '#89b4fa', success: '#a6e3a1', error: '#f38ba8',
      inputBg: '#181825', hover: '#585b70', accentHover: '#74c7ec',
      successHover: '#94d991', errorHover: '#f07090', btnText: '#1e1e2e'
    }
    ```

    将所有硬编码颜色替换为 `colors.xxx` 引用：
    - `.notif-card` 的 `background`, `border`, `color` → 使用 `colors.bg`, `colors.border`, `colors.text`
    - `.notif-body` 的 `color` → `colors.subtext`
    - `.notif-btn-allow` 的 `background`, `color` → `colors.success`, `colors.btnText`
    - `.notif-btn-allow:hover` → `colors.successHover`
    - `.notif-btn-deny` → `colors.error`, `colors.btnText`
    - `.notif-btn-deny:hover` → `colors.errorHover`
    - `.notif-btn-view` → `colors.accent`, `colors.btnText`
    - `.notif-btn-view:hover` → `colors.accentHover`
    - `.notif-btn-error` → `colors.hover`, `colors.text`
    - `.notif-btn-error:hover` → `colors.border`
    - `.notif-input` 的 `border`, `background`, `color` → `colors.border`, `colors.inputBg`, `colors.text`
    - `.notif-input:focus` → `colors.accent`

    注意：由于 index.html 是静态 HTML，CSS 在 `<style>` 中硬编码，需要改为在 `<script>` 中动态注入 CSS 或使用内联样式。

    最简洁的方案：在 `<style>` 中将颜色替换为 CSS 变量，然后在 `<script>` 中设置这些变量：

    ```html
    <style>
      .notif-card {
        background: var(--notif-bg);
        border: 1px solid var(--notif-border);
        color: var(--notif-text);
        /* ... */
      }
      .notif-body { color: var(--notif-subtext); }
      .notif-btn-allow { background: var(--notif-success); color: var(--notif-btn-text); }
      .notif-btn-allow:hover { background: var(--notif-success-hover); }
      .notif-btn-deny { background: var(--notif-error); color: var(--notif-btn-text); }
      .notif-btn-deny:hover { background: var(--notif-error-hover); }
      .notif-btn-view { background: var(--notif-accent); color: var(--notif-btn-text); }
      .notif-btn-view:hover { background: var(--notif-accent-hover); }
      .notif-btn-error { background: var(--notif-hover); color: var(--notif-text); }
      .notif-btn-error:hover { background: var(--notif-border); }
      .notif-input {
        border: 1px solid var(--notif-border);
        background: var(--notif-input-bg);
        color: var(--notif-text);
      }
      .notif-input:focus { border-color: var(--notif-accent); }
    </style>
    ```

    在 `<script>` 开头动态设置 CSS 变量：
    ```javascript
    var theme = params.get('theme') || 'dark'
    var colors = theme === 'light' ? { /* ... */ } : { /* ... */ }
    var root = document.documentElement
    root.style.setProperty('--notif-bg', colors.bg)
    root.style.setProperty('--notif-border', colors.border)
    root.style.setProperty('--notif-text', colors.text)
    root.style.setProperty('--notif-subtext', colors.subtext)
    root.style.setProperty('--notif-accent', colors.accent)
    root.style.setProperty('--notif-success', colors.success)
    root.style.setProperty('--notif-error', colors.error)
    root.style.setProperty('--notif-input-bg', colors.inputBg)
    root.style.setProperty('--notif-hover', colors.hover)
    root.style.setProperty('--notif-accent-hover', colors.accentHover)
    root.style.setProperty('--notif-success-hover', colors.successHover)
    root.style.setProperty('--notif-error-hover', colors.errorHover)
    root.style.setProperty('--notif-btn-text', colors.btnText)
    ```

    同时需要移除 `<style>` 中所有对应的硬编码颜色值。
  </action>
  <verify>
    <automated>cd E:/work/projects/new-aitools && grep -c 'setWebviewTheme' src/main/claude/webview-server.ts && grep -c 'getCurrentTheme' src/main/notification/notification-manager.ts && grep -c "params.get('theme')" src/notification/index.html && grep -c 'var colors' src/notification/index.html</automated>
  </verify>
  <done>
    - webview-server.ts 模块级 `currentTheme` 变量和 `setWebviewTheme()` 导出函数
    - `generateHostHtml` 使用模板变量动态生成暗色/亮色 CSS
    - webview 中的 badge 脚本也使用动态颜色
    - src/main/index.ts 导入 `setWebviewTheme`，在 theme:update 时调用
    - notification-manager.ts 在 URLSearchParams 中传递 `theme: getCurrentTheme()`
    - notification/index.html 使用 CSS 变量 + JS 动态设置实现主题感知
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 渲染进程 -> 主进程 theme:update | 传递 mode 和 resolved 值，主进程需验证 |
| 主进程 -> webview HTML 生成 | 内部数据流，无外部输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | theme:update IPC resolved 参数 | mitigate | 主进程验证 resolved 值为 'dark' 或 'light'，忽略非法值 |
| T-05-05 | Spoofing | notification URL 参数 theme | accept | 通知窗口主题仅影响视觉，不影响安全功能 |
</threat_model>

<verification>
1. 启动应用，检查主窗口背景色与当前主题匹配
2. 在 Windows 设置中切换明暗模式，观察应用是否跟随变化（跟随系统模式下）
3. 打开 Claude 对话，检查 webview 颜色与主题匹配
4. 触发通知，检查通知窗口颜色与当前主题匹配
5. `pnpm dev` 编译无错误
</verification>

<success_criteria>
1. 主进程注册 nativeTheme 监听器，导出 getCurrentTheme 函数
2. BrowserWindow 创建时 backgroundColor 根据系统主题设置
3. webview-server.ts 的 HTML 生成根据主题动态输出 CSS
4. 通知窗口接收并应用主题参数
5. 无编译错误
</success_criteria>

<output>
After completion, create `.planning/phases/05-theme-personalization/05-03-SUMMARY.md`
</output>

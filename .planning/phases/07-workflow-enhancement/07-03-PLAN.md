---
phase: 07-workflow-enhancement
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/renderer/src/composables/useTokenUsage.ts
  - src/renderer/src/components/TokenUsagePanel.vue
  - src/renderer/src/components/SettingsDrawer.vue
autonomous: true
requirements: [UX-09]

must_haves:
  truths:
    - "用户可在设置抽屉中看到 Token 用量统计分组"
    - "统计面板显示今日/本周/本月汇总数据（总 token 数）"
    - "统计面板显示每日用量趋势柱状图"
    - "统计面板显示按工作区分组的用量列表"
    - "点击工作区列表项可展开查看该工作区趋势图"
    - "所有 UI 使用 CSS 变量，适配暗色/亮色主题"
  artifacts:
    - path: "src/renderer/src/composables/useTokenUsage.ts"
      provides: "Token 用量数据获取 composable"
      exports: ["useTokenUsage"]
    - path: "src/renderer/src/components/TokenUsagePanel.vue"
      provides: "Token 用量统计面板子组件（Chart.js 柱状图 + 汇总卡片 + 工作区列表）"
      contains: "Bar"
    - path: "src/renderer/src/components/SettingsDrawer.vue"
      provides: "设置抽屉新增用量统计分组"
      contains: "TokenUsagePanel"
  key_links:
    - from: "src/renderer/src/composables/useTokenUsage.ts"
      to: "src/preload/index.ts"
      via: "window.api.getTokenUsageStats(range)"
      pattern: "getTokenUsageStats"
    - from: "src/renderer/src/components/TokenUsagePanel.vue"
      to: "src/renderer/src/composables/useTokenUsage.ts"
      via: "import { useTokenUsage }"
      pattern: "useTokenUsage"
    - from: "src/renderer/src/components/SettingsDrawer.vue"
      to: "src/renderer/src/components/TokenUsagePanel.vue"
      via: "import TokenUsagePanel"
      pattern: "TokenUsagePanel"
---

<objective>
构建 Token 用量统计面板 UI，使用 Chart.js 柱状图展示每日趋势，汇总今日/本周/本月数据，按工作区分组展示。

Purpose: 将后端 token 数据可视化呈现给用户 (UX-09 D-05/D-06/D-07/D-08)
Output: TokenUsagePanel 组件 + useTokenUsage composable + SettingsDrawer 集成
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workflow-enhancement/07-CONTEXT.md
@.planning/phases/07-workflow-enhancement/07-RESEARCH.md
@.planning/phases/07-workflow-enhancement/07-PATTERNS.md
@.planning/phases/07-workflow-enhancement/07-01-SUMMARY.md

<interfaces>
<!-- preload API 类型声明 -- useTokenUsage 通过此接口获取数据 -->
```typescript
getTokenUsageStats: (range: string) => Promise<{
  totalInputTokens: number
  totalOutputTokens: number
  totalSessions: number
  days: { date: string; inputTokens: number; outputTokens: number }[]
  workspaces: { cwd: string; inputTokens: number; outputTokens: number; sessions: number }[]
}>
```

<!-- SettingsDrawer.vue 分组 section 结构 -->
```vue
<section class="settings-section">
  <h3 class="section-title">主题</h3>
  <!-- 设置项内容 -->
</section>
```

<!-- CSS 变量列表 -- 所有新 UI 必须使用 -->
```css
color: var(--text-primary);       /* 主文本 */
color: var(--text-secondary);     /* 次要文本 */
color: var(--text-muted);         /* 弱化文本 */
background: var(--bg-secondary);  /* 背景色 */
background: var(--bg-tertiary);   /* 三级背景 */
border: 1px solid var(--border);  /* 边框 */
color: var(--accent);             /* 强调色 */
```

<!-- Chart.js tree-shaking 注册模式 (RESEARCH Pattern 2) -->
```typescript
import {
  Chart as ChartJS,
  Title,
  Tooltip,
  Legend,
  BarElement,
  CategoryScale,
  LinearScale,
  type ChartData
} from 'chart.js'
import { Bar } from 'vue-chartjs'

ChartJS.register(Title, Tooltip, Legend, BarElement, CategoryScale, LinearScale)
```

<!-- composable 模式参考 -->
From src/renderer/src/composables/useNotification.ts:
```typescript
export function useNotification(): void {
  window.api.onNotificationFocusTab?.((channelId: string) => {
    window.dispatchEvent(new CustomEvent('notification:focus-tab', { detail: channelId }))
  })
}
```

<!-- SettingsDrawer.vue 导入和 section 插入位置 -->
From src/renderer/src/components/SettingsDrawer.vue:
- line 1-2: `<script setup>` 中导入 store
- line 219-237: "关闭窗口时" section 是最后一个 section
- 需要在 line 237 `</section>` 之后、line 238 `</div>` 之前插入新 section
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 useTokenUsage composable + TokenUsagePanel 组件</name>
  <files>src/renderer/src/composables/useTokenUsage.ts, src/renderer/src/components/TokenUsagePanel.vue</files>
  <read_first>
    - src/renderer/src/composables/useTokenUsage.ts (不存在则跳过)
    - src/renderer/src/components/TokenUsagePanel.vue (不存在则跳过)
    - src/renderer/src/composables/useNotification.ts (参考 composable 模式)
    - src/renderer/src/components/SettingsDrawer.vue (看 CSS 变量使用和分组结构)
    - src/renderer/src/assets/main.css (确认 CSS 变量名)
  </read_first>
  <acceptance_criteria>
    - `src/renderer/src/composables/useTokenUsage.ts` 文件存在
    - useTokenUsage.ts 导出 `useTokenUsage` 函数
    - useTokenUsage.ts 中通过 `window.api.getTokenUsageStats(range)` 获取数据
    - useTokenUsage.ts 返回响应式状态：`stats`, `loading`, `selectedRange`, `selectedWorkspace`, `fetchStats`
    - `src/renderer/src/components/TokenUsagePanel.vue` 文件存在
    - TokenUsagePanel.vue 使用 `<script setup lang="ts">` 模式
    - TokenUsagePanel.vue 导入并注册 Chart.js 组件（tree-shaking：Title, Tooltip, Legend, BarElement, CategoryScale, LinearScale）
    - TokenUsagePanel.vue 使用 vue-chartjs `<Bar>` 组件渲染柱状图
    - TokenUsagePanel.vue 通过 `getComputedStyle` 获取 CSS 变量实际值传给 Chart.js 颜色（RESEARCH Pitfall 5）
    - TokenUsagePanel.vue 显示时间范围选择器（今日/本周/本月）
    - TokenUsagePanel.vue 显示汇总卡片（总输入 token、总输出 token、总会话数）
    - TokenUsagePanel.vue 显示每日用量趋势柱状图（高度 200px，最近 30 天）
    - TokenUsagePanel.vue 显示工作区列表（每行：工作区名 + token 用量）
    - TokenUsagePanel.vue 工作区列表项可点击展开显示该工作区趋势图
    - TokenUsagePanel.vue 所有颜色使用 CSS 变量 `var(--*)`
    - Chart.js 柱状图配置：`responsive: true`, `maintainAspectRatio: false`, `beginAtZero: true`
  </acceptance_criteria>
  <action>
    1. 创建 `src/renderer/src/composables/useTokenUsage.ts`：
       ```typescript
       /**
        * Token 用量数据获取 composable
        * 通过 IPC 获取主进程中的 token 用量统计数据
        */
       import { ref, type Ref } from 'vue'

       interface DailyUsage {
         date: string
         inputTokens: number
         outputTokens: number
       }

       interface WorkspaceUsage {
         cwd: string
         inputTokens: number
         outputTokens: number
         sessions: number
       }

       interface TokenStats {
         totalInputTokens: number
         totalOutputTokens: number
         totalSessions: number
         days: DailyUsage[]
         workspaces: WorkspaceUsage[]
       }

       type TimeRange = 'today' | 'week' | 'month'

       export function useTokenUsage(): {
         stats: Ref<TokenStats | null>
         loading: Ref<boolean>
         selectedRange: Ref<TimeRange>
         selectedWorkspace: Ref<string | null>
         fetchStats: (range?: TimeRange) => Promise<void>
       } {
         const stats = ref<TokenStats | null>(null)
         const loading = ref(false)
         const selectedRange = ref<TimeRange>('week')
         const selectedWorkspace = ref<string | null>(null)

         async function fetchStats(range?: TimeRange): Promise<void> {
           if (range) selectedRange.value = range
           loading.value = true
           try {
             const result = await window.api.getTokenUsageStats(selectedRange.value)
             stats.value = result as TokenStats
           } catch (e) {
             console.error('[TokenUsage] 获取统计数据失败:', e)
             stats.value = null
           } finally {
             loading.value = false
           }
         }

         return { stats, loading, selectedRange, selectedWorkspace, fetchStats }
       }
       ```

    2. 创建 `src/renderer/src/components/TokenUsagePanel.vue`：

       ```vue
       <script setup lang="ts">
       import { ref, computed, watch, onMounted } from 'vue'
       import {
         Chart as ChartJS,
         Title,
         Tooltip,
         Legend,
         BarElement,
         CategoryScale,
         LinearScale,
         type ChartData,
         type ChartOptions
       } from 'chart.js'
       import { Bar } from 'vue-chartjs'
       import { useTokenUsage } from '../composables/useTokenUsage'

       // Tree-shaking: 只注册柱状图需要的组件
       ChartJS.register(Title, Tooltip, Legend, BarElement, CategoryScale, LinearScale)

       const { stats, loading, selectedRange, selectedWorkspace, fetchStats } = useTokenUsage()

       // 时间范围选项
       const rangeOptions: { value: string; label: string }[] = [
         { value: 'today', label: '今日' },
         { value: 'week', label: '本周' },
         { value: 'month', label: '本月' }
       ]

       // 展开的工作区
       const expandedWorkspace = ref<string | null>(null)

       // 获取 CSS 变量实际值 -- Canvas 不支持 CSS 变量 (RESEARCH Pitfall 5)
       function getCSSVar(name: string): string {
         return getComputedStyle(document.documentElement).getPropertyValue(name).trim()
       }

       // 柱状图数据
       const chartData = computed<ChartData<'bar'>>(() => {
         if (!stats.value) return { labels: [], datasets: [] }

         const days = stats.value.days
         const accentColor = getCSSVar('--accent') || '#89b4fa'
         const accentColorAlt = getCSSVar('--text-muted') || '#a6adc8'

         return {
           labels: days.map((d) => d.date.slice(5)), // MM-DD
           datasets: [
             {
               label: '输入 Token',
               backgroundColor: accentColor,
               data: days.map((d) => d.inputTokens)
             },
             {
               label: '输出 Token',
               backgroundColor: accentColorAlt,
               data: days.map((d) => d.outputTokens)
             }
           ]
         }
       })

       // 展开工作区的柱状图数据
       const workspaceChartData = computed<ChartData<'bar'>>(() => {
         if (!stats.value || !expandedWorkspace.value) return { labels: [], datasets: [] }

         // 从 days 数据中筛选该工作区的 sessions -- 这里用主数据的 days 作为替代
         // 实际上后端返回的是全局 days，工作区详情需要更细粒度数据
         // 当前方案：使用全局 days 数据作为工作区趋势的近似展示
         const days = stats.value.days
         const accentColor = getCSSVar('--accent') || '#89b4fa'

         return {
           labels: days.map((d) => d.date.slice(5)),
           datasets: [
             {
               label: 'Token 用量',
               backgroundColor: accentColor,
               data: days.map((d) => d.inputTokens + d.outputTokens)
             }
           ]
         }
       })

       // Chart.js 配置
       const chartOptions: ChartOptions<'bar'> = {
         responsive: true,
         maintainAspectRatio: false,
         plugins: {
           legend: {
             labels: {
               color: getCSSVar('--text-secondary') || '#bac2de',
               font: { size: 11 }
             }
           },
           tooltip: {
             mode: 'index',
             intersect: false
           }
         },
         scales: {
           x: {
             ticks: { color: getCSSVar('--text-muted') || '#a6adc8' },
             grid: { color: getCSSVar('--border') || '#313244' }
           },
           y: {
             beginAtZero: true,
             ticks: { color: getCSSVar('--text-muted') || '#a6adc8' },
             grid: { color: getCSSVar('--border') || '#313244' }
           }
         }
       }

       // 格式化 token 数量（K/M）
       function formatTokens(n: number): string {
         if (n >= 1_000_000) return `${(n / 1_000_000).toFixed(1)}M`
         if (n >= 1_000) return `${(n / 1_000).toFixed(1)}K`
         return String(n)
       }

       // 获取工作区显示名（取最后一级目录名）
       function workspaceName(cwd: string): string {
         const parts = cwd.replace(/\\/g, '/').split('/')
         return parts[parts.length - 1] || cwd
       }

       // 切换工作区展开
       function toggleWorkspace(cwd: string): void {
         expandedWorkspace.value = expandedWorkspace.value === cwd ? null : cwd
       }

       // 初始加载
       onMounted(() => {
         fetchStats()
       })
       </script>

       <template>
         <div class="token-usage-panel">
           <!-- 时间范围选择器 -->
           <div class="range-selector">
             <button
               v-for="opt in rangeOptions"
               :key="opt.value"
               :class="['range-btn', { active: selectedRange === opt.value }]"
               @click="fetchStats(opt.value as any)"
             >
               {{ opt.label }}
             </button>
           </div>

           <!-- 加载状态 -->
           <div v-if="loading" class="loading-state">加载中...</div>

           <!-- 无数据状态 -->
           <div v-else-if="!stats || stats.totalSessions === 0" class="empty-state">
             暂无用量数据
           </div>

           <!-- 数据展示 -->
           <template v-else>
             <!-- 汇总卡片 -->
             <div class="summary-cards">
               <div class="summary-card">
                 <span class="summary-label">输入 Token</span>
                 <span class="summary-value">{{ formatTokens(stats.totalInputTokens) }}</span>
               </div>
               <div class="summary-card">
                 <span class="summary-label">输出 Token</span>
                 <span class="summary-value">{{ formatTokens(stats.totalOutputTokens) }}</span>
               </div>
               <div class="summary-card">
                 <span class="summary-label">会话数</span>
                 <span class="summary-value">{{ stats.totalSessions }}</span>
               </div>
             </div>

             <!-- 每日趋势柱状图 (D-06) -->
             <div class="chart-container">
               <Bar :data="chartData" :options="chartOptions" />
             </div>

             <!-- 工作区分组统计 (D-08) -->
             <div v-if="stats.workspaces.length > 0" class="workspace-list">
               <h4 class="workspace-title">按工作区</h4>
               <div
                 v-for="ws in stats.workspaces"
                 :key="ws.cwd"
                 class="workspace-item"
                 @click="toggleWorkspace(ws.cwd)"
               >
                 <div class="workspace-row">
                   <span class="ws-name">{{ workspaceName(ws.cwd) }}</span>
                   <span class="ws-tokens">{{ formatTokens(ws.inputTokens + ws.outputTokens) }}</span>
                 </div>
                 <!-- 展开的工作区趋势图 -->
                 <div v-if="expandedWorkspace === ws.cwd" class="workspace-chart">
                   <Bar :data="workspaceChartData" :options="chartOptions" />
                 </div>
               </div>
             </div>
           </template>
         </div>
       </template>

       <style scoped>
       .token-usage-panel {
         padding: 4px 0;
       }

       .range-selector {
         display: flex;
         gap: 4px;
         margin-bottom: 12px;
       }

       .range-btn {
         flex: 1;
         padding: 4px 8px;
         border: 1px solid var(--border);
         background: var(--bg-tertiary);
         color: var(--text-secondary);
         border-radius: 4px;
         cursor: pointer;
         font-size: 12px;
         transition: all 0.15s;
       }

       .range-btn.active {
         background: var(--accent);
         color: #fff;
         border-color: var(--accent);
       }

       .loading-state,
       .empty-state {
         text-align: center;
         color: var(--text-muted);
         padding: 20px 0;
         font-size: 13px;
       }

       .summary-cards {
         display: grid;
         grid-template-columns: 1fr 1fr 1fr;
         gap: 8px;
         margin-bottom: 12px;
       }

       .summary-card {
         background: var(--bg-tertiary);
         border-radius: 6px;
         padding: 8px;
         text-align: center;
       }

       .summary-label {
         display: block;
         font-size: 11px;
         color: var(--text-muted);
         margin-bottom: 4px;
       }

       .summary-value {
         display: block;
         font-size: 16px;
         font-weight: 600;
         color: var(--text-primary);
       }

       .chart-container {
         height: 200px;
         margin-bottom: 12px;
         background: var(--bg-tertiary);
         border-radius: 6px;
         padding: 8px;
       }

       .workspace-title {
         font-size: 12px;
         color: var(--text-muted);
         margin: 0 0 6px 0;
         font-weight: 400;
       }

       .workspace-list {
         border-top: 1px solid var(--border);
         padding-top: 8px;
       }

       .workspace-item {
         cursor: pointer;
         padding: 4px 0;
       }

       .workspace-row {
         display: flex;
         justify-content: space-between;
         align-items: center;
         padding: 4px 0;
       }

       .ws-name {
         font-size: 13px;
         color: var(--text-primary);
         overflow: hidden;
         text-overflow: ellipsis;
         white-space: nowrap;
         max-width: 200px;
       }

       .ws-tokens {
         font-size: 12px;
         color: var(--text-secondary);
         font-weight: 500;
       }

       .workspace-chart {
         height: 150px;
         margin-top: 6px;
         background: var(--bg-tertiary);
         border-radius: 4px;
         padding: 6px;
       }
       </style>
       ```

       关键设计决策（Claude's Discretion）：
       - 汇总卡片使用 3 列网格（输入/输出/会话数）
       - 数字格式化使用 K/M 缩写
       - 柱状图高度固定 200px，工作区趋势图 150px
       - 时间范围选择器使用按钮组形式
       - 工作区名取最后一级目录名
       - 所有颜色使用 CSS 变量，Chart.js 颜色通过 getComputedStyle 获取实际值
  </action>
  <verify>
    <automated>cd E:/work/projects/new-aitools && grep -n "ChartJS.register" src/renderer/src/components/TokenUsagePanel.vue && grep -n "useTokenUsage" src/renderer/src/components/TokenUsagePanel.vue && grep -n "getTokenUsageStats" src/renderer/src/composables/useTokenUsage.ts && grep -n "var(--" src/renderer/src/components/TokenUsagePanel.vue | head -10</automated>
  </verify>
  <done>
    TokenUsagePanel 组件创建完成，包含时间范围选择器、汇总卡片、Chart.js 柱状图、工作区分组列表
    useTokenUsage composable 提供响应式数据获取能力
  </done>
</task>

<task type="auto">
  <name>Task 2: 将 TokenUsagePanel 集成到 SettingsDrawer</name>
  <files>src/renderer/src/components/SettingsDrawer.vue</files>
  <read_first>
    - src/renderer/src/components/SettingsDrawer.vue (完整文件，看 script setup line 1-34、最后一个 section line 219-237、slot 插入位置)
  </read_first>
  <acceptance_criteria>
    - SettingsDrawer.vue 的 `<script setup>` 中新增 `import TokenUsagePanel from './TokenUsagePanel.vue'`
    - SettingsDrawer.vue 中 "关闭窗口时" section 之后新增 "用量统计" section
    - "用量统计" section 包含 `<TokenUsagePanel />` 组件
    - section 标题为 "用量统计"
  </acceptance_criteria>
  <action>
    修改 `src/renderer/src/components/SettingsDrawer.vue`：

    1. 在 `<script setup>` 导入区（line 2 附近）添加：
       ```typescript
       import TokenUsagePanel from './TokenUsagePanel.vue'
       ```

    2. 在 "关闭窗口时" section 结束标签 `</section>`（约 line 237）之后，`</div>` 关闭 drawer-body（约 line 238）之前，插入新 section（D-05）：
       ```vue
       <!-- 用量统计 (UX-09) -->
       <section class="settings-section">
         <h3 class="section-title">用量统计</h3>
         <TokenUsagePanel />
       </section>
       ```
  </action>
  <verify>
    <automated>cd E:/work/projects/new-aitools && grep -n "TokenUsagePanel" src/renderer/src/components/SettingsDrawer.vue && grep -n "用量统计" src/renderer/src/components/SettingsDrawer.vue</automated>
  </verify>
  <done>
    TokenUsagePanel 组件成功嵌入 SettingsDrawer，用户通过设置齿轮图标 -> "用量统计" 分组即可查看 Token 统计数据
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 渲染进程内部 | 纯展示组件，数据来自 preload IPC，无直接外部输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Information Disclosure | TokenUsagePanel | accept | token 用量数据仅在本地存储和展示，不涉及网络传输；仅当前用户可见 |
</threat_model>

<verification>
1. `grep -n "TokenUsagePanel" src/renderer/src/components/SettingsDrawer.vue` -- 集成完成
2. `grep -n "ChartJS.register" src/renderer/src/components/TokenUsagePanel.vue` -- Chart.js tree-shaking 注册
3. `grep -n "getComputedStyle" src/renderer/src/components/TokenUsagePanel.vue` -- CSS 变量获取
4. `grep -n "var(--" src/renderer/src/components/TokenUsagePanel.vue | wc -l` -- CSS 变量使用数量 > 5
5. `pnpm dev` 启动后点击设置齿轮图标，确认 "用量统计" 分组可见
</verification>

<success_criteria>
1. 设置抽屉中显示 "用量统计" 分组
2. 点击今日/本周/本月切换时间范围，数据正确刷新
3. 汇总卡片显示总输入 token、总输出 token、会话数
4. 柱状图显示每日用量趋势，颜色跟随主题
5. 工作区列表可点击展开趋势图
6. 暗色/亮色主题切换后图表颜色正确更新
</success_criteria>

<output>
After completion, create `.planning/phases/07-workflow-enhancement/07-03-SUMMARY.md`
</output>

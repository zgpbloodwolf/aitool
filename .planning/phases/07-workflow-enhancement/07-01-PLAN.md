---
phase: 07-workflow-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/claude/token-usage-store.ts
  - src/main/ipc/token-usage.ts
  - src/main/ipc/claude-webview.ts
  - src/preload/index.ts
  - src/preload/index.d.ts
  - package.json
autonomous: true
requirements: [UX-09]

must_haves:
  truths:
    - "Channel 关闭时 token 用量数据写入 token-usage.json"
    - "历史数据超过 90 天自动清理"
    - "渲染进程可通过 IPC 获取指定时间范围的 token 用量统计"
  artifacts:
    - path: "src/main/claude/token-usage-store.ts"
      provides: "Token 用量 JSON 持久化、聚合、清理"
      exports: ["recordTokenUsage", "getTokenUsageStats"]
    - path: "src/main/ipc/token-usage.ts"
      provides: "Token 用量数据 IPC handlers"
      exports: ["registerTokenUsageHandlers"]
    - path: "src/main/ipc/claude-webview.ts"
      provides: "Channel 关闭时调用 recordTokenUsage"
      contains: "recordTokenUsage"
    - path: "src/preload/index.ts"
      provides: "getTokenUsageStats IPC 桥接 + onOpenDirectory 事件监听"
      contains: "getTokenUsageStats"
    - path: "src/preload/index.d.ts"
      provides: "新增 API 类型声明"
      contains: "getTokenUsageStats"
    - path: "package.json"
      provides: "chart.js + vue-chartjs 依赖"
      contains: "chart.js"
  key_links:
    - from: "src/main/ipc/claude-webview.ts"
      to: "src/main/claude/token-usage-store.ts"
      via: "handleCloseChannel 调用 recordTokenUsage"
      pattern: "recordTokenUsage\\("
    - from: "src/main/ipc/token-usage.ts"
      to: "src/main/claude/token-usage-store.ts"
      via: "IPC handler 调用 getTokenUsageStats"
      pattern: "getTokenUsageStats"
    - from: "src/preload/index.ts"
      to: "src/main/ipc/token-usage.ts"
      via: "ipcRenderer.invoke('token-usage:get-stats')"
      pattern: "token-usage:get-stats"
---

<objective>
构建 Token 用量数据管线：从 Channel 关闭时采集 token 数据，持久化到 JSON 文件，提供 IPC 通道供渲染进程查询。

Purpose: 为 Token 用量统计面板 (UX-09) 提供后端数据基础
Output: token-usage-store 模块 + IPC handlers + preload 桥接
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workflow-enhancement/07-CONTEXT.md
@.planning/phases/07-workflow-enhancement/07-RESEARCH.md
@.planning/phases/07-workflow-enhancement/07-PATTERNS.md

<interfaces>
<!-- Channel 接口 -- 需要在其中添加 cwd 字段 -->
From src/main/ipc/claude-webview.ts lines 28-39:
```typescript
interface Channel {
  process: ClaudeProcessManager
  permissionMode: string
  planText: string
  pendingToolUse: PendingToolUse | null
  permissionResolvers: Map<string, PermissionResolver>
  sentPermissionRequests: Set<string>
  totalInputTokens: number
  totalOutputTokens: number
  lastSessionId: string | null
}
```

<!-- handleCloseChannel -- 在此函数中插入 token 记录调用 -->
From src/main/ipc/claude-webview.ts lines 492-503:
```typescript
function handleCloseChannel(channelId: string): void {
  const channel = channels.get(channelId)
  if (!channel) return
  safeLog('[ClaudeIPC] 正在关闭频道:', channelId)
  for (const resolver of channel.permissionResolvers.values()) {
    clearTimeout(resolver.timeoutId)
  }
  channel.permissionResolvers.clear()
  channel.process.stop()
  channels.delete(channelId)
}
```

<!-- Channel 创建 -- 在此添加 cwd 字段 -->
From src/main/ipc/claude-webview.ts lines 314-325:
```typescript
channels.set(channelId, {
  process: proc,
  permissionMode: 'default',
  planText: '',
  pendingToolUse: null,
  permissionResolvers: new Map(),
  sentPermissionRequests: new Set(),
  totalInputTokens: 0,
  totalOutputTokens: 0,
  lastSessionId: resumeSessionId || null
})
```

<!-- JSON 持久化模式参考 -->
From src/main/tray/tray-manager.ts (close-behavior.json 模式):
```typescript
import { readFileSync, writeFileSync, existsSync } from 'fs'
import { join } from 'path'
import { app } from 'electron'

const CLOSE_BEHAVIOR_FILE = join(app.getPath('userData'), 'close-behavior.json')
```

<!-- IPC handler 注册模式参考 -->
From src/main/ipc/dialog.ts:
```typescript
import { dialog, BrowserWindow, ipcMain } from 'electron'

export function registerDialogHandlers(): void {
  ipcMain.handle('dialog:selectFolder', async () => {
    // ...
  })
}
```

<!-- Preload handle 模式 -->
From src/preload/index.ts lines 48-50:
```typescript
claudeGetContextUsage: (): Promise<
  Record<string, { inputTokens: number; outputTokens: number }>
> => ipcRenderer.invoke('claude:get-context-usage'),
```

<!-- Preload on/removeListener 模式 -->
From src/preload/index.ts lines 118-123:
```typescript
onCloseBehaviorChanged: (callback: (behavior: string) => void): (() => void) => {
  const handler = (_event: Electron.IpcRendererEvent, behavior: string): void =>
    callback(behavior)
  ipcRenderer.on('settings:close-behavior-changed', handler)
  return () => ipcRenderer.removeListener('settings:close-behavior-changed', handler)
},
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 token-usage-store 持久化模块</name>
  <files>src/main/claude/token-usage-store.ts, package.json</files>
  <read_first>
    - src/main/claude/token-usage-store.ts (不存在则跳过)
    - src/main/tray/tray-manager.ts (参考 close-behavior.json 持久化模式)
    - package.json (当前依赖列表)
  </read_first>
  <acceptance_criteria>
    - `src/main/claude/token-usage-store.ts` 文件存在
    - 文件导出 `recordTokenUsage` 函数
    - 文件导出 `getTokenUsageStats` 函数
    - 文件包含 `writeQueue` 或 `serializeWrite` 写入队列机制，防止并发写入损坏
    - 文件包含 90 天清理逻辑：`const MAX_DAYS = 90`
    - 数据结构使用 DayRecord + SessionRecord 接口（D-02）
    - `recordTokenUsage` 接受 `{ id, cwd, inputTokens, outputTokens, timestamp }` 参数
    - `getTokenUsageStats` 接受 `range` 参数（'today' | 'week' | 'month' | 'all'）返回聚合数据
    - `getTokenUsageStats` 返回值包含按工作区分组的统计
    - `package.json` 的 dependencies 中包含 `"chart.js"` 和 `"vue-chartjs"`
  </acceptance_criteria>
  <action>
    1. 在 `package.json` 的 `dependencies` 中新增 `"chart.js": "^4.5.1"` 和 `"vue-chartjs": "^5.3.3"`
    2. 创建 `src/main/claude/token-usage-store.ts`

    文件内容要求：

    a) 导入：`readFileSync`, `writeFileSync`, `existsSync` from `fs`；`join` from `path`；`app` from `electron`

    b) 定义接口（D-02 按天汇总结构）：
    ```typescript
    interface SessionRecord {
      id: string        // channel ID
      cwd: string       // 工作目录路径
      inputTokens: number
      outputTokens: number
      timestamp: number // 会话结束时间戳
    }

    interface DayRecord {
      date: string      // 'YYYY-MM-DD' 格式
      sessions: SessionRecord[]
    }

    interface TokenUsageData {
      days: DayRecord[]
    }

    interface TokenUsageStats {
      totalInputTokens: number
      totalOutputTokens: number
      totalSessions: number
      days: { date: string; inputTokens: number; outputTokens: number }[]
      workspaces: { cwd: string; inputTokens: number; outputTokens: number; sessions: number }[]
    }
    ```

    c) 文件路径定义：
    ```typescript
    const DATA_FILE = join(app.getPath('userData'), 'token-usage.json')
    const MAX_DAYS = 90 // D-04: 保留 90 天
    ```

    d) 写入队列机制（RESEARCH Pitfall 4 — 防止并发写入损坏）：
    ```typescript
    let writeQueue: Promise<void> = Promise.resolve()

    function serializeWrite(fn: () => void): void {
      writeQueue = writeQueue.then(() => {
        try { fn() } catch (e) { console.error('[TokenUsage] 写入失败:', e) }
      })
    }
    ```

    e) `loadData()` 函数：复用 close-behavior.json 的 readFileSync 模式，try-catch 包裹，返回默认值 `{ days: [] }`

    f) `saveData(data: TokenUsageData)` 函数：通过 `serializeWrite` 调用 writeFileSync，确保串行写入

    g) `recordTokenUsage(session: SessionRecord)` 函数：
       - 获取当天日期字符串 `new Date().toISOString().slice(0, 10)`
       - 加载现有数据
       - 找到或创建当天的 DayRecord
       - 将 session push 到当天的 sessions 数组
       - 清理超过 MAX_DAYS 的旧数据
       - 保存数据

    h) `getTokenUsageStats(range: string): TokenUsageStats` 函数：
       - 加载数据
       - 根据 range 计算日期范围：
         - 'today': 只看今天
         - 'week': 最近 7 天
         - 'month': 最近 30 天
         - 'all': 全部
       - 按天聚合 inputTokens/outputTokens
       - 按工作区 (cwd) 分组聚合
       - 返回 TokenUsageStats 对象

    i) 导出：`export { recordTokenUsage, getTokenUsageStats }` 和类型导出 `export type { TokenUsageStats, DayRecord, SessionRecord }`
  </action>
  <verify>
    <automated>cd E:/work/projects/new-aitools && npx tsc --noEmit src/main/claude/token-usage-store.ts --skipLibCheck 2>&1 | head -20 || echo "TypeScript check attempted"</automated>
  </verify>
  <done>
    token-usage-store.ts 文件创建完成，导出 recordTokenUsage 和 getTokenUsageStats，包含写入队列和 90 天清理逻辑
    package.json 已添加 chart.js 和 vue-chartjs 依赖
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 Token IPC handlers + 修改 handleCloseChannel 记录用量</name>
  <files>src/main/ipc/token-usage.ts, src/main/ipc/claude-webview.ts</files>
  <read_first>
    - src/main/ipc/claude-webview.ts (完整文件，重点看 Channel 接口 line 28-39、channels.set line 314-325、handleCloseChannel line 492-503)
    - src/main/ipc/dialog.ts (参考 IPC handler 注册模式)
    - src/main/index.ts (看 registerXxxHandlers 调用位置 line 127-142)
  </read_first>
  <acceptance_criteria>
    - `src/main/ipc/token-usage.ts` 文件存在并导出 `registerTokenUsageHandlers`
    - `token-usage.ts` 中注册 `ipcMain.handle('token-usage:get-stats', ...)`
    - `src/main/ipc/claude-webview.ts` 的 Channel 接口新增 `cwd: string` 字段
    - `src/main/ipc/claude-webview.ts` 的 `channels.set` 调用中新增 `cwd: cwd || process.cwd()`
    - `src/main/ipc/claude-webview.ts` 的 `handleCloseChannel` 函数在 `channel.process.stop()` 之前调用 `recordTokenUsage`
    - `src/main/ipc/claude-webview.ts` 顶部新增 `import { recordTokenUsage } from '../claude/token-usage-store'`
    - `src/main/index.ts` 新增 `import { registerTokenUsageHandlers } from './ipc/token-usage'`
    - `src/main/index.ts` 在 `registerClaudeWebviewHandlers()` 之后调用 `registerTokenUsageHandlers()`
  </acceptance_criteria>
  <action>
    1. 创建 `src/main/ipc/token-usage.ts`：
       ```typescript
       import { ipcMain } from 'electron'
       import { getTokenUsageStats } from '../claude/token-usage-store'

       export function registerTokenUsageHandlers(): void {
         ipcMain.handle('token-usage:get-stats', async (_event, range: string) => {
           return getTokenUsageStats(range || 'week')
         })
       }
       ```

    2. 修改 `src/main/ipc/claude-webview.ts`：
       a) 在文件顶部导入区新增：`import { recordTokenUsage } from '../claude/token-usage-store'`

       b) 在 Channel 接口（line 28-39）中添加 `cwd: string` 字段：
          在 `lastSessionId: string | null` 之后添加 `cwd: string`

       c) 在 `channels.set` 调用（约 line 314-325）中添加 cwd：
          在 `lastSessionId: resumeSessionId || null` 之后添加 `cwd: cwd || process.cwd()`

       d) 修改 `handleCloseChannel` 函数（约 line 492-503）：
          在 `safeLog` 之后、`for (const resolver...)` 之前插入 token 记录逻辑：
          ```typescript
          // Token 用量记录 -- 会话结束时聚合 (D-03)
          if (channel.totalInputTokens > 0 || channel.totalOutputTokens > 0) {
            recordTokenUsage({
              id: channelId,
              cwd: channel.cwd,
              inputTokens: channel.totalInputTokens,
              outputTokens: channel.totalOutputTokens,
              timestamp: Date.now()
            })
          }
          ```

    3. 修改 `src/main/index.ts`：
       a) 在导入区新增：`import { registerTokenUsageHandlers } from './ipc/token-usage'`
       b) 在 `registerClaudeWebviewHandlers()` 调用之后添加：`registerTokenUsageHandlers()`
  </action>
  <verify>
    <automated>cd E:/work/projects/new-aitools && grep -n "recordTokenUsage" src/main/ipc/claude-webview.ts && grep -n "registerTokenUsageHandlers" src/main/index.ts && grep -n "token-usage:get-stats" src/main/ipc/token-usage.ts</automated>
  </verify>
  <done>
    Token IPC handler 注册完成，handleCloseChannel 在 Channel 关闭时写入 token 用量数据，Channel 接口包含 cwd 字段用于工作区分组
  </done>
</task>

<task type="auto">
  <name>Task 3: 扩展 preload IPC 桥接</name>
  <files>src/preload/index.ts, src/preload/index.d.ts</files>
  <read_first>
    - src/preload/index.ts (完整文件，重点看 handle 模式 line 48-50 和 on/removeListener 模式 line 118-123)
    - src/preload/index.d.ts (完整文件，看类型声明模式)
  </read_first>
  <acceptance_criteria>
    - `src/preload/index.ts` 包含 `getTokenUsageStats` 方法，使用 `ipcRenderer.invoke('token-usage:get-stats', range)` 调用
    - `src/preload/index.ts` 包含 `onOpenDirectory` 方法，使用 `ipcRenderer.on('open-directory', handler)` 监听，返回 unsubscribe 函数
    - `src/preload/index.d.ts` 包含 `getTokenUsageStats` 类型声明
    - `src/preload/index.d.ts` 包含 `onOpenDirectory` 类型声明
    - `onOpenDirectory` 的类型声明为 `(callback: (dirPath: string) => void) => () => void`
  </acceptance_criteria>
  <action>
    1. 修改 `src/preload/index.ts`：
       a) 在 `claudeGetContextUsage` 方法附近添加 Token 用量 IPC 桥接（handle 模式）：
          ```typescript
          // Token 用量统计 (UX-09)
          getTokenUsageStats: (range: string): Promise<TokenUsageStats> =>
            ipcRenderer.invoke('token-usage:get-stats', range),
          ```
          注意：需要在文件顶部添加类型导入，从 token-usage-store 导入类型不太合适（preload 不能直接引用主进程），所以使用内联类型或 any。

          实际做法：直接在 api 对象中内联类型：
          ```typescript
          getTokenUsageStats: (range: string): Promise<unknown> =>
            ipcRenderer.invoke('token-usage:get-stats', range),
          ```

       b) 在 `onUpdaterError` 之后、`updaterDownload` 之前添加右键菜单打开目录事件监听（on/removeListener 模式）：
          ```typescript
          // 右键菜单打开目录 (UX-10)
          onOpenDirectory: (callback: (dirPath: string) => void): (() => void) => {
            const handler = (_event: Electron.IpcRendererEvent, dirPath: string): void =>
              callback(dirPath)
            ipcRenderer.on('open-directory', handler)
            return () => ipcRenderer.removeListener('open-directory', handler)
          },
          ```

    2. 修改 `src/preload/index.d.ts`：
       在 `Window.api` 接口中，`exportSession` 之后添加：
       ```typescript
       // Token 用量统计 (UX-09)
       getTokenUsageStats: (range: string) => Promise<{
         totalInputTokens: number
         totalOutputTokens: number
         totalSessions: number
         days: { date: string; inputTokens: number; outputTokens: number }[]
         workspaces: { cwd: string; inputTokens: number; outputTokens: number; sessions: number }[]
       }>
       // 右键菜单打开目录 (UX-10)
       onOpenDirectory: (callback: (dirPath: string) => void) => () => void
       ```
  </action>
  <verify>
    <automated>cd E:/work/projects/new-aitools && grep -n "getTokenUsageStats" src/preload/index.ts src/preload/index.d.ts && grep -n "onOpenDirectory" src/preload/index.ts src/preload/index.d.ts</automated>
  </verify>
  <done>
    Preload 层完成 IPC 桥接：getTokenUsageStats handle 模式 + onOpenDirectory 事件监听模式，类型声明同步更新
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 渲染进程 -> IPC -> 主进程 token-usage-store | 渲染进程通过 IPC 查询 token 数据，range 参数需验证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | token-usage:get-stats IPC | mitigate | 在 IPC handler 中验证 range 参数为合法枚举值 ('today'/'week'/'month'/'all')，非法值降级为 'week' |
| T-07-02 | Denial of Service | recordTokenUsage | accept | 仅在 Channel 关闭时写入，频率低；JSON 文件数据量受 90 天限制，不会无限增长 |
</threat_model>

<verification>
1. `grep -n "recordTokenUsage" src/main/ipc/claude-webview.ts` -- 确认 handleCloseChannel 中调用了 recordTokenUsage
2. `grep -n "getTokenUsageStats" src/preload/index.ts` -- 确认 preload 桥接已注册
3. `grep -n "token-usage:get-stats" src/main/ipc/token-usage.ts` -- 确认 IPC handler 已注册
4. `grep "chart.js" package.json` -- 确认依赖已添加
</verification>

<success_criteria>
1. Channel 关闭时 token 数据（inputTokens/outputTokens/cwd）被写入 token-usage.json
2. 90 天自动清理逻辑在每次写入时执行
3. 渲染进程可通过 `window.api.getTokenUsageStats('week')` 获取聚合数据
4. 写入队列机制防止并发写入损坏
5. Channel 接口新增 cwd 字段，handleLaunchClaude 中记录 cwd
</success_criteria>

<output>
After completion, create `.planning/phases/07-workflow-enhancement/07-01-SUMMARY.md`
</output>

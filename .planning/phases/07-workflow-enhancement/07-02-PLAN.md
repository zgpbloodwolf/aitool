---
phase: 07-workflow-enhancement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - build/installer.nsh
  - electron-builder.yml
  - src/main/index.ts
  - src/renderer/src/components/ChatPanel.vue
autonomous: true
requirements: [UX-10]

must_haves:
  truths:
    - "Windows 资源管理器右键文件夹显示 '使用 AI Tools 打开' 菜单项"
    - "右键菜单项带应用图标"
    - "应用未启动时右键菜单启动应用并打开目录"
    - "应用已启动时右键菜单在已有窗口中创建新标签页并切换到该目录"
    - "安装/卸载时自动注册/清理注册表"
  artifacts:
    - path: "build/installer.nsh"
      provides: "NSIS 安装/卸载时注册表操作宏"
      contains: "customInstall"
    - path: "electron-builder.yml"
      provides: "NSIS 配置引用 installer.nsh"
      contains: "include:"
    - path: "src/main/index.ts"
      provides: "单实例锁 + second-instance 事件 + 注册表检查"
      contains: "requestSingleInstanceLock"
    - path: "src/renderer/src/components/ChatPanel.vue"
      provides: "open-directory 事件监听，创建新标签页并设置 cwd"
      contains: "onOpenDirectory"
  key_links:
    - from: "build/installer.nsh"
      to: "Windows Registry"
      via: "NSIS WriteRegStr 写入 HKCR"
      pattern: "WriteRegStr"
    - from: "src/main/index.ts"
      to: "src/renderer/src/components/ChatPanel.vue"
      via: "mainWindow.webContents.send('open-directory', dirPath)"
      pattern: "open-directory"
    - from: "src/renderer/src/components/ChatPanel.vue"
      to: "src/main/ipc/claude-webview.ts"
      via: "window.api.claudeSetCwd + addNewTab"
      pattern: "claudeSetCwd"
---

<objective>
实现 Windows 右键菜单集成和单实例通信机制，使用户可以从资源管理器右键菜单直接打开目录到 AI Tools。

Purpose: 系统级集成提升日常使用效率 (UX-10)
Output: NSIS 安装脚本 + 单实例锁 + 注册表检查 + 渲染进程目录接收
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workflow-enhancement/07-CONTEXT.md
@.planning/phases/07-workflow-enhancement/07-RESEARCH.md
@.planning/phases/07-workflow-enhancement/07-PATTERNS.md

<interfaces>
<!-- electron-builder.yml 当前 NSIS 配置 -->
From electron-builder.yml lines 29-34:
```yaml
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  artifactName: ${name}-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
```

<!-- win.executableName = ai-tools -->
From electron-builder.yml line 25:
```yaml
win:
  executableName: ai-tools
```

<!-- 主进程入口 -- 需要在 app.whenReady 之前添加单实例锁 -->
From src/main/index.ts lines 127-173:
```typescript
app.whenReady().then(() => {
  app.setAppUserModelId('com.aitools.desktop')
  // ... registerXxxHandlers()
  const mainWindow = createWindow()
  // ...
})
```

<!-- ChatPanel.vue -- addNewTab 和 webview 初始化 -->
From src/renderer/src/components/ChatPanel.vue lines 292-297:
```typescript
function addNewTab() {
  const id = generateTabId()
  tabs.value.push({ id, label: `对话 ${tabs.value.length + 1}` })
  tabOrder.set(id, tabs.value.length - 1)
  activeTabId.value = id
}
```

<!-- ChatPanel.vue 中 initWebview 使用 extPath 和 port -->
From src/renderer/src/components/ChatPanel.vue lines 267-279:
```typescript
const extPath = await window.api.claudeGetExtensionPath()
const port = await window.api.claudeStartWebviewServer(extPath)
webviewUrl.value = `http://127.0.0.1:${port}/`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 NSIS 安装脚本 + 配置 electron-builder + 单实例锁</name>
  <files>build/installer.nsh, electron-builder.yml, src/main/index.ts</files>
  <read_first>
    - electron-builder.yml (完整文件，确认 nsis 段和 win.executableName)
    - src/main/index.ts (完整文件，看 app.whenReady 位置和导入)
    - build/installer.nsh (不存在则跳过)
  </read_first>
  <acceptance_criteria>
    - `build/installer.nsh` 文件存在
    - installer.nsh 包含 `!macro customInstall` 宏
    - installer.nsh 包含 `!macro customUnInstall` 宏
    - installer.nsh 中使用 `$INSTDIR\ai-tools.exe`（不是 `AI Tools.exe`，与 executableName 一致）
    - installer.nsh 中使用 `SHCTX`（不是直接写 `HKCR`）
    - installer.nsh 注册 `Software\Classes\Directory\shell\ai-tools`
    - installer.nsh 注册 `Software\Classes\Directory\Background\shell\ai-tools`
    - installer.nsh 中每个注册表项包含 `Icon` 值指向 exe 路径 (D-11)
    - installer.nsh 中 customUnInstall 删除两个注册表键
    - electron-builder.yml 的 nsis 段新增 `include: build/installer.nsh`
    - src/main/index.ts 在 `app.whenReady()` 之前添加 `requestSingleInstanceLock()` 逻辑
    - src/main/index.ts 添加 `second-instance` 事件处理
    - src/main/index.ts 的 second-instance 中过滤 argv 匹配 `^[A-Z]:\\` 格式的路径
    - src/main/index.ts 的 second-instance 中调用 `mainWindow.webContents.send('open-directory', dirPath)`
    - src/main/index.ts 的 app.whenReady 中添加 `ensureContextMenuRegistered()` 调用
    - src/main/index.ts 包含 `ensureContextMenuRegistered` 函数，使用 reg.exe 检查并补注册
  </acceptance_criteria>
  <action>
    1. 创建 `build/installer.nsh`（D-09: NSIS 安装脚本注册表操作）：

       ```nsis
       ; Windows 右键菜单集成 (UX-10)
       ; 文件夹右键 + 文件夹背景右键，不含驱动器右键 (D-10)

       !macro customInstall
         ; 文件夹右键菜单
         WriteRegStr SHCTX "Software\Classes\Directory\shell\ai-tools" "" "使用 AI Tools 打开"
         WriteRegStr SHCTX "Software\Classes\Directory\shell\ai-tools" "Icon" "$INSTDIR\ai-tools.exe"
         WriteRegStr SHCTX "Software\Classes\Directory\shell\ai-tools\command" "" '"$INSTDIR\ai-tools.exe" "%V"'

         ; 目录背景右键菜单
         WriteRegStr SHCTX "Software\Classes\Directory\Background\shell\ai-tools" "" "使用 AI Tools 打开当前目录"
         WriteRegStr SHCTX "Software\Classes\Directory\Background\shell\ai-tools" "Icon" "$INSTDIR\ai-tools.exe"
         WriteRegStr SHCTX "Software\Classes\Directory\Background\shell\ai-tools\command" "" '"$INSTDIR\ai-tools.exe" "%V"'
       !macroend

       !macro customUnInstall
         DeleteRegKey SHCTX "Software\Classes\Directory\shell\ai-tools"
         DeleteRegKey SHCTX "Software\Classes\Directory\Background\shell\ai-tools"
       !macroend
       ```

       关键注意事项（RESEARCH Pitfall 6 + PATTERNS 指引）：
       - 使用 `$INSTDIR\ai-tools.exe`（与 electron-builder.yml 中 `win.executableName: ai-tools` 一致）
       - 使用 `SHCTX` 让 NSIS 根据安装模式自动路由到 HKLM 或 HKCU
       - `customInstall` 和 `customUnInstall` 宏名称由 electron-builder 约定，不可更改

    2. 修改 `electron-builder.yml`：
       在 nsis 段中添加 `include: build/installer.nsh`：
       ```yaml
       nsis:
         oneClick: false
         allowToChangeInstallationDirectory: true
         createDesktopShortcut: true
         artifactName: ${name}-${version}-setup.${ext}
         shortcutName: ${productName}
         uninstallDisplayName: ${productName}
         include: build/installer.nsh
       ```

    3. 修改 `src/main/index.ts`：

       a) 在 `if (!app.isPackaged && process.env['ELECTRON_DEBUG'])` 之前（约 line 123 之前），添加单实例锁逻辑（D-12）：
       ```typescript
       // D-12: 单实例锁 + 右键菜单通信 (UX-10)
       const gotTheLock = app.requestSingleInstanceLock()
       if (!gotTheLock) {
         app.quit()
       } else {
         app.on('second-instance', (_event, argv, _workingDirectory) => {
           // 从命令行参数中提取目录路径
           // 过滤掉 electron 开发模式参数和 .exe 文件 (RESEARCH Pitfall 3)
           const dirPath = argv.find((arg) =>
             /^[A-Z]:\\/i.test(arg) && !arg.endsWith('.exe')
           )
           const mainWindow = BrowserWindow.getAllWindows()[0]
           if (mainWindow) {
             mainWindow.show()
             mainWindow.focus()
             if (dirPath) {
               // 通过 IPC 通知渲染进程创建新标签页并设置工作目录
               mainWindow.webContents.send('open-directory', dirPath)
             }
           }
         })
       }
       ```

       b) 在 `setupAutoUpdater(mainWindow)` 之后（约 line 168 之后），添加注册表检查调用（D-09 双重注册保障）：
       ```typescript
       // D-09: 首次启动检查右键菜单注册表 (UX-10)
       ensureContextMenuRegistered()
       ```

       c) 在文件末尾 `getCurrentTheme` 函数之后添加 `ensureContextMenuRegistered` 函数：
       ```typescript
       /** 检查并补注册右键菜单（D-09 双重注册保障） */
       function ensureContextMenuRegistered(): void {
         if (!app.isPackaged) return // 开发模式不注册

         const exePath = app.getPath('exe')
         const regCheckCmd = 'reg query "HKCU\\Software\\Classes\\Directory\\shell\\ai-tools" /ve 2>nul'

         try {
           execSync(regCheckCmd, { stdio: 'pipe' })
           // 键已存在，无需操作
         } catch {
           // 键不存在，补注册
           const regKey = 'HKCU\\Software\\Classes\\Directory\\shell\\ai-tools'
           execSync(`reg add "${regKey}" /ve /d "使用 AI Tools 打开" /f`, { stdio: 'pipe' })
           execSync(`reg add "${regKey}" /v Icon /d "${exePath}" /f`, { stdio: 'pipe' })
           execSync(`reg add "${regKey}\\command" /ve /d "\\"${exePath}\\" \\"%V\\"" /f`, {
             stdio: 'pipe'
           })

           const bgKey = 'HKCU\\Software\\Classes\\Directory\\Background\\shell\\ai-tools'
           execSync(`reg add "${bgKey}" /ve /d "使用 AI Tools 打开当前目录" /f`, { stdio: 'pipe' })
           execSync(`reg add "${bgKey}" /v Icon /d "${exePath}" /f`, { stdio: 'pipe' })
           execSync(`reg add "${bgKey}\\command" /ve /d "\\"${exePath}\\" \\"%V\\"" /f`, {
             stdio: 'pipe'
           })
         }
       }
       ```

       注意：`execSync` 已在文件顶部导入（line 3），无需重复导入。
  </action>
  <verify>
    <automated>cd E:/work/projects/new-aitools && grep -n "customInstall" build/installer.nsh && grep -n "ai-tools.exe" build/installer.nsh && grep -n "requestSingleInstanceLock" src/main/index.ts && grep -n "open-directory" src/main/index.ts && grep -n "ensureContextMenuRegistered" src/main/index.ts && grep -n "include:" electron-builder.yml</automated>
  </verify>
  <done>
    NSIS 安装脚本在安装/卸载时自动注册/清理右键菜单注册表项
    electron-builder.yml 引用 installer.nsh
    单实例锁确保右键菜单参数传递到已有实例
    首次启动代码检查注册表完整性并补注册
  </done>
</task>

<task type="auto">
  <name>Task 2: ChatPanel 监听 open-directory 事件创建新标签页</name>
  <files>src/renderer/src/components/ChatPanel.vue</files>
  <read_first>
    - src/renderer/src/components/ChatPanel.vue (完整文件，重点看 addNewTab 函数 line 292-297、initWebview line 254-289、onMounted 生命周期、window.api.claudeSetCwd 调用、onBeforeUnmount 清理)
  </read_first>
  <acceptance_criteria>
    - ChatPanel.vue 中注册 `window.api.onOpenDirectory` 事件监听
    - 收到 dirPath 后调用 `window.api.claudeSetCwd(dirPath)` 设置工作目录
    - 收到 dirPath 后调用 `addNewTab()` 创建新标签页
    - onBeforeUnmount 中清理 onOpenDirectory 的 unsubscribe 函数
    - 监听器在 onMounted 中注册
  </acceptance_criteria>
  <action>
    修改 `src/renderer/src/components/ChatPanel.vue`：

    1. 在 `onMounted` 生命周期中（在现有事件监听注册之后），添加 open-directory 事件监听：
       ```typescript
       // UX-10: 右键菜单打开目录 — 创建新标签页并设置工作目录
       offOpenDirectory = window.api.onOpenDirectory?.((dirPath: string) => {
         if (dirPath) {
           window.api.claudeSetCwd(dirPath)
           addNewTab()
         }
       })
       ```
       注意：`onOpenDirectory` 可能在 preload 中尚未定义（开发阶段），使用可选链 `?.` 防护。

    2. 在文件顶部变量声明区添加 unsubscribe 变量：
       ```typescript
       let offOpenDirectory: (() => void) | undefined
       ```

    3. 在 `onBeforeUnmount` 生命周期中添加清理：
       ```typescript
       offOpenDirectory?.()
       ```
  </action>
  <verify>
    <automated>cd E:/work/projects/new-aitools && grep -n "onOpenDirectory" src/renderer/src/components/ChatPanel.vue && grep -n "offOpenDirectory" src/renderer/src/components/ChatPanel.vue</automated>
  </verify>
  <done>
    ChatPanel 监听 open-directory IPC 事件，收到目录路径后创建新标签页并设置 cwd
    事件监听在组件卸载时正确清理
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Windows Shell -> 应用启动 | 右键菜单传入的目录路径为外部输入，需验证 |
| 主进程 -> 渲染进程 | second-instance 事件中的 argv 传递到渲染进程 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Spoofing | second-instance argv | mitigate | argv 过滤只接受 `^[A-Z]:\\` 格式路径，排除 .exe 后缀，防止注入恶意参数 |
| T-07-04 | Elevation | 注册表操作 | accept | 仅写入 HKCU（当前用户），不需要管理员权限；HKCU 下的 Software\Classes 对当前用户有效 |
| T-07-05 | Tampering | 注册表键值 | accept | 注册表键值在安装时写入，应用代码仅做检查补注册，不存在运行时高频修改 |
</threat_model>

<verification>
1. `grep -n "customInstall" build/installer.nsh` -- NSIS 安装宏存在
2. `grep -n "customUnInstall" build/installer.nsh` -- NSIS 卸载宏存在
3. `grep -n "ai-tools.exe" build/installer.nsh` -- 使用正确的 exe 名称
4. `grep -n "requestSingleInstanceLock" src/main/index.ts` -- 单实例锁已添加
5. `grep -n "open-directory" src/main/index.ts` -- IPC 事件发送
6. `grep -n "onOpenDirectory" src/renderer/src/components/ChatPanel.vue` -- 渲染进程监听
7. `grep -n "ensureContextMenuRegistered" src/main/index.ts` -- 注册表检查函数存在
</verification>

<success_criteria>
1. NSIS 安装脚本写入 Directory\shell 和 Directory\Background\shell 注册表项
2. 卸载时清理对应注册表项
3. 单实例锁阻止多实例，已有实例收到 second-instance 事件
4. 首次启动检查注册表完整性
5. 渲染进程接收 open-directory 事件并创建新标签页
</success_criteria>

<output>
After completion, create `.planning/phases/07-workflow-enhancement/07-02-SUMMARY.md`
</output>

---
phase: 08-advanced-features
plan: 04
type: execute
wave: 2
depends_on:
  - 08-01-PLAN
  - 08-02-PLAN
files_modified:
  - src/main/claude/webview-server.ts
  - src/renderer/src/components/ChatPanel.vue
  - src/preload/index.ts
autonomous: true
requirements:
  - UX-12

must_haves:
  truths:
    - "每条用户消息右侧出现分支按钮，悬停时显示"
    - "点击分支按钮后从该消息创建新分支标签页"
    - "分叉点在消息流中显示 'N 个分支' 标签"
    - "点击分支标签展开分支列表可选择切换"
    - "标签上标注分支来源（如 '分支 #1'）"
    - "标签右键菜单可重命名分支"
  artifacts:
    - path: "src/main/claude/webview-server.ts"
      provides: "webview 内分支按钮和指示器注入"
      contains: "injectBranchButtons"
    - path: "src/renderer/src/components/ChatPanel.vue"
      provides: "分支创建处理 + 标签页分支信息展示"
      contains: "branch:create"
  key_links:
    - from: "src/main/claude/webview-server.ts"
      to: "ChatPanel.vue"
      via: "postMessage branch:create 事件"
      pattern: "branch:create"
    - from: "ChatPanel.vue"
      to: "src/main/ipc/claude-webview.ts"
      via: "IPC branch:create"
      pattern: "window\\.api\\.branchCreate"
---

<objective>
在 webview 中注入分支按钮（MutationObserver 模式），处理分支创建请求，展示内联分支指示器，并在标签页上标注分支信息。

Purpose: 实现用户可感知的分支交互（D-07, D-08, D-09, D-11），包括消息旁分支按钮、分叉点指示器、标签页分支标签和重命名。

Output: 改造后的 webview-server.ts（分支按钮/指示器注入）、ChatPanel.vue（分支创建处理和标签展示）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-advanced-features/08-CONTEXT.md
@.planning/phases/08-advanced-features/08-RESEARCH.md
@.planning/phases/08-advanced-features/08-01-SUMMARY.md
@.planning/phases/08-advanced-features/08-02-SUMMARY.md

<interfaces>
<!-- Wave 1 完成后可用的接口 -->

从 src/main/claude/branch-manager.ts (Wave 1 Plan 02 创建):
```typescript
interface BranchCreateResult {
  success: boolean
  branch?: BranchMeta
  channelId?: string
  error?: string
}
// createBranch(parentSessionId, branchPointIndex, cwd, handleLaunchFn, sendToWebviewFn)
```

从 src/preload/index.ts (Wave 1 Plan 02 扩展):
```typescript
branchCreate: (data: {
  parentSessionId: string; branchPointIndex: number; cwd: string
}): Promise<{ success: boolean; branch?: unknown; channelId?: string; error?: string }>
branchList: (parentSessionId: string): Promise<unknown[]>
branchListAtPoint: (data: {
  parentSessionId: string; branchPointIndex: number
}): Promise<unknown[]>
branchRename: (branchId: string, newLabel: string): Promise<boolean>
branchCanCreate: (parentSessionId: string): Promise<{ canCreate: boolean; remaining: number }>
```

从 src/main/claude/webview-server.ts:
```typescript
// generateHostHtml() 末尾的 <script> 块 — 已有 context usage badge 注入逻辑
// 分支按钮注入需要在同一位置新增
```

从 src/renderer/src/components/ChatPanel.vue:
```typescript
// 频道-标签映射
const channelToTab = new Map<string, string>()
// 标签页结构
interface SessionTab { id: string; label: string; cwd?: string }
// handleIframeMessage — 处理 iframe postMessage
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: webview 内注入分支按钮和内联分支指示器</name>
  <files>
    src/main/claude/webview-server.ts
  </files>
  <read_first>
    src/main/claude/webview-server.ts
    src/main/ipc/claude-webview.ts
  </read_first>
  <action>
    **在 `src/main/claude/webview-server.ts` 的 `generateHostHtml()` 函数中：**

    在现有的 context usage badge `<script>` 块之后、`</body>` 标签之前，新增分支按钮注入 `<script>` 块：

    ```javascript
    // 分支按钮注入 — 在每条用户消息旁添加分支图标 (UX-12, D-07)
    (function() {
      // 注入分支按钮样式
      var style = document.createElement('style');
      style.textContent = `
        .aitools-branch-btn {
          position: absolute; right: 8px; top: 50%; transform: translateY(-50%);
          width: 24px; height: 24px; border-radius: 4px; border: none;
          background: transparent; cursor: pointer; opacity: 0;
          transition: opacity 0.15s, background 0.15s;
          display: flex; align-items: center; justify-content: center;
          color: ${colors.fg}; font-size: 14px; z-index: 5;
        }
        .aitools-branch-btn:hover {
          background: ${colors.list.hover}; opacity: 1 !important;
        }
        /* 父元素 hover 时显示分支按钮 */
        .aitools-user-msg:hover .aitools-branch-btn { opacity: 0.6; }

        /* 内联分支指示器 */
        .aitools-branch-indicator {
          display: inline-flex; align-items: center; gap: 4px;
          padding: 2px 8px; margin: 4px 0;
          background: ${colors.list.hover}; border-radius: 4px;
          font-size: 12px; color: ${colors.fg}; cursor: pointer;
          transition: background 0.15s;
        }
        .aitools-branch-indicator:hover {
          background: ${colors.list.activeBg};
        }
        .aitools-branch-indicator-count {
          font-weight: 600; color: ${colors.focus};
        }
      `;
      document.head.appendChild(style);

      // 消息索引计数器
      var messageIndex = 0;
      // 用户消息选择器 — 尝试多种选择器模式
      var USER_MSG_SELECTOR = '.chat-message.user, [data-is-user="true"], .message-turn-user';

      // MutationObserver 监听 DOM 变化，注入分支按钮
      var observer = new MutationObserver(function() {
        injectBranchButtons();
      });
      observer.observe(document.body, { childList: true, subtree: true });

      function injectBranchButtons() {
        // 查找所有用户消息容器
        // Claude Code webview 的用户消息通常包含特定属性或类名
        // 由于具体 DOM 结构需要实际检查，使用多种选择器尝试
        var userMessages = document.querySelectorAll('.chat-message, [data-message-author-role="user"], .message-row');

        userMessages.forEach(function(msg) {
          if (msg.classList.contains('aitools-branch-processed')) return;

          // 只在用户消息上注入
          var isUserMsg = msg.querySelector('[data-is-user="true"]') !== null ||
                          msg.classList.contains('user') ||
                          msg.getAttribute('data-message-author-role') === 'user' ||
                          msg.textContent && !msg.querySelector('.assistant');

          if (!isUserMsg) return;

          msg.classList.add('aitools-branch-processed');
          msg.style.position = 'relative';
          msg.classList.add('aitools-user-msg');

          var currentMsgIndex = messageIndex++;

          var btn = document.createElement('button');
          btn.className = 'aitools-branch-btn';
          // 使用 SVG 分支图标（Unicode: U+1F500 或自定义 SVG）
          btn.innerHTML = '<svg width="14" height="14" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 4v3a4 4 0 0 0 4 4h1"/><path d="M4 4v3a4 4 0 0 1 4 4h1"/><circle cx="4" cy="3" r="1.5" fill="currentColor"/><circle cx="9" cy="12" r="1.5" fill="currentColor"/></svg>';
          btn.title = '从此消息创建分支';
          btn.setAttribute('data-msg-index', String(currentMsgIndex));

          btn.onclick = function(e) {
            e.stopPropagation();
            e.preventDefault();
            // 通过 postMessage 通知父页面创建分支
            window.parent.postMessage({
              type: 'claude-webview-message',
              message: {
                type: 'branch:create',
                messageIndex: currentMsgIndex
              }
            }, '*');
          };

          msg.appendChild(btn);
        });
      }

      // 监听来自父页面的分支指示器数据
      window.addEventListener('message', function(e) {
        var data = e.data;
        if (!data || data.type !== 'from-extension') return;
        var msg = data.message;
        if (!msg || msg.type !== 'branch:indicators') return;

        // msg.branchPoints = [{ messageIndex: 5, count: 2, branches: [...] }, ...]
        updateBranchIndicators(msg.branchPoints || []);
      });

      function updateBranchIndicators(branchPoints) {
        // 移除旧的指示器
        document.querySelectorAll('.aitools-branch-indicator').forEach(function(el) {
          el.remove();
        });

        branchPoints.forEach(function(bp) {
          // 找到对应的用户消息
          var userMsgs = document.querySelectorAll('.aitools-branch-processed');
          var targetMsg = null;
          userMsgs.forEach(function(msg) {
            var btn = msg.querySelector('.aitools-branch-btn');
            if (btn && parseInt(btn.getAttribute('data-msg-index')) === bp.messageIndex) {
              targetMsg = msg;
            }
          });
          if (!targetMsg) return;

          // 在消息后插入分支指示器
          var indicator = document.createElement('div');
          indicator.className = 'aitools-branch-indicator';
          indicator.innerHTML = '<span class="aitools-branch-indicator-count">' + bp.count + '</span> 个分支';

          indicator.onclick = function() {
            window.parent.postMessage({
              type: 'claude-webview-message',
              message: {
                type: 'branch:switch',
                messageIndex: bp.messageIndex
              }
            }, '*');
          };

          // 插入到消息元素后面
          if (targetMsg.nextSibling) {
            targetMsg.parentNode.insertBefore(indicator, targetMsg.nextSibling);
          } else {
            targetMsg.parentNode.appendChild(indicator);
          }
        });
      }
    })();
    ```

    **注意：** 上述注入代码中的 DOM 选择器（`.chat-message`、`[data-message-author-role="user"]` 等）需要根据 Claude Code webview 的实际 DOM 结构调整。RESEARCH.md 的 Open Question #1 指出用户消息的具体 DOM 结构需要在实现阶段通过 DevTools 检查确定。实现时先用通用选择器，如果匹配不到则在 DevTools 中检查实际结构后更新选择器。

    分支按钮图标使用简单的 SVG，避免 emoji 在不同系统上渲染不一致的问题。
  </action>
  <verify>
    cd E:/work/projects/new-aitools && npx tsc --noEmit
  </verify>
  <done>
    - src/main/claude/webview-server.ts 的 generateHostHtml() 中包含分支按钮注入 script 块
    - MutationObserver 监听 DOM 变化并在用户消息上注入分支按钮
    - 分支按钮通过 postMessage 发送 branch:create 事件到父页面
    - 内联分支指示器（"N 个分支"标签）在分叉点消息后显示
    - 点击分支指示器通过 postMessage 发送 branch:switch 事件
    - TypeScript 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 2: ChatPanel 处理分支创建事件 + 标签页分支信息展示 + 右键菜单重命名</name>
  <files>
    src/renderer/src/components/ChatPanel.vue
  </files>
  <read_first>
    src/renderer/src/components/ChatPanel.vue
    src/preload/index.ts
    src/shared/types.ts
  </read_first>
  <action>
    **在 `src/renderer/src/components/ChatPanel.vue` 中：**

    a) 扩展 `SessionTab` 接口（第 10-14 行），新增分支信息字段：
    ```typescript
    interface SessionTab {
      id: string
      label: string
      cwd?: string
      /** 分支元数据（可选，分支标签页才有） */
      branchId?: string
      /** 分支来源标签（如 "分支 #1"） */
      branchLabel?: string
      /** 父会话 ID */
      parentSessionId?: string
    }
    ```

    b) 新增分支创建处理函数：
    ```typescript
    /** UX-12: 处理来自 webview 的分支创建请求 */
    async function handleBranchCreate(sourceTabId: string, messageIndex: number): Promise<void> {
      // 查找源标签对应的频道和会话
      let channelId: string | null = null
      for (const [chId, tabId] of channelToTab) {
        if (tabId === sourceTabId) { channelId = chId; break }
      }
      if (!channelId) {
        showStatus('无法定位当前频道', 'error')
        return
      }

      // 获取活跃会话 ID
      const sessionId = await window.api.getActiveSessionId(channelId)
      if (!sessionId) {
        showStatus('当前无活跃会话，无法创建分支', 'warning')
        return
      }

      // 检查分支配额
      const quota = await window.api.branchCanCreate(sessionId)
      if (!quota.canCreate) {
        showStatus(`已达最大分支数量（10），请关闭旧分支后重试`, 'warning', 3000)
        return
      }

      showStatus('正在创建分支...', 'info')

      // 调用分支创建 IPC
      const result = await window.api.branchCreate({
        parentSessionId: sessionId,
        branchPointIndex: messageIndex,
        cwd: tabs.value.find(t => t.id === sourceTabId)?.cwd || ''
      })

      if (result.success && result.channelId && result.branch) {
        // 创建新标签页显示分支
        const branchTabId = generateTabId()
        const branch = result.branch as { id: string; label: string }
        tabs.value.push({
          id: branchTabId,
          label: branch.label,
          branchId: branch.id,
          branchLabel: branch.label,
          parentSessionId: sessionId,
          cwd: tabs.value.find(t => t.id === sourceTabId)?.cwd
        })
        tabOrder.set(branchTabId, tabs.value.length - 1)
        activeTabId.value = branchTabId

        // 注册频道到标签映射
        channelToTab.set(result.channelId, branchTabId)
        // 通知主进程注册频道到当前窗口
        window.api.windowRegisterChannel(result.channelId)

        showStatus('分支已创建', 'success', 2000)
      } else {
        showStatus('创建分支失败: ' + (result.error || '未知错误'), 'error', 3000)
      }
    }
    ```

    c) 修改 `handleIframeMessage` 函数，处理来自 webview 的分支消息（在 `case 'launch_claude'` 处理之前添加）：
    ```typescript
    // UX-12: 处理 webview 分支按钮点击
    if (message?.type === 'branch:create' && sourceTabId) {
      handleBranchCreate(sourceTabId, (message as Record<string, unknown>).messageIndex as number || 0)
      return  // 不转发到主进程
    }

    // UX-12: 处理 webview 分支切换点击 — 显示分支列表选择器并切换到目标分支
    if (message?.type === 'branch:switch' && sourceTabId) {
      const msgIndex = (message as Record<string, unknown>).messageIndex as number || 0
      handleBranchSwitch(sourceTabId, msgIndex)
      return
    }
    ```

    d) 修改标签右键菜单（第 45-48 行），添加分支重命名选项：
    ```typescript
    const contextMenuItems: MenuItem[] = [
      { label: '导出对话', action: 'export', icon: '\u{1F4C4}' },
      { label: '重命名分支', action: 'rename-branch', icon: '\u{270F}' }
    ]
    ```

    e) 修改 `handleContextMenuAction` 函数，处理重命名分支：
    ```typescript
    function handleContextMenuAction(action: string): void {
      if (action === 'export' && contextMenuTabId.value) {
        exportCurrentChat(contextMenuTabId.value).catch(...)
      } else if (action === 'rename-branch' && contextMenuTabId.value) {
        renameBranchTab(contextMenuTabId.value)
      }
      contextMenuVisible.value = false
    }

    /** UX-12: 重命名分支标签页 (D-11) */
    async function renameBranchTab(tabId: string): Promise<void> {

    /** UX-12: 处理分支切换 — 显示分支选择器并切换到目标分支标签页 (D-08) */
    async function handleBranchSwitch(sourceTabId: string, messageIndex: number): Promise<void> {
      // 查找源标签对应的频道和会话
      let channelId: string | null = null
      for (const [chId, tabId] of channelToTab) {
        if (tabId === sourceTabId) { channelId = chId; break }
      }
      if (!channelId) return

      const sessionId = await window.api.getActiveSessionId(channelId)
      if (!sessionId) return

      // 查询该分支点的所有分支
      const branches = await window.api.branchListAtPoint({
        parentSessionId: sessionId,
        branchPointIndex: messageIndex
      })

      if (!branches || branches.length === 0) {
        showStatus('此消息点没有其他分支', 'info', 2000)
        return
      }

      // 找到每个分支对应的标签页
      const branchTabs = (branches as Array<{ channelId: string; label: string; id: string }>)
        .map(b => {
          const tabId = channelToTab.get(b.channelId)
          const tab = tabId ? tabs.value.find(t => t.id === tabId) : null
          return { branch: b, tabId, tab }
        })

      // 显示分支选择器弹出面板
      // 使用简单的 confirm 式选择（或下拉列表）
      const options = branchTabs.map(bt =>
        `${bt.tab ? '✓ ' : ''}${bt.branch.label}${bt.tab ? '' : ' (未打开)'}`
      )

      const choice = prompt(
        `选择要切换到的分支:\n${options.map((o, i) => `${i + 1}. ${o}`).join('\n')}\n\n输入编号（取消则留空）:`
      )

      if (!choice) return
      const idx = parseInt(choice) - 1
      if (idx < 0 || idx >= branchTabs.length) return

      const selected = branchTabs[idx]
      if (selected.tabId) {
        // 分支已打开 — 切换到该标签页
        activeTabId.value = selected.tabId
      } else {
        showStatus('该分支未在当前窗口打开', 'info', 2000)
      }
    }

    /** UX-12: 重命名分支标签页 (D-11) */
    async function renameBranchTab(tabId: string): Promise<void> {
      const tab = tabs.value.find(t => t.id === tabId)
      if (!tab?.branchId) {
        showStatus('此标签不是分支，无法重命名', 'warning', 2000)
        return
      }

      // 使用内联编辑方式重命名
      const newLabel = prompt('输入新分支名称:', tab.branchLabel || tab.label)
      if (!newLabel || newLabel === tab.branchLabel) return

      const success = await window.api.branchRename(tab.branchId, newLabel)
      if (success) {
        tab.label = newLabel
        tab.branchLabel = newLabel
        showStatus('分支已重命名', 'success', 2000)
      } else {
        showStatus('重命名失败', 'error')
      }
    }
    ```

    f) 修改标签页模板中的标签文本显示，标注分支来源：
    在模板中 `<span class="tab-label">` 部分修改为：
    ```html
    <span class="tab-text">
      <span class="tab-label">{{ tab.label }}</span>
      <span v-if="tab.branchLabel" class="tab-branch-tag" :title="'分支来源: ' + tab.parentSessionId">
        {{ tab.branchLabel }}
      </span>
      <span v-else-if="tab.cwd" class="tab-cwd" :title="tab.cwd">{{ tab.cwd.split(/[\\/]/).pop() }}</span>
    </span>
    ```

    g) 新增分支标签样式：
    ```css
    .tab-branch-tag {
      font-size: 10px;
      color: var(--accent);
      background: var(--bg-tertiary);
      padding: 0 4px;
      border-radius: 2px;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      line-height: 1.2;
    }
    ```
  </action>
  <verify>
    cd E:/work/projects/new-aitools && npx tsc --noEmit
  </verify>
  <done>
    - ChatPanel.vue 的 SessionTab 接口包含 branchId/branchLabel/parentSessionId 字段
    - handleBranchCreate 函数处理 webview 发来的 branch:create 事件
    - 分支创建后自动新建标签页，注册频道映射，标签名显示分支标签
    - 标签右键菜单包含 "重命名分支" 选项
    - renameBranchTab 函数调用 branch:rename IPC 更新分支标签
    - 标签页模板显示分支来源标签（.tab-branch-tag）
    - handleIframeMessage 中拦截 branch:create 和 branch:switch 消息
    - TypeScript 编译通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Webview (iframe) → Parent (postMessage) | branch:create 的 messageIndex 来自 iframe 内部 DOM |
| Renderer → Main (branch IPC) | parentSessionId 和 branchPointIndex 来自渲染进程 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | I (Info Disclosure) | 分支按钮 DOM 注入 | accept | 按钮仅显示在当前用户的 webview 中 |
| T-08-10 | T (Tampering) | messageIndex 篡改 | mitigate | BranchManager 校验 branchPointIndex 范围 |
</threat_model>

<verification>
1. TypeScript 编译通过: `npx tsc --noEmit`
2. webview 内用户消息旁出现分支按钮（需实际运行验证 DOM 选择器）
3. 点击分支按钮触发 branch:create postMessage
4. ChatPanel 接收后调用 branch:create IPC 创建新分支
5. 新分支显示为新标签页，标签名包含分支标签
6. 右键菜单可重命名分支
7. 内联分支指示器在分叉点显示 "N 个分支"
</verification>

<success_criteria>
- webview 中每条用户消息右侧有分支按钮（悬停时显示）
- 点击分支按钮创建新分支（新 CLI 会话 + 新标签页）
- 分支标签页标签名显示 "分支 #N"
- 标签右键菜单可重命名分支
- 内联分支指示器在分叉点显示分支数量
- TypeScript 编译 0 错误
</success_criteria>

<output>
After completion, create `.planning/phases/08-advanced-features/08-04-SUMMARY.md`
</output>

---
phase: 08-advanced-features
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01-PLAN
files_modified:
  - src/main/claude/branch-store.ts
  - src/main/claude/branch-manager.ts
  - src/main/ipc/claude-webview.ts
  - src/preload/index.ts
  - src/shared/types.ts
autonomous: true
requirements:
  - UX-12

must_haves:
  truths:
    - "从任意会话的任意消息索引可创建新分支"
    - "新分支创建新的 CLI 会话，分支点上下文作为历史注入"
    - "分支元数据持久化到 JSON 文件，重启后可恢复"
    - "每个会话限制最多 10 个分支，超出后提示用户"
    - "分支自动编号（分支 #1、分支 #2），可通过 IPC 重命名"
  artifacts:
    - path: "src/main/claude/branch-store.ts"
      provides: "分支元数据持久化（JSON 文件读写）"
      exports: ["BranchStore", "BranchMeta", "BranchesData"]
    - path: "src/main/claude/branch-manager.ts"
      provides: "分支创建、查询、重命名、上下文注入"
      exports: ["BranchManager"]
  key_links:
    - from: "src/main/claude/branch-manager.ts"
      to: "src/main/claude/session-store.ts"
      via: "getSessionMessages 读取历史消息"
      pattern: "getSessionMessages"
    - from: "src/main/claude/branch-manager.ts"
      to: "src/main/ipc/claude-webview.ts"
      via: "handleLaunchClaude 创建新 CLI 进程"
      pattern: "handleLaunchClaude"
    - from: "src/main/claude/branch-store.ts"
      to: "userData/branches.json"
      via: "JSON 文件持久化"
      pattern: "branches\\.json"
---

<objective>
创建分支数据模型、持久化存储和分支管理器，实现从任意历史消息创建新分支对话的核心逻辑。

Purpose: 为分支 UI（按钮注入、内联指示器、标签页分支标签）提供后端数据层和 IPC 接口。分支创建、查询、重命名、限制检查等核心逻辑在此完成。

Output: BranchStore 类（JSON 持久化）、BranchManager 类（分支创建/查询/重命名）、新增 IPC 处理器
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-advanced-features/08-CONTEXT.md
@.planning/phases/08-advanced-features/08-RESEARCH.md

<interfaces>
<!-- 分支系统需要交互的现有代码接口 -->

从 src/main/claude/session-store.ts:
```typescript
// 读取会话历史消息 — 分支上下文注入的数据源
export async function getSessionMessages(sessionId: string, cwd: string): Promise<unknown[]>
// 消息对象结构: { type: 'user' | 'assistant' | 'system', message: { content: [...] }, ... }
```

从 src/main/ipc/claude-webview.ts:
```typescript
// 启动新的 CLI 进程 — 分支创建时调用
async function handleLaunchClaude(
  channelId: string,
  cwd: string,
  _permissionMode: string,
  _thinkingLevel: string,
  resumeSessionId?: string,
  persistent?: boolean
): Promise<void>

// 发送到 webview — 分支消息重放时调用
function sendToWebview(msg: unknown, targetChannelId?: string): void

// 当前活跃频道
const channels = new Map<string, Channel>()
```

从 src/shared/types.ts:
```typescript
// 已有的类型定义
export interface SessionInfo { id: string; lastModified: number; ... }
```

从 src/preload/index.ts:
```typescript
// preload API — 需要新增分支相关方法
const api = { ... }
```

从 RESEARCH.md 的分支元数据格式:
```json
{
  "branches": {
    "branch_001": {
      "id": "branch_001",
      "parentSessionId": "550e8400-...",
      "branchPointIndex": 5,
      "channelId": "ch_1745856000000_abc123",
      "label": "分支 #1",
      "createdAt": 1745856000000
    }
  },
  "sessionBranchCount": {
    "550e8400-...": 1
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 BranchStore 分支元数据持久化</name>
  <files>
    src/main/claude/branch-store.ts
    src/shared/types.ts
  </files>
  <read_first>
    src/main/claude/session-store.ts
    src/shared/types.ts
    src/main/claude/token-usage-store.ts
  </read_first>
  <action>
    **1. 在 `src/shared/types.ts` 中新增分支相关类型定义：**

    ```typescript
    /** 分支元数据 (UX-12) */
    export interface BranchMeta {
      /** 分支唯一 ID */
      id: string
      /** 父会话 ID (JSONL 文件名) */
      parentSessionId: string
      /** 分支点消息索引（在父会话 JSONL 中的行号） */
      branchPointIndex: number
      /** 分支对应的频道 ID */
      channelId: string
      /** 分支显示标签 */
      label: string
      /** 创建时间戳 */
      createdAt: number
      /** 分支来源标签页 ID（可选） */
      parentTabId?: string
    }

    /** 分支存储数据结构 */
    export interface BranchesData {
      /** 分支 ID → 分支元数据 */
      branches: Record<string, BranchMeta>
      /** 父会话 ID → 分支数量 */
      sessionBranchCount: Record<string, number>
    }
    ```

    **2. 创建 `src/main/claude/branch-store.ts`：**

    参考 `token-usage-store.ts` 的 Promise chain 写入模式，创建分支元数据持久化模块：

    ```typescript
    import { readFileSync, writeFileSync, existsSync } from 'fs'
    import { join } from 'path'
    import { app } from 'electron'
    import type { BranchMeta, BranchesData } from '../../shared/types'
    import { safeLog, safeError } from './logger'

    const BRANCHES_FILE = join(app.getPath('userData'), 'branches.json')
    const MAX_BRANCHES_PER_SESSION = 10 // D-12: 每个会话最多 10 个分支

    let data: BranchesData = { branches: {}, sessionBranchCount: {} }

    // Promise chain 写入队列（与 token-usage-store 模式一致）
    let writeChain: Promise<void> = Promise.resolve()

    /** 加载分支数据 */
    export function loadBranchData(): BranchesData {
      try {
        if (existsSync(BRANCHES_FILE)) {
          const raw = readFileSync(BRANCHES_FILE, 'utf-8')
          data = JSON.parse(raw)
          // 兼容性：确保字段存在
          if (!data.branches) data.branches = {}
          if (!data.sessionBranchCount) data.sessionBranchCount = {}
        }
      } catch (err) {
        safeError('[BranchStore] 加载分支数据失败:', err)
        data = { branches: {}, sessionBranchCount: {} }
      }
      return data
    }

    /** 异步保存分支数据（Promise chain 队列写入） */
    function saveBranchData(): Promise<void> {
      writeChain = writeChain.then(() => {
        try {
          writeFileSync(BRANCHES_FILE, JSON.stringify(data, null, 2), 'utf-8')
        } catch (err) {
          safeError('[BranchStore] 保存分支数据失败:', err)
        }
      })
      return writeChain
    }

    /** 检查会话是否还能创建新分支 */
    export function canCreateBranch(parentSessionId: string): boolean {
      loadBranchData()
      const count = data.sessionBranchCount[parentSessionId] || 0
      return count < MAX_BRANCHES_PER_SESSION
    }

    /** 获取会话的剩余分支配额 */
    export function getRemainingQuota(parentSessionId: string): number {
      loadBranchData()
      const count = data.sessionBranchCount[parentSessionId] || 0
      return MAX_BRANCHES_PER_SESSION - count
    }

    /** 保存分支元数据 */
    export function saveBranch(branch: BranchMeta): void {
      loadBranchData()
      data.branches[branch.id] = branch
      data.sessionBranchCount[branch.parentSessionId] =
        (data.sessionBranchCount[branch.parentSessionId] || 0) + 1
      saveBranchData()
      safeLog('[BranchStore] 已保存分支:', branch.id, '标签:', branch.label)
    }

    /** 获取父会话的所有分支 */
    export function getBranchesByParent(parentSessionId: string): BranchMeta[] {
      loadBranchData()
      return Object.values(data.branches)
        .filter(b => b.parentSessionId === parentSessionId)
        .sort((a, b) => a.branchPointIndex - b.branchPointIndex || a.createdAt - b.createdAt)
    }

    /** 获取分支点相同的所有分支 */
    export function getBranchesAtPoint(parentSessionId: string, branchPointIndex: number): BranchMeta[] {
      loadBranchData()
      return Object.values(data.branches)
        .filter(b => b.parentSessionId === parentSessionId && b.branchPointIndex === branchPointIndex)
    }

    /** 获取所有分支中指定频道 ID 对应的分支 */
    export function getBranchByChannelId(channelId: string): BranchMeta | undefined {
      loadBranchData()
      return Object.values(data.branches).find(b => b.channelId === channelId)
    }

    /** 重命名分支 */
    export function renameBranch(branchId: string, newLabel: string): boolean {
      loadBranchData()
      const branch = data.branches[branchId]
      if (!branch) return false
      branch.label = newLabel
      saveBranchData()
      safeLog('[BranchStore] 已重命名分支:', branchId, '新标签:', newLabel)
      return true
    }

    /** 删除分支元数据 */
    export function deleteBranch(branchId: string): boolean {
      loadBranchData()
      const branch = data.branches[branchId]
      if (!branch) return false
      delete data.branches[branchId]
      const count = data.sessionBranchCount[branch.parentSessionId] || 0
      if (count > 0) {
        data.sessionBranchCount[branch.parentSessionId] = count - 1
      }
      saveBranchData()
      safeLog('[BranchStore] 已删除分支:', branchId)
      return true
    }

    /** 获取下一个分支编号 */
    export function getNextBranchNumber(parentSessionId: string): number {
      loadBranchData()
      const branches = getBranchesByParent(parentSessionId)
      return branches.length + 1
    }
    ```
  </action>
  <verify>
    cd E:/work/projects/new-aitools && npx tsc --noEmit
  </verify>
  <done>
    - src/shared/types.ts 包含 BranchMeta 和 BranchesData 接口定义
    - src/main/claude/branch-store.ts 存在，导出 loadBranchData/canCreateBranch/saveBranch/getBranchesByParent/getBranchesAtPoint/renameBranch/deleteBranch/getNextBranchNumber/getRemainingQuota
    - TypeScript 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 BranchManager + 注册分支 IPC 处理器</name>
  <files>
    src/main/claude/branch-manager.ts
    src/main/ipc/claude-webview.ts
    src/preload/index.ts
  </files>
  <read_first>
    src/main/claude/session-store.ts
    src/main/claude/branch-store.ts
    src/main/ipc/claude-webview.ts
    src/preload/index.ts
    src/shared/types.ts
  </read_first>
  <action>
    **1. 创建 `src/main/claude/branch-manager.ts`：**

    ```typescript
    import type { BranchMeta } from '../../shared/types'
    import {
      canCreateBranch, saveBranch, getBranchesByParent,
      getBranchesAtPoint, getBranchByChannelId,
      renameBranch as renameBranchInStore, getNextBranchNumber,
      getRemainingQuota, deleteBranch as deleteBranchInStore
    } from './branch-store'
    import { getSessionMessages } from './session-store'
    import { safeLog, safeError } from './logger'

    /** 分支创建结果 */
    export interface BranchCreateResult {
      success: boolean
      branch?: BranchMeta
      channelId?: string
      error?: string
    }

    /**
     * 创建对话分支 (D-07, D-10)
     *
     * 流程:
     * 1. 验证分支配额
     * 2. 读取父会话历史消息到分支点
     * 3. 创建新的 CLI 频道
     * 4. 将分支点上下文消息重放到 webview
     * 5. 保存分支元数据
     *
     * @param parentSessionId - 父会话 ID
     * @param branchPointIndex - 分支点消息索引
     * @param cwd - 工作目录
     * @param handleLaunchFn - 创建 CLI 进程的函数引用（避免循环依赖）
     * @param sendToWebviewFn - 发送消息到 webview 的函数引用
     */
    export async function createBranch(
      parentSessionId: string,
      branchPointIndex: number,
      cwd: string,
      handleLaunchFn: (channelId: string, cwd: string, permMode: string, thinkingLevel: string, resumeSessionId?: string, persistent?: boolean) => Promise<void>,
      sendToWebviewFn: (msg: unknown, targetChannelId?: string) => void
    ): Promise<BranchCreateResult> {
      // 1. 检查分支配额
      if (!canCreateBranch(parentSessionId)) {
        const remaining = getRemainingQuota(parentSessionId)
        return {
          success: false,
          error: `此会话已达到最大分支数量（10），请关闭旧分支后重试。剩余配额: ${remaining}`
        }
      }

      try {
        // 2. 读取父会话历史消息到分支点
        const allMessages = await getSessionMessages(parentSessionId, cwd)
        if (branchPointIndex >= allMessages.length) {
          return { success: false, error: '分支点索引超出消息范围' }
        }
        const branchMessages = allMessages.slice(0, branchPointIndex + 1)

        // 3. 生成新的频道 ID
        const channelId = `ch_${Date.now()}_${Math.random().toString(36).slice(2)}`

        // 4. 创建新的 CLI 进程
        await handleLaunchFn(channelId, cwd, 'default', '')

        // 5. 将分支点上下文消息重放到 webview
        for (const msg of branchMessages) {
          sendToWebviewFn({ type: 'io_message', channelId, message: msg }, channelId)
        }

        safeLog('[BranchManager] 已重放', branchMessages.length, '条消息到分支频道:', channelId)

        // 6. 保存分支元数据
        const branchNumber = getNextBranchNumber(parentSessionId)
        const branchId = `branch_${Date.now()}_${Math.random().toString(36).slice(2)}`
        const branch: BranchMeta = {
          id: branchId,
          parentSessionId,
          branchPointIndex,
          channelId,
          label: `分支 #${branchNumber}`,
          createdAt: Date.now()
        }
        saveBranch(branch)

        safeLog('[BranchManager] 已创建分支:', branchId, '标签:', branch.label, '频道:', channelId)

        return { success: true, branch, channelId }
      } catch (err) {
        safeError('[BranchManager] 创建分支失败:', err)
        return { success: false, error: String(err) }
      }
    }

    /** 查询父会话的所有分支 */
    export function listBranches(parentSessionId: string): BranchMeta[] {
      return getBranchesByParent(parentSessionId)
    }

    /** 查询分支点处的所有分支 */
    export function listBranchesAtPoint(parentSessionId: string, branchPointIndex: number): BranchMeta[] {
      return getBranchesAtPoint(parentSessionId, branchPointIndex)
    }

    /** 重命名分支 */
    export function renameBranch(branchId: string, newLabel: string): boolean {
      return renameBranchInStore(branchId, newLabel)
    }

    /** 删除分支 */
    export function removeBranch(branchId: string): boolean {
      return deleteBranchInStore(branchId)
    }

    /** 通过频道 ID 查找分支 */
    export function findBranchByChannel(channelId: string): BranchMeta | undefined {
      return getBranchByChannelId(channelId)
    }
    ```

    **2. 在 `src/main/ipc/claude-webview.ts` 中注册分支相关 IPC 处理器：**

    在 `registerClaudeWebviewHandlers()` 函数末尾（第 815 行 `}` 之前）新增：

    ```typescript
    // UX-12: 分支管理 IPC
    ipcMain.handle('branch:create', async (_event, data: {
      parentSessionId: string
      branchPointIndex: number
      cwd: string
    }) => {
      const { createBranch } = await import('../claude/branch-manager')
      return await createBranch(
        data.parentSessionId,
        data.branchPointIndex,
        data.cwd,
        handleLaunchClaude,
        sendToWebview
      )
    })

    ipcMain.handle('branch:list', async (_event, parentSessionId: string) => {
      const { listBranches } = await import('../claude/branch-manager')
      return listBranches(parentSessionId)
    })

    ipcMain.handle('branch:list-at-point', async (_event, data: {
      parentSessionId: string
      branchPointIndex: number
    }) => {
      const { listBranchesAtPoint } = await import('../claude/branch-manager')
      return listBranchesAtPoint(data.parentSessionId, data.branchPointIndex)
    })

    ipcMain.handle('branch:rename', async (_event, branchId: string, newLabel: string) => {
      const { renameBranch } = await import('../claude/branch-manager')
      return renameBranch(branchId, newLabel)
    })

    ipcMain.handle('branch:delete', async (_event, branchId: string) => {
      const { removeBranch } = await import('../claude/branch-manager')
      return removeBranch(branchId)
    })

    ipcMain.handle('branch:find-by-channel', async (_event, channelId: string) => {
      const { findBranchByChannel } = await import('../claude/branch-manager')
      return findBranchByChannel(channelId) || null
    })

    ipcMain.handle('branch:can-create', async (_event, parentSessionId: string) => {
      const { canCreateBranch, getRemainingQuota } = await import('../claude/branch-store')
      return { canCreate: canCreateBranch(parentSessionId), remaining: getRemainingQuota(parentSessionId) }
    })
    ```

    **3. 在 `src/preload/index.ts` 的 `api` 对象中新增分支 IPC 桥接：**

    ```typescript
    // 分支管理 IPC (UX-12)
    branchCreate: (data: {
      parentSessionId: string
      branchPointIndex: number
      cwd: string
    }): Promise<{ success: boolean; branch?: unknown; channelId?: string; error?: string }> =>
      ipcRenderer.invoke('branch:create', data),
    branchList: (parentSessionId: string): Promise<unknown[]> =>
      ipcRenderer.invoke('branch:list', parentSessionId),
    branchListAtPoint: (data: {
      parentSessionId: string
      branchPointIndex: number
    }): Promise<unknown[]> =>
      ipcRenderer.invoke('branch:list-at-point', data),
    branchRename: (branchId: string, newLabel: string): Promise<boolean> =>
      ipcRenderer.invoke('branch:rename', branchId, newLabel),
    branchDelete: (branchId: string): Promise<boolean> =>
      ipcRenderer.invoke('branch:delete', branchId),
    branchFindByChannel: (channelId: string): Promise<unknown | null> =>
      ipcRenderer.invoke('branch:find-by-channel', channelId),
    branchCanCreate: (parentSessionId: string): Promise<{ canCreate: boolean; remaining: number }> =>
      ipcRenderer.invoke('branch:can-create', parentSessionId),
    ```
  </action>
  <verify>
    cd E:/work/projects/new-aitools && npx tsc --noEmit
  </verify>
  <done>
    - src/main/claude/branch-manager.ts 存在，导出 createBranch/listBranches/listBranchesAtPoint/renameBranch/removeBranch/findBranchByChannel
    - src/main/ipc/claude-webview.ts 的 registerClaudeWebviewHandlers 中包含 branch:create/branch:list/branch:list-at-point/branch:rename/branch:delete/branch:find-by-channel/branch:can-create 处理器
    - src/preload/index.ts 包含 branchCreate/branchList/branchListAtPoint/branchRename/branchDelete/branchFindByChannel/branchCanCreate 方法
    - createBranch 函数接受 handleLaunchFn 和 sendToWebviewFn 参数（避免循环依赖）
    - 分支配额检查使用 MAX_BRANCHES_PER_SESSION = 10
    - TypeScript 编译通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → Main (branch:create IPC) | branchPointIndex 和 parentSessionId 来自渲染进程，需校验 |
| File System (branches.json) | 分支元数据文件，需防止路径注入和损坏 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | T (Tampering) | branch:create IPC | mitigate | 校验 parentSessionId 为 UUID 格式，branchPointIndex 为非负整数 |
| T-08-05 | D (DoS) | 无限制创建分支 | mitigate | MAX_BRANCHES_PER_SESSION = 10 硬限制 |
| T-08-06 | I (Info Disclosure) | branches.json | accept | 文件存储在 userData 目录，仅当前用户可访问 |
</threat_model>

<verification>
1. TypeScript 编译通过: `npx tsc --noEmit`
2. BranchStore 读写 branches.json 正确
3. BranchManager.createBranch 读取历史消息、创建 CLI 进程、重放消息
4. 分支配额检查生效（超出 10 个分支返回错误）
5. 所有分支 IPC 端点已注册
6. preload API 包含所有分支方法
</verification>

<success_criteria>
- BranchMeta/BranchesData 类型定义在 shared/types.ts 中
- BranchStore 提供完整的 CRUD 操作，数据持久化到 userData/branches.json
- BranchManager.createBranch 读取父会话历史、创建新 CLI 进程、重放消息、保存元数据
- 分支限制每个会话最多 10 个
- 7 个分支相关 IPC 端点已注册（create/list/list-at-point/rename/delete/find-by-channel/can-create）
- preload API 桥接所有分支 IPC
- TypeScript 编译 0 错误
</success_criteria>

<output>
After completion, create `.planning/phases/08-advanced-features/08-02-SUMMARY.md`
</output>

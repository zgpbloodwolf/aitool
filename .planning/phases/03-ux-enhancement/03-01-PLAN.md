---
phase: 03-ux-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/ConfirmDialog.vue
  - src/renderer/src/components/ChatPanel.vue
  - src/renderer/src/App.vue
  - src/renderer/src/stores/extension.ts
  - src/renderer/src/components/Sidebar.vue
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-06, D-07, D-08, D-09]

must_haves:
  truths:
    - "Webview 初始化失败时用户可以看到错误信息和重试按钮"
    - "删除会话前弹出确认对话框，用户可以取消"
    - "关闭有活跃 Claude 进程的标签前弹出确认对话框"
    - "切换工作区时若有活跃进程弹出警告"
    - "扩展加载失败和会话列表加载失败时显示错误提示"
    - "确认对话框使用 Catppuccin Mocha 主题样式"
  artifacts:
    - path: "src/renderer/src/components/ConfirmDialog.vue"
      provides: "通用确认对话框组件"
      exports: ["ConfirmDialog"]
    - path: "src/renderer/src/components/ChatPanel.vue"
      provides: "错误恢复 + 确认对话框集成 + defineExpose"
      contains: "ConfirmDialog"
    - path: "src/renderer/src/App.vue"
      provides: "扩展加载错误处理 + 工作区切换确认"
      contains: "ConfirmDialog"
    - path: "src/renderer/src/stores/extension.ts"
      provides: "扩展加载错误状态"
      contains: "error"
    - path: "src/renderer/src/components/Sidebar.vue"
      provides: "工作区切换确认按钮触发入口"
      contains: "handleOpenFolder"
  key_links:
    - from: "ChatPanel.vue"
      to: "ConfirmDialog.vue"
      via: "import ConfirmDialog"
      pattern: "import.*ConfirmDialog"
    - from: "App.vue"
      to: "ConfirmDialog.vue"
      via: "import ConfirmDialog"
      pattern: "import.*ConfirmDialog"
    - from: "ChatPanel.vue"
      to: "extension.ts"
      via: "useExtensionStore"
      pattern: "useExtensionStore"
    - from: "Sidebar.vue"
      to: "App.vue"
      via: "emit 或 ref 调用工作区切换确认"
      pattern: "handleOpenFolder"
---

<objective>
创建通用确认对话框组件并集成到错误恢复和操作确认流程中。

Purpose: 为所有需要用户确认的操作（删除会话、关闭活跃标签、切换工作区）提供统一的确认 UI，同时为 Webview 初始化失败和各类加载错误提供恢复路径。
Output: ConfirmDialog.vue 组件，ChatPanel.vue 和 App.vue 的错误恢复与确认集成。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ux-enhancement/03-CONTEXT.md
@.planning/phases/03-ux-enhancement/03-RESEARCH.md

<interfaces>
<!-- ConfirmDialog 需要 Catppuccin Mocha CSS 变量 -->
<!-- 来自 src/renderer/src/assets/main.css -->
:root {
  --bg-primary: #1e1e2e;
  --bg-secondary: #181825;
  --bg-tertiary: #313244;
  --text-primary: #cdd6f4;
  --text-secondary: #a6adc8;
  --text-muted: #6c7086;
  --accent: #89b4fa;
  --accent-hover: #74c7ec;
  --border: #45475a;
  --success: #a6e3a1;
  --error: #f38ba8;
  --warning: #fab387;
}

<!-- 来自 ChatPanel.vue 关键接口 -->
interface SessionTab {
  id: string
  label: string
}

<!-- channelToTab: Map<string, string> — channelId 到 tabId 的映射 -->
<!-- pendingResume: Map<string, string> — tabId 到 sessionId 的映射 -->
<!-- showStatus(msg, type, duration) — 已有状态消息系统 -->
<!-- initWebview() — webview 初始化函数 -->
<!-- deleteSessionById(sessionId) — 会话删除函数 -->
<!-- closeTab(id) — 标签关闭函数 -->

<!-- 来自 extension.ts store -->
interface ExtensionStore {
  extensions: Ref<ExtensionInfo[]>
  activeExtensionId: Ref<string | null>
  loading: Ref<boolean>
  loadExtensions(): Promise<void>
  activateExtension(id: string): Promise<void>
}

<!-- 来自 workspace.ts store -->
interface WorkspaceStore {
  rootPath: Ref<string | null>
  openFolder(): Promise<void>
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 ConfirmDialog 组件 + ChatPanel 错误恢复与确认集成</name>
  <files>src/renderer/src/components/ConfirmDialog.vue, src/renderer/src/components/ChatPanel.vue</files>
  <read_first>
    src/renderer/src/components/ChatPanel.vue
    src/renderer/src/assets/main.css
    .planning/phases/03-ux-enhancement/03-RESEARCH.md (Pattern 1: ConfirmDialog 组件)
  </read_first>
  <behavior>
    - Test 1: ConfirmDialog 在 visible=true 时渲染显示，visible=false 时不渲染
    - Test 2: 点击确认按钮触发 confirm emit
    - Test 3: 点击取消按钮或遮罩层触发 cancel emit
    - Test 4: type='danger' 时确认按钮使用 error 颜色 class
  </behavior>
  <action>
    **前置：确保 vitest 已安装和配置。** 如果项目尚未安装 vitest，执行：
    ```bash
    pnpm add -D vitest @vue/test-utils happy-dom
    ```
    在 `package.json` 的 scripts 中添加 `"test": "vitest run"`。
    创建 `vitest.config.ts`（或追加到 `electron.vite.config.ts`），配置 `environment: 'happy-dom'`。

    **1. 创建 `src/renderer/src/components/ConfirmDialog.vue`** (per D-09)

    创建一个通用确认对话框 Vue SFC 组件，使用 `<script setup lang="ts">`：
    - Props: `visible: boolean`, `title: string`, `message: string`, `confirmText?: string`（默认 `'确定'`）, `cancelText?: string`（默认 `'取消'`）, `type?: 'danger' | 'warning' | 'info'`（默认 `'info'`）
    - Emits: `confirm`, `cancel`
    - 模板：fixed 定位覆盖层 + 居中对话框盒子，使用 CSS 变量（`var(--bg-secondary)`, `var(--text-primary)`, `var(--accent)`, `var(--error)`, `var(--warning)` 等）
    - 确认按钮根据 type 使用不同背景色：danger 用 `var(--error)`, warning 用 `var(--warning)`, info 用 `var(--accent)`
    - 点击遮罩层触发 cancel
    - Vue `<Transition name="dialog-fade">` 包裹，淡入淡出 0.15s
    - 样式参考 RESEARCH.md Pattern 1 中的完整示例代码

    **2. 修改 `src/renderer/src/components/ChatPanel.vue`**

    a) **D-01: Webview 初始化失败重试按钮**
       - 在 error 显示区域（模板约第 347-349 行的 `v-else-if="error"` 块）中，在错误文本 `<p class="error">` 下方添加重试按钮：
         ```html
         <button class="retry-btn" @click="resetAndRetry">重试</button>
         ```
       - 添加 `resetAndRetry()` 方法：设置 `webviewReady = false`，然后调用 `initWebview()`

    b) **D-02, D-06: 删除会话确认弹窗**
       - import ConfirmDialog 组件
       - 添加响应式变量：`confirmVisible = ref(false)`, `confirmTitle = ref('')`, `confirmMessage = ref('')`, `confirmType = ref<'danger'|'warning'|'info'>('danger')`, `pendingAction = ref<(() => void) | null>(null)`
       - 创建 `showConfirm(title, message, type, onConfirm)` 通用方法
       - 修改 `deleteSessionById()`：在 `try` 块最前面调用 `showConfirm('删除会话', '确定删除此会话？此操作不可撤销。', 'danger', ...)` 包裹原有删除逻辑
       - 在模板底部（status-overlay 之后）添加 `<ConfirmDialog>` 组件实例

    c) **D-04: 会话列表加载失败提示**
       - 修改 `toggleSessionHistory()` 的 catch 分支：不静默设置空数组，而是设置 `sessionError = ref<string | null>(null)` 显示错误信息
       - 在模板的 `session-loading` 之后、`session-empty` 之前添加错误状态显示

    d) **D-07: 关闭活跃标签确认**
       - 修改 `closeTab(id)` 方法：在关闭前检查 `channelToTab` 中是否有该 tabId 的映射（即有活跃 channel）
       - 如果有活跃 channel，调用 `showConfirm('关闭对话', '此对话正在运行，关闭将终止进程。', 'warning', ...)` 包裹原有关闭逻辑

    e) **defineExpose 暴露方法**（为 Plan 02 键盘快捷键和 Plan 04 标签增强准备）：
       - 在 `<script setup>` 末尾添加 `defineExpose({ addNewTab, closeTab, switchTab, switchToNextTab, switchToPrevTab, hasActiveChannels })`
       - 实现 `hasActiveChannels(): boolean`：返回 `channelToTab.size > 0`
       - 实现 `switchToNextTab()`: 切换到当前 tab 的下一个（循环）
       - 实现 `switchToPrevTab()`: 切换到当前 tab 的上一个（循环）

    f) **添加 `.retry-btn` 样式**：使用 `var(--accent)` 背景，hover 用 `var(--accent-hover)`

    g) **添加会话错误显示样式**

    **3. 编写 ConfirmDialog 组件测试**

    创建 `src/renderer/src/components/__tests__/ConfirmDialog.test.ts`：
    - 使用 `@vue/test-utils` 的 `mount` 和 `shallowMount`
    - 测试 visible=true 时渲染内容包含 `.confirm-dialog` 容器
    - 测试 visible=false 时组件不渲染（v-if）
    - 测试点击确认按钮 emit `confirm` 事件
    - 测试点击取消按钮 emit `cancel` 事件
    - 测试点击遮罩层 emit `cancel` 事件
    - 测试 type='danger' 时确认按钮有 `.btn-danger` class
  </action>
  <verify>
    <automated>cd "E:/work/projects/new-aitools" && npx tsc --noEmit 2>&1 | head -20 && npx vitest run src/renderer/src/components/__tests__/ConfirmDialog.test.ts 2>&1 | tail -20 && npx eslint src/renderer/src/components/ConfirmDialog.vue src/renderer/src/components/ChatPanel.vue 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/renderer/src/components/ConfirmDialog.vue` 文件存在，包含 `defineProps` 和 `visible`, `title`, `message` props
    - `ChatPanel.vue` 包含 `import.*ConfirmDialog`
    - `ChatPanel.vue` 包含 `resetAndRetry` 或 `重试` 按钮处理函数
    - `ChatPanel.vue` 中 `deleteSessionById` 前有确认逻辑
    - `ChatPanel.vue` 包含 `defineExpose` 且暴露 `addNewTab`, `closeTab`, `hasActiveChannels`
    - `src/renderer/src/components/__tests__/ConfirmDialog.test.ts` 存在且测试通过
    - TypeScript 编译通过，0 error
    - ESLint 检查通过
  </acceptance_criteria>
  <done>
    ConfirmDialog 组件创建完成，Catppuccin Mocha 主题风格，通过单元测试验证显示/隐藏、确认/取消回调。ChatPanel 集成：初始化失败重试、删除会话确认、关闭活跃标签确认、会话列表错误提示、defineExpose 暴露方法。TypeScript 编译通过。ESLint 通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: App.vue 扩展加载错误处理 + 工作区切换确认 + Sidebar 集成</name>
  <files>src/renderer/src/App.vue, src/renderer/src/stores/extension.ts, src/renderer/src/components/Sidebar.vue</files>
  <read_first>
    src/renderer/src/App.vue
    src/renderer/src/stores/extension.ts
    src/renderer/src/components/Sidebar.vue
    src/renderer/src/components/ChatPanel.vue
  </read_first>
  <action>
    **1. 修改 `src/renderer/src/stores/extension.ts` (per D-03)**

    - 添加 `error = ref<string | null>(null)` 响应式变量
    - 修改 `loadExtensions()` 方法：在 try 块前清除 `error.value = null`，在 catch 中设置 `error.value = String(e)`（目前 catch 被 `finally` 隐式吞掉了，需要添加 catch 块）
    - 在 return 中导出 `error`

    **2. 修改 `src/renderer/src/App.vue`**

    a) **D-03: 扩展加载失败 UI 提示**
       - import ConfirmDialog（用于工作区切换确认，复用同一组件）
       - 在 `onMounted` 的 `extStore.loadExtensions()` 调用不做额外处理（store 已处理错误状态）
       - 在模板中，可以在 `.main-content` 区域添加条件渲染：当 `extStore.error` 存在时显示错误提示

    b) **D-08: 切换工作区确认**
       - import ChatPanel 组件获取 ref（`chatPanelRef`）
       - 添加 `workspaceStore = useWorkspaceStore()`
       - 创建 `handleOpenFolder()` 方法替代直接调用 `workspace.openFolder()`：
         - 检查 `chatPanelRef.value` 是否有活跃 channel（通过 defineExpose 暴露的 `hasActiveChannels(): boolean` 方法）
         - 如果有活跃 channel，弹出确认对话框（"当前有活跃的 Claude 对话，切换工作区将终止所有进程。"，type: 'warning'）
         - 用户确认后调用 `workspace.openFolder()`
       - 添加 ConfirmDialog 相关响应式变量和模板实例
       - 将 Sidebar 的 "打开文件夹" 按钮改为通过事件触发 App.vue 的 `handleOpenFolder()`

    c) **D-05 部分工作: 添加 chatPanelRef**
       - 在模板中给 ChatPanel 添加 ref：`<ChatPanel ref="chatPanelRef" class="chat-area" />`
       - 在 script 中声明：`const chatPanelRef = ref<InstanceType<typeof ChatPanel> | null>(null)`

    **3. 修改 `src/renderer/src/components/Sidebar.vue`**

    - 将 "打开文件夹" 按钮的点击事件改为 emit 一个自定义事件（如 `@click="$emit('open-folder')"`）而非直接调用 workspace store 的 `openFolder()`
    - 这使得 App.vue 可以拦截该事件并执行工作区切换确认逻辑 (per D-08)
    - 添加 `defineEmits(['open-folder'])` 声明
  </action>
  <verify>
    <automated>cd "E:/work/projects/new-aitools" && npx tsc --noEmit 2>&1 | head -20 && npx eslint src/renderer/src/App.vue src/renderer/src/stores/extension.ts src/renderer/src/components/Sidebar.vue 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `extension.ts` 包含 `error` ref 并在 catch 中赋值
    - `App.vue` 包含 `import.*ConfirmDialog`
    - `App.vue` 包含 `chatPanelRef` ref 声明
    - `App.vue` 包含 `handleOpenFolder` 或工作区切换确认逻辑
    - `Sidebar.vue` 包含 `emit.*open-folder` 或等效事件触发
    - `ChatPanel.vue` 的 `defineExpose` 包含 `hasActiveChannels`
    - TypeScript 编译通过，0 error
    - ESLint 检查通过
  </acceptance_criteria>
  <done>
    扩展加载失败时显示错误提示。切换工作区时若有活跃 Claude 进程弹出确认对话框。Sidebar 的打开文件夹按钮通过事件触发 App.vue 的确认流程。App.vue 持有 ChatPanel ref 为后续键盘快捷键做准备。TypeScript 编译通过。ESLint 通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户->确认对话框 | 用户点击确认/取消，纯 UI 操作，无安全风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | S (Spoofing) | ConfirmDialog UI | accept | 确认对话框是纯 UI 组件，不处理敏感数据，低风险 |
| T-03-02 | D (Denial of Service) | 工作区切换中断进程 | mitigate | 通过 ConfirmDialog 让用户确认操作，防止误操作导致进程中断 |
</threat_model>

<verification>
1. TypeScript 编译通过: `npx tsc --noEmit`
2. ESLint 检查通过: `npx eslint src/renderer/src/components/ConfirmDialog.vue src/renderer/src/components/ChatPanel.vue src/renderer/src/App.vue src/renderer/src/stores/extension.ts src/renderer/src/components/Sidebar.vue`
3. ConfirmDialog 单元测试通过: `npx vitest run src/renderer/src/components/__tests__/ConfirmDialog.test.ts`
4. ConfirmDialog.vue 存在且包含 required props
5. ChatPanel.vue 中错误恢复逻辑（重试按钮）存在
6. ChatPanel.vue 中 deleteSessionById 有确认前置逻辑
7. ChatPanel.vue 中 closeTab 有活跃检查
8. App.vue 中 handleOpenFolder 有活跃 channel 检查
9. Sidebar.vue 通过事件触发工作区打开
</verification>

<success_criteria>
- ConfirmDialog.vue 组件创建，使用 Catppuccin Mocha CSS 变量 (D-09)
- ConfirmDialog 单元测试通过（显示/隐藏、确认/取消回调）
- Webview 初始化失败显示重试按钮 (D-01)
- 删除会话有确认弹窗 (D-02, D-06)
- 关闭活跃标签有确认弹窗 (D-07)
- 切换工作区有确认弹窗 (D-08)
- 扩展加载失败有 UI 提示 (D-03)
- 会话列表加载失败有提示 (D-04)
- TypeScript 0 error
- ESLint 0 error
</success_criteria>

<output>
After completion, create `.planning/phases/03-ux-enhancement/03-01-SUMMARY.md`
</output>

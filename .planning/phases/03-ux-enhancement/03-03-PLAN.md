---
phase: 03-ux-enhancement
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/main/claude/process-manager.ts
  - src/main/ipc/claude-webview.ts
  - src/main/index.ts
  - src/renderer/src/components/ChatPanel.vue
  - src/preload/index.ts
  - src/preload/index.d.ts
autonomous: true
requirements: [D-10, D-11, D-12, D-24]

must_haves:
  truths:
    - "claude.exe 崩溃后自动重连并恢复会话状态"
    - "每 30 秒心跳检测 claude.exe 是否存活"
    - "进程挂死时用户收到通知并可以重启"
    - "channel 关闭时清理所有 pending 的超时定时器"
    - "主进程全局未捕获异常被记录而非静默崩溃"
    - "渲染进程收到崩溃通知后显示恢复状态并自动恢复"
    - "渲染进程收到无响应通知后显示重启按钮"
  artifacts:
    - path: "src/main/claude/process-manager.ts"
      provides: "心跳检测 + 崩溃恢复支持"
      contains: "startHealthCheck"
    - path: "src/main/ipc/claude-webview.ts"
      provides: "崩溃恢复 IPC 通知 + 超时清理"
      contains: "claude:process-crashed"
    - path: "src/main/index.ts"
      provides: "全局未捕获异常处理器"
      contains: "uncaughtException"
    - path: "src/renderer/src/components/ChatPanel.vue"
      provides: "渲染进程崩溃恢复 UI + 自动重连"
      contains: "process_crashed"
    - path: "src/preload/index.ts"
      provides: "进程恢复和崩溃事件 preload 桥接"
      contains: "claudeRecoverProcess"
    - path: "src/preload/index.d.ts"
      provides: "进程恢复和崩溃事件 TypeScript 类型声明"
      contains: "claudeRecoverProcess"
  key_links:
    - from: "claude-webview.ts"
      to: "process-manager.ts"
      via: "proc.on('exit') -> IPC 通知渲染进程"
      pattern: "sendToWebview.*crash|recovery"
    - from: "index.ts"
      to: "Node.js process"
      via: "process.on('uncaughtException')"
      pattern: "process\\.on.*uncaughtException"
    - from: "ChatPanel.vue"
      to: "preload/index.ts"
      via: "window.api.claudeRecoverProcess 和 onProcessCrashed/onProcessUnresponsive"
      pattern: "claudeRecoverProcess|onProcessCrashed"
---

<objective>
实现 Claude 进程健康监控、崩溃自动恢复、超时定时器清理、全局异常处理和渲染进程恢复 UI。

Purpose: 确保用户不会因为进程崩溃或挂死而丢失工作状态，同时防止内存泄漏和未处理异常导致应用崩溃。渲染进程能感知进程状态并自动恢复或提供手动恢复选项。本 Plan 在 Wave 2 执行（depends_on: 03-01），因为 Task 2 修改 ChatPanel.vue，需等待 Plan 01 完成确认对话框和 defineExpose 集成后再叠加崩溃恢复 UI。
Output: process-manager.ts 心跳检测，claude-webview.ts 崩溃恢复和超时清理，index.ts 全局异常处理，ChatPanel.vue 渲染进程恢复 UI，preload API 桥接。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ux-enhancement/03-CONTEXT.md
@.planning/phases/03-ux-enhancement/03-RESEARCH.md
@.planning/phases/03-ux-enhancement/03-01-SUMMARY.md

<interfaces>
<!-- 来自 process-manager.ts 关键接口 -->
export class ClaudeProcessManager extends EventEmitter {
  private process: ChildProcess | null = null
  private _running = false
  get running(): boolean
  start(options: ClaudeProcessOptions): void
  send(msg: unknown): void
  sendUserMessage(text: string): void
  stop(): void
  interrupt(): void
  // 事件: 'message', 'exit', 'error', 'stderr'
}

export interface ClaudeProcessOptions {
  claudePath: string
  cwd?: string
  env?: Record<string, string>
  resumeSessionId?: string
}

<!-- 来自 claude-webview.ts 关键结构 -->
interface Channel {
  process: ClaudeProcessManager
  permissionMode: string
  planText: string
  pendingToolUse: PendingToolUse | null
  permissionResolvers: Map<string, PermissionResolver>
  sentPermissionRequests: Set<string>
  totalInputTokens: number
  totalOutputTokens: number
}

interface PermissionResolver {
  resolve: (response: unknown) => void
  toolUseId: string
}

// 超时定时器在 sendToolPermissionRequest 中创建 (line 305-319):
// const timeout = setTimeout(() => { ... }, 300000)
// promise.then(() => { clearTimeout(timeout) ... })

<!-- 来自 index.ts 入口 -->
// 当前只有 EPIPE 处理，没有 uncaughtException/unhandledRejection

<!-- 来自 preload/index.ts 当前 API 桥接 -->
const api = {
  // Claude 进程相关
  claudeStart: (options) => ipcRenderer.invoke('claude:start', options),
  claudeSend: (channelId, msg) => ipcRenderer.invoke('claude:send', channelId, msg),
  // ...
}

<!-- 来自 ChatPanel.vue 状态系统（Plan 01 已执行后） -->
// showStatus(msg: string, type: 'info'|'warning'|'error'|'success', duration?: number): void
// clearStatus(): void
// channelToTab: Map<string, string> — channelId 到 tabId 的映射
// Plan 01 已添加: ConfirmDialog, showConfirm(), resetAndRetry(), defineExpose
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 主进程心跳检测 + 崩溃恢复 IPC + 超时清理 + 全局异常处理 (D-10, D-11, D-12, D-24)</name>
  <files>src/main/claude/process-manager.ts, src/main/ipc/claude-webview.ts, src/main/index.ts, src/preload/index.ts, src/preload/index.d.ts</files>
  <read_first>
    src/main/claude/process-manager.ts
    src/main/ipc/claude-webview.ts
    src/main/index.ts
    src/preload/index.ts
    src/preload/index.d.ts
    .planning/phases/03-ux-enhancement/03-RESEARCH.md (Pattern 3: 进程心跳检测)
  </read_first>
  <behavior>
    - Test 1: startHealthCheck 在 intervalMs 后检测到进程不存活时调用 onUnresponsive
    - Test 2: stopHealthCheck 清除定时器不再调用 onUnresponsive
    - Test 3: resumeSessionId getter 返回 start 时传入的 resumeSessionId
    - Test 4: start() 后 stop() 同时停止心跳定时器
  </behavior>
  <action>
    **1. 修改 `src/main/claude/process-manager.ts`**

    a) **D-11: 心跳检测**
       - 添加私有成员 `healthCheckInterval: ReturnType<typeof setInterval> | null = null`
       - 添加 `startHealthCheck(onUnresponsive: () => void, intervalMs = 30000): void` 方法：
         - 先调用 `stopHealthCheck()` 清除旧的定时器
         - 创建 setInterval，每次执行：
           - 如果 `!this.process || !this._running` 则 return（没有进程在运行）
           - 尝试 `process.kill(this.process.pid!, 0)` 检查进程是否存在
           - 如果抛出异常且 `this._running` 仍为 true -> 进程挂死（exit 事件未触发），调用 `onUnresponsive()`
       - 添加 `stopHealthCheck(): void`：清除定时器并置 null
       - 在 `stop()` 方法中调用 `this.stopHealthCheck()`（确保停止进程时也停止心跳）

    b) **D-10: 崩溃恢复支持**
       - 添加私有成员 `private _resumeSessionId: string | null = null`
       - 在 `start()` 方法中保存 `this._resumeSessionId = options.resumeSessionId || null`
       - 添加 getter `get resumeSessionId(): string | null { return this._resumeSessionId }`

    **2. 修改 `src/main/ipc/claude-webview.ts`**

    a) **D-12: 清理超时定时器**
       - 修改 `PermissionResolver` 接口，添加 `timeoutId: ReturnType<typeof setTimeout>` 字段
       - 在 `sendToolPermissionRequest` 中（约 line 305），将 `const timeout = setTimeout(...)` 的返回值存入 resolver：
         ```typescript
         const timeoutId = setTimeout(() => { ... }, 300000)
         channel.permissionResolvers.set(requestId, { resolve, toolUseId, timeoutId })
         ```
       - 在 promise.then 的清理中（约 line 333-334），保持 `clearTimeout(timeoutId)` 但改用 resolver 中的 timeoutId
       - 修改 `handleCloseChannel` 函数：在 `channel.process.stop()` 之前，遍历 `channel.permissionResolvers`，对每个 resolver 调用 `clearTimeout(resolver.timeoutId)`，然后清空 `channel.permissionResolvers.clear()`

    b) **D-10: 崩溃自动恢复 IPC**
       - 修改 `handleLaunchClaude` 中的 `proc.on('exit', ...)` 回调：
         - 在 Channel 接口中添加 `lastSessionId: string | null = null`
         - 在 handleLaunchClaude 中设置：`channel.lastSessionId = resumeSessionId || null`
         - 当 exit 事件触发且非 persistent channel 时：
           1. 通过 `sendToWebview` 发送 `{ type: 'process_crashed', channelId, canRecover: !!channel.lastSessionId }` 通知渲染进程
           2. 不立即从 channels 中删除（保持 channel 记录用于恢复）
         - 当 exit 触发且是 persistent channel 时，保持现有行为
       - 添加新的 IPC handler `claude:recover-process`：
         - 接收 `channelId` 参数
         - 获取已退出 channel 的 `lastSessionId`
         - 重新调用 `handleLaunchClaude` 并传入 `resumeSessionId`
         - 返回 `{ success: true, channelId }` 或 `{ success: false, error }`

    c) **D-11: 启动心跳**
       - 在 `handleLaunchClaude` 中，进程启动后调用 `proc.startHealthCheck(() => { ... })`
       - onUnresponsive 回调：通过 `sendToWebview` 发送 `{ type: 'process_unresponsive', channelId }` 通知渲染进程
       - 在 `handleCloseChannel` 中，`channel.process.stop()` 已包含 `stopHealthCheck()` 调用（因为 stop() 方法会调用 stopHealthCheck()）

    d) **注册新的 IPC handlers**
       - 在 `registerClaudeWebviewHandlers()` 中添加 `ipcMain.handle('claude:recover-process', ...)` 注册

    **3. 修改 `src/preload/index.ts`**

    在 `api` 对象中添加进程恢复和崩溃事件 API（在 claude 相关方法附近）：
    ```typescript
    // 进程恢复
    claudeRecoverProcess: (channelId: string): Promise<{ success: boolean; channelId?: string; error?: string }> =>
      ipcRenderer.invoke('claude:recover-process', channelId),
    // 进程崩溃事件
    onProcessCrashed: (callback: (data: { channelId: string; canRecover: boolean }) => void): (() => void) => {
      const handler = (_event: Electron.IpcRendererEvent, data: { channelId: string; canRecover: boolean }): void => callback(data)
      ipcRenderer.on('claude:process-crashed', handler)
      return () => ipcRenderer.removeListener('claude:process-crashed', handler)
    },
    // 进程无响应事件
    onProcessUnresponsive: (callback: (data: { channelId: string }) => void): (() => void) => {
      const handler = (_event: Electron.IpcRendererEvent, data: { channelId: string }): void => callback(data)
      ipcRenderer.on('claude:process-unresponsive', handler)
      return () => ipcRenderer.removeListener('claude:process-unresponsive', handler)
    },
    ```

    注意：claude-webview.ts 中需要使用 `event.sender.send('claude:process-crashed', data)` 而非 `sendToWebview`，因为 sendToWebview 是发给 webview iframe 的，而崩溃通知需要发给渲染进程的 ipcRenderer。或者使用 `BrowserWindow.getAllWindows()[0].webContents.send('claude:process-crashed', data)`。

    **4. 修改 `src/preload/index.d.ts`**

    在 `Window.api` 接口中添加（在 claude 相关方法之后）：
    ```typescript
    claudeRecoverProcess: (channelId: string) => Promise<{ success: boolean; channelId?: string; error?: string }>
    onProcessCrashed: (callback: (data: { channelId: string; canRecover: boolean }) => void) => (() => void)
    onProcessUnresponsive: (callback: (data: { channelId: string }) => void) => (() => void)
    ```

    **5. 修改 `src/main/index.ts`**

    在文件顶部（`process.stdout.on('error', ...)` 之后，`createWindow()` 函数之前）添加全局异常处理器：

    1. **`process.on('uncaughtException')`**
       ```typescript
       process.on('uncaughtException', (err) => {
         console.error('[主进程] 未捕获异常:', err)
         // 对可能导致数据丢失的错误弹出对话框
         const win = BrowserWindow.getAllWindows()[0]
         if (win) {
           dialog.showErrorBox('应用错误', '发生意外错误，请查看日志或重启应用。\n' + String(err))
         }
       })
       ```
       - 使用 `dialog.showErrorBox`（非阻塞，不需要 import dialog）
       - 需要在顶部 import 中添加 `dialog`：`import { app, shell, BrowserWindow, ipcMain, dialog } from 'electron'`

    2. **`process.on('unhandledRejection')`**
       ```typescript
       process.on('unhandledRejection', (reason) => {
         console.error('[主进程] 未处理的 Promise 拒绝:', reason)
         // 仅记录日志，不退出进程，不弹对话框（避免干扰用户）
       })
       ```

    3. 将这两段代码放在 `createWindow()` 定义之前，确保在任何代码执行前就注册了异常处理器

    **6. 编写心跳检测逻辑单元测试**

    创建 `src/main/claude/__tests__/process-manager-healthcheck.test.ts`：
    - 测试 `startHealthCheck` 启动后，模拟进程不存活（mock `process.kill` 抛出异常），验证 `onUnresponsive` 被调用
    - 测试 `stopHealthCheck` 清除定时器后 `onUnresponsive` 不再被调用
    - 测试 `resumeSessionId` getter 返回正确值
    - 测试 `stop()` 同时停止心跳定时器
    - 使用 `vi.useFakeTimers()` 控制 setInterval 行为，`vi.spyOn(process, 'kill')` mock 进程检测
  </action>
  <verify>
    <automated>cd "E:/work/projects/new-aitools" && npx tsc --noEmit -p tsconfig.node.json 2>&1 | head -20 && npx vitest run src/main/claude/__tests__/process-manager-healthcheck.test.ts 2>&1 | tail -20 && npx eslint src/main/claude/process-manager.ts src/main/ipc/claude-webview.ts src/main/index.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `process-manager.ts` 包含 `startHealthCheck` 方法
    - `process-manager.ts` 包含 `stopHealthCheck` 方法
    - `process-manager.ts` 包含 `_resumeSessionId` 或 `resumeSessionId` 属性
    - `claude-webview.ts` 的 `PermissionResolver` 接口包含 `timeoutId` 字段
    - `claude-webview.ts` 的 `handleCloseChannel` 包含 `clearTimeout` 调用
    - `claude-webview.ts` 包含 `process_crashed` 或 `process_unresponsive` 消息发送
    - `claude-webview.ts` 包含 `claude:recover-process` IPC handler
    - `preload/index.ts` 包含 `claudeRecoverProcess` 方法
    - `preload/index.ts` 包含 `onProcessCrashed` 方法
    - `preload/index.ts` 包含 `onProcessUnresponsive` 方法
    - `preload/index.d.ts` 包含 `claudeRecoverProcess` 类型声明
    - `preload/index.d.ts` 包含 `onProcessCrashed` 类型声明
    - `index.ts` 包含 `import.*dialog.*from.*electron`
    - `index.ts` 包含 `process.on.*uncaughtException`
    - `index.ts` 包含 `process.on.*unhandledRejection`
    - `src/main/claude/__tests__/process-manager-healthcheck.test.ts` 存在且测试通过
    - TypeScript 编译通过，0 error
    - ESLint 检查通过
  </acceptance_criteria>
  <done>
    主进程：claude.exe 崩溃后通知渲染进程并提供恢复路径。每 30 秒心跳检测进程存活，挂死时通知用户。channel 关闭时清理所有 pending 超时定时器。全局 uncaughtException 和 unhandledRejection 处理器注册。Preload 桥接：claudeRecoverProcess、onProcessCrashed、onProcessUnresponsive API。心跳检测逻辑单元测试通过。TypeScript 编译通过。ESLint 通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: 渲染进程崩溃恢复 UI + 自动重连 (D-10, D-11 渲染侧)</name>
  <files>src/renderer/src/components/ChatPanel.vue</files>
  <read_first>
    src/renderer/src/components/ChatPanel.vue
  </read_first>
  <action>
    **修改 `src/renderer/src/components/ChatPanel.vue`**

    在 `<script setup>` 中添加进程崩溃/无响应的渲染侧处理逻辑：

    a) **D-10 渲染侧: 崩溃自动恢复**

    在 `onMounted` 或初始化阶段注册进程崩溃事件监听：
    ```typescript
    // 进程崩溃自动恢复
    const offProcessCrashed = window.api.onProcessCrashed(async (data) => {
      const { channelId, canRecover } = data
      const tabId = channelToTab.get(channelId)
      if (!tabId) return

      if (canRecover) {
        // 显示 "正在恢复..." 状态
        showStatus('进程已崩溃，正在恢复...', 'warning')
        try {
          const result = await window.api.claudeRecoverProcess(channelId)
          if (result.success) {
            showStatus('已恢复对话', 'success', 3000)
          } else {
            showStatus('恢复失败: ' + (result.error || '未知错误'), 'error')
          }
        } catch (e) {
          showStatus('恢复失败: ' + String(e), 'error')
        }
      } else {
        showStatus('进程已崩溃，无法自动恢复', 'error')
      }
    })
    ```

    b) **D-11 渲染侧: 进程无响应 - 显示重启按钮**

    ```typescript
    // 进程无响应 - 显示重启按钮
    const unresponsiveChannels = ref<Set<string>>(new Set())

    const offProcessUnresponsive = window.api.onProcessUnresponsive(async (data) => {
      const { channelId } = data
      const tabId = channelToTab.get(channelId)
      if (!tabId) return

      unresponsiveChannels.value.add(channelId)
      showStatus('进程无响应，可能已挂死', 'error')

      // 注意：不自动重启，等用户确认
    })

    // 手动重启挂死进程
    async function restartUnresponsiveProcess(channelId: string): Promise<void> {
      showStatus('正在重启进程...', 'warning')
      try {
        const result = await window.api.claudeRecoverProcess(channelId)
        if (result.success) {
          unresponsiveChannels.value.delete(channelId)
          showStatus('进程已重启', 'success', 3000)
        } else {
          showStatus('重启失败: ' + (result.error || '未知错误'), 'error')
        }
      } catch (e) {
        showStatus('重启失败: ' + String(e), 'error')
      }
    }
    ```

    c) **在模板中添加重启按钮**

    在当前活跃 tab 对应的 channel 为 unresponsive 时，显示重启按钮：
    ```html
    <!-- 在 webview 区域或状态覆盖层中添加 -->
    <div v-if="currentUnresponsive" class="unresponsive-overlay">
      <p>进程无响应</p>
      <button class="retry-btn" @click="restartUnresponsiveProcess(currentUnresponsiveChannelId)">重启进程</button>
    </div>
    ```

    添加 `currentUnresponsive` computed：
    ```typescript
    const currentUnresponsiveChannelId = computed(() => {
      for (const channelId of unresponsiveChannels.value) {
        if (channelToTab.get(channelId) === activeTabId.value) {
          return channelId
        }
      }
      return null
    })
    const currentUnresponsive = computed(() => !!currentUnresponsiveChannelId.value)
    ```

    d) **在 `onBeforeUnmount` 中清理事件监听**
    ```typescript
    onBeforeUnmount(() => {
      if (offProcessCrashed) offProcessCrashed()
      if (offProcessUnresponsive) offProcessUnresponsive()
    })
    ```

    e) **添加 `.unresponsive-overlay` CSS 样式**：与 error 显示区域风格一致，居中显示提示和按钮
  </action>
  <verify>
    <automated>cd "E:/work/projects/new-aitools" && npx tsc --noEmit 2>&1 | head -20 && npx eslint src/renderer/src/components/ChatPanel.vue 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `ChatPanel.vue` 包含 `onProcessCrashed` 事件监听注册
    - `ChatPanel.vue` 包含 `onProcessUnresponsive` 事件监听注册
    - `ChatPanel.vue` 包含 `claudeRecoverProcess` 调用
    - `ChatPanel.vue` 包含 `restartUnresponsiveProcess` 方法
    - `ChatPanel.vue` 包含 `unresponsiveChannels` ref
    - `ChatPanel.vue` 包含 `unresponsive-overlay` 模板元素
    - `ChatPanel.vue` 的 `onBeforeUnmount` 中清理事件监听
    - TypeScript 编译通过，0 error
    - ESLint 检查通过
  </acceptance_criteria>
  <done>
    渲染进程：收到 process_crashed 通知后自动调用 recoverProcess 恢复会话，显示"正在恢复..."状态。收到 process_unresponsive 通知后显示"重启进程"按钮供用户手动触发恢复。事件监听在组件卸载时清理。TypeScript 编译通过。ESLint 通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 主进程->子进程 | Claude.exe 子进程生命周期管理，主进程完全控制 |
| 异常->日志 | 异常信息可能包含敏感数据（文件路径等），但仅本地日志 |
| 主进程->渲染进程 | 崩溃/无响应通知通过 IPC 发送，channelId 为可信数据 |
| 渲染进程->主进程 | recoverProcess 调用通过 IPC，channelId 来自事件通知 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | I (Info Disclosure) | dialog.showErrorBox | accept | 错误信息仅在本地显示，不传输到外部 |
| T-03-05 | D (Denial of Service) | 心跳检测误报 | mitigate | 结合 `_running` 状态和 `process.exitCode` 检查，避免误判 |
| T-03-06 | E (Elevation) | recoverProcess channelId 注入 | accept | channelId 来自 IPC 事件，渲染进程无法伪造不存在的 channel |
</threat_model>

<verification>
1. TypeScript 编译通过: `npx tsc --noEmit`
2. ESLint 检查通过: `npx eslint src/main/claude/process-manager.ts src/main/ipc/claude-webview.ts src/main/index.ts src/renderer/src/components/ChatPanel.vue`
3. 心跳检测单元测试通过: `npx vitest run src/main/claude/__tests__/process-manager-healthcheck.test.ts`
4. process-manager.ts 包含 startHealthCheck/stopHealthCheck
5. claude-webview.ts handleCloseChannel 清理 timeoutId
6. claude-webview.ts 包含崩溃恢复 IPC 通道
7. preload/index.ts 包含 claudeRecoverProcess/onProcessCrashed/onProcessUnresponsive
8. ChatPanel.vue 包含崩溃自动恢复逻辑
9. ChatPanel.vue 包含无响应重启按钮
10. index.ts 包含 uncaughtException/unhandledRejection 处理器
</verification>

<success_criteria>
- claude.exe 崩溃后通知渲染进程并自动恢复 (D-10)
- 每 30 秒心跳检测进程存活 (D-11)
- 进程无响应时渲染进程显示重启按钮 (D-11)
- channel 关闭时清理超时定时器 (D-12)
- 全局未捕获异常被记录并弹对话框 (D-24)
- 未处理 Promise rejection 被记录 (D-24)
- 心跳检测逻辑单元测试通过
- Preload API 桥接进程恢复和崩溃事件
- TypeScript 0 error
- ESLint 0 error
</success_criteria>

<output>
After completion, create `.planning/phases/03-ux-enhancement/03-03-SUMMARY.md`
</output>

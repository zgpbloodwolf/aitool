---
phase: 03-ux-enhancement
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/App.vue
  - src/renderer/src/components/Sidebar.vue
  - src/renderer/src/components/FileTree.vue
  - src/renderer/src/stores/workspace.ts
  - src/main/ipc/file-watcher.ts
  - src/main/index.ts
  - src/preload/index.ts
  - src/preload/index.d.ts
autonomous: true
requirements: [D-17, D-18, D-19, D-20]

must_haves:
  truths:
    - "用户可以拖拽侧边栏右边缘调整宽度，刷新后宽度保持不变"
    - "文件树在工作区文件变更后自动刷新"
    - "搜索框输入文本后文件树实时过滤只显示匹配项"
    - "用户可以收藏常用项目路径并在侧边栏快速切换"
  artifacts:
    - path: "src/renderer/src/App.vue"
      provides: "侧边栏拖拽手柄 + 宽度持久化"
      contains: "resize-handle"
    - path: "src/renderer/src/components/Sidebar.vue"
      provides: "搜索框 + 收藏目录 UI"
      contains: "favorites"
    - path: "src/renderer/src/components/FileTree.vue"
      provides: "搜索过滤 + 高亮匹配文本"
      contains: "filterText"
    - path: "src/renderer/src/stores/workspace.ts"
      provides: "文件搜索过滤逻辑 + 收藏目录状态"
      contains: "favorites"
    - path: "src/main/ipc/file-watcher.ts"
      provides: "chokidar 文件监听 IPC handler"
      exports: ["registerFileWatcherHandlers"]
    - path: "src/preload/index.ts"
      provides: "文件监听 IPC API 桥接"
      contains: "fs:startWatch"
    - path: "src/preload/index.d.ts"
      provides: "文件监听 TypeScript 类型声明"
      contains: "fs:startWatch"
  key_links:
    - from: "App.vue"
      to: "localStorage"
      via: "sidebarWidth 读写 SIDEBAR_WIDTH_KEY"
      pattern: "SIDEBAR_WIDTH_KEY"
    - from: "Sidebar.vue"
      to: "workspace.ts store"
      via: "filterText prop 和 favorites 操作"
      pattern: "useWorkspaceStore"
    - from: "file-watcher.ts"
      to: "chokidar"
      via: "chokidar.watch 监听工作区变更"
      pattern: "chokidar\\.watch"
    - from: "preload/index.ts"
      to: "file-watcher.ts"
      via: "ipcRenderer.invoke('fs:startWatch')"
      pattern: "fs:startWatch"
---

<objective>
实现侧边栏拖拽调整宽度、文件系统监听自动刷新、文件搜索过滤、收藏项目目录。

Purpose: 让侧边栏成为可定制、自更新的工作区导航中心，用户无需手动刷新文件树，可以快速搜索文件和切换常用项目。
Output: App.vue 拖拽手柄，file-watcher.ts chokidar 监听，Sidebar.vue 搜索框和收藏列表，workspace.ts 过滤逻辑。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ux-enhancement/03-CONTEXT.md
@.planning/phases/03-ux-enhancement/03-RESEARCH.md

<interfaces>
<!-- 来自 App.vue 当前布局结构 -->
<!-- .main-content { display: flex; flex: 1; overflow: hidden; } -->
<!-- Sidebar 是 flex 子项，当前 width 由 --sidebar-width CSS 变量控制 -->
<!-- Sidebar.vue: width: var(--sidebar-width); min-width: 200px; -->

<!-- 来自 Sidebar.vue 当前结构 -->
<!-- .sidebar-header: 标题 + 打开文件夹按钮 -->
<!-- .sidebar-content: workspace-name + FileTree -->
<!-- .sidebar-empty: 未打开文件夹提示 -->

<!-- 来自 workspace.ts store -->
export interface FileEntry {
  name: string
  path: string
  isDirectory: boolean
  isFile: boolean
  children?: FileEntry[]
}

export const useWorkspaceStore = defineStore('workspace', () => {
  const rootPath = ref<string | null>(null)
  const files = ref<FileEntry[]>([])
  const expandedDirs = ref(new Set<string>())
  const selectedFile = ref<string | null>(null)
  // openFolder, loadDirectory, toggleDir, updateNodeChildren, selectFile
})

<!-- 来自 preload/index.ts 当前 API 桥接 -->
const api = {
  selectFolder: (): Promise<string | null> => ipcRenderer.invoke('dialog:selectFolder'),
  readDir: (dirPath: string): Promise<string[]> => ipcRenderer.invoke('fs:readDir', dirPath),
  // ... 其他 claude API
}

<!-- 来自 main/index.ts 入口 -->
// 当前注册: registerDialogHandlers, registerFilesystemHandlers, registerExtensionHandlers, registerClaudeWebviewHandlers

<!-- 来自 RESEARCH.md Pattern 4: chokidar 文件监听 -->
// chokidar.watch(dirPath, { ignored: /node_modules|\.git/, persistent: true, ignoreInitial: true, depth: 10 })

<!-- 来自 RESEARCH.md Pattern 5: 侧边栏拖拽 -->
// localStorage 存储 sidebarWidth，mousedown/mousemove/mouseup 事件驱动

<!-- Catppuccin Mocha CSS 变量 -->
:root {
  --bg-primary: #1e1e2e;
  --bg-secondary: #181825;
  --bg-tertiary: #313244;
  --text-primary: #cdd6f4;
  --text-secondary: #a6adc8;
  --text-muted: #6c7086;
  --accent: #89b4fa;
  --accent-hover: #74c7ec;
  --border: #45475a;
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 侧边栏拖拽宽度 + chokidar 文件监听 + preload 桥接 (D-17, D-18)</name>
  <files>src/renderer/src/App.vue, src/main/ipc/file-watcher.ts, src/main/index.ts, src/preload/index.ts, src/preload/index.d.ts, src/renderer/src/stores/workspace.ts</files>
  <read_first>
    src/renderer/src/App.vue
    src/renderer/src/stores/workspace.ts
    src/main/index.ts
    src/preload/index.ts
    src/preload/index.d.ts
    .planning/phases/03-ux-enhancement/03-RESEARCH.md (Pattern 4 和 Pattern 5)
  </read_first>
  <action>
    **1. 安装 chokidar 依赖**
       ```bash
       pnpm add chokidar
       ```

    **2. 创建 `src/main/ipc/file-watcher.ts`** (per D-18)

    新建文件，实现 chokidar 文件监听 IPC handler：
    ```typescript
    import chokidar from 'chokidar'
    import { ipcMain, BrowserWindow } from 'electron'

    let watcher: chokidar.FSWatcher | null = null

    export function registerFileWatcherHandlers(): void {
      ipcMain.handle('fs:startWatch', (_event, dirPath: string) => {
        stopWatcher()
        watcher = chokidar.watch(dirPath, {
          ignored: /node_modules|\.git/,
          persistent: true,
          ignoreInitial: true,
          depth: 10
        })
        watcher.on('all', (_event, _path) => {
          // 任何文件变更都通知渲染进程刷新文件树
          const win = BrowserWindow.getAllWindows()[0]
          if (win) {
            win.webContents.send('fs:changed')
          }
        })
        return true
      })

      ipcMain.handle('fs:stopWatch', () => {
        stopWatcher()
        return true
      })
    }

    function stopWatcher(): void {
      if (watcher) {
        watcher.close()
        watcher = null
      }
    }
    ```

    **3. 修改 `src/main/index.ts`**

    - 在文件顶部 import 区域添加：`import { registerFileWatcherHandlers } from './ipc/file-watcher'`
    - 在 `app.whenReady().then(() => {` 块中，在 `registerClaudeWebviewHandlers()` 行之后添加：`registerFileWatcherHandlers()`

    **4. 修改 `src/preload/index.ts`**

    在 `api` 对象中添加文件监听相关方法（在 `stat` 方法之后）：
    ```typescript
    // File Watcher
    startFileWatch: (dirPath: string): Promise<boolean> =>
      ipcRenderer.invoke('fs:startWatch', dirPath),
    stopFileWatch: (): Promise<boolean> =>
      ipcRenderer.invoke('fs:stopWatch'),
    onFileChanged: (callback: () => void): (() => void) => {
      const handler = (): void => callback()
      ipcRenderer.on('fs:changed', handler)
      return () => ipcRenderer.removeListener('fs:changed', handler)
    },
    ```

    **5. 修改 `src/preload/index.d.ts`**

    在 `Window.api` 接口中添加（在 `stat` 方法之后）：
    ```typescript
    startFileWatch: (dirPath: string) => Promise<boolean>
    stopFileWatch: () => Promise<boolean>
    onFileChanged: (callback: () => void) => (() => void)
    ```

    **6. 修改 `src/renderer/src/stores/workspace.ts`** (per D-18 文件监听集成)

    - 在 store 的返回值之前添加文件监听逻辑：
    ```typescript
    let offFileChanged: (() => void) | null = null

    async function startWatch(): Promise<void> {
      if (!rootPath.value) return
      stopWatch()
      await window.api.startFileWatch(rootPath.value)
      offFileChanged = window.api.onFileChanged(async () => {
        // 刷新文件树
        if (rootPath.value) {
          files.value = await loadDirectory(rootPath.value)
        }
      })
    }

    function stopWatch(): void {
      if (offFileChanged) {
        offFileChanged()
        offFileChanged = null
      }
      window.api.stopFileWatch()
    }
    ```
    - 修改 `openFolder()`：在 `files.value = await loadDirectory(path)` 之后调用 `await startWatch()`
    - 在 return 中导出 `startWatch`, `stopWatch`

    - 添加 `filterText` ref 和 `filteredFiles` computed（为 Task 2 的 FileTree 搜索过滤准备）：
    ```typescript
    const filterText = ref('')
    const favorites = ref<string[]>(JSON.parse(localStorage.getItem('aitools-favorites') || '[]'))
    ```
    - 在 return 中导出 `filterText`, `favorites`

    - 添加收藏目录方法：
    ```typescript
    function addFavorite(path: string): void {
      if (!favorites.value.includes(path)) {
        favorites.value.push(path)
        localStorage.setItem('aitools-favorites', JSON.stringify(favorites.value))
      }
    }

    function removeFavorite(path: string): void {
      favorites.value = favorites.value.filter(p => p !== path)
      localStorage.setItem('aitools-favorites', JSON.stringify(favorites.value))
    }

    async function openFavorite(path: string): Promise<void> {
      rootPath.value = path
      await window.api.claudeSetCwd(path)
      expandedDirs.value.clear()
      files.value = await loadDirectory(path)
      await startWatch()
    }
    ```
    - 在 return 中导出 `addFavorite`, `removeFavorite`, `openFavorite`

    **7. 修改 `src/renderer/src/App.vue`** (per D-17 侧边栏拖拽)

    在 `<script setup>` 中添加：
    ```typescript
    const SIDEBAR_WIDTH_KEY = 'aitools-sidebar-width'
    const sidebarWidth = ref(parseInt(localStorage.getItem(SIDEBAR_WIDTH_KEY) || '260'))
    const isResizing = ref(false)

    function startResize(e: MouseEvent): void {
      isResizing.value = true
      const startX = e.clientX
      const startWidth = sidebarWidth.value

      // 拖拽期间禁用 iframe 指针事件（防止 iframe 拦截 mousemove）
      const iframes = document.querySelectorAll('.webview-iframe')
      iframes.forEach(iframe => { (iframe as HTMLElement).style.pointerEvents = 'none' })

      function onMouseMove(e: MouseEvent): void {
        const newWidth = Math.max(200, Math.min(500, startWidth + (e.clientX - startX)))
        sidebarWidth.value = newWidth
      }

      function onMouseUp(): void {
        isResizing.value = false
        localStorage.setItem(SIDEBAR_WIDTH_KEY, String(sidebarWidth.value))
        document.removeEventListener('mousemove', onMouseMove)
        document.removeEventListener('mouseup', onMouseUp)
        // 恢复 iframe 指针事件
        iframes.forEach(iframe => { (iframe as HTMLElement).style.pointerEvents = '' })
      }

      document.addEventListener('mousemove', onMouseMove)
      document.addEventListener('mouseup', onMouseUp)
    }
    ```

    在模板中修改 Sidebar 元素：
    ```html
    <Sidebar v-if="sidebarVisible" :style="{ width: sidebarWidth + 'px', minWidth: sidebarWidth + 'px' }" />
    <div
      v-if="sidebarVisible"
      class="resize-handle"
      :class="{ active: isResizing }"
      @mousedown="startResize"
    />
    ```

    在 `<style scoped>` 中添加：
    ```css
    .resize-handle {
      width: 4px;
      cursor: col-resize;
      background: transparent;
      flex-shrink: 0;
      transition: background 0.15s;
    }
    .resize-handle:hover,
    .resize-handle.active {
      background: var(--accent);
    }
    ```
  </action>
  <verify>
    <automated>cd "E:/work/projects/new-aitools" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/ipc/file-watcher.ts` 文件存在且包含 `chokidar.watch` 调用
    - `src/main/ipc/file-watcher.ts` 包含 `registerFileWatcherHandlers` 导出函数
    - `src/main/index.ts` 包含 `import.*registerFileWatcherHandlers`
    - `src/main/index.ts` 包含 `registerFileWatcherHandlers()` 调用
    - `src/preload/index.ts` 包含 `startFileWatch` 方法
    - `src/preload/index.ts` 包含 `onFileChanged` 方法
    - `src/preload/index.d.ts` 包含 `startFileWatch` 类型声明
    - `src/renderer/src/stores/workspace.ts` 包含 `startWatch` 函数
    - `src/renderer/src/stores/workspace.ts` 包含 `favorites` ref
    - `src/renderer/src/stores/workspace.ts` 包含 `addFavorite` 函数
    - `src/renderer/src/App.vue` 包含 `resize-handle` class 的 div
    - `src/renderer/src/App.vue` 包含 `sidebarWidth` ref
    - `src/renderer/src/App.vue` 包含 `startResize` 函数
    - TypeScript 编译通过，0 error
  </acceptance_criteria>
  <done>
    侧边栏宽度可通过拖拽 4px 手柄调整（200-500px），宽度持久化到 localStorage。文件系统变更通过 chokidar 监听并自动刷新文件树。文件监听 IPC handler 注册到主进程。workspace store 包含 filterText、favorites、startWatch/stopWatch 等方法。preload 桥接文件监听 API。TypeScript 编译通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: 文件搜索过滤 + 收藏目录 UI (D-19, D-20)</name>
  <files>src/renderer/src/components/Sidebar.vue, src/renderer/src/components/FileTree.vue, src/renderer/src/stores/workspace.ts</files>
  <read_first>
    src/renderer/src/components/Sidebar.vue
    src/renderer/src/components/FileTree.vue
    src/renderer/src/stores/workspace.ts
    .planning/phases/03-ux-enhancement/03-CONTEXT.md (D-19, D-20)
  </read_first>
  <action>
    **1. 修改 `src/renderer/src/stores/workspace.ts`**

    添加 `filteredFiles` computed 属性（用于搜索过滤）：
    ```typescript
    const filteredFiles = computed(() => {
      if (!filterText.value.trim()) return files.value
      return filterFiles(files.value, filterText.value.trim().toLowerCase())
    })

    function filterFiles(entries: FileEntry[], query: string): FileEntry[] {
      const result: FileEntry[] = []
      for (const entry of entries) {
        if (entry.isDirectory) {
          // 递归过滤子目录
          const filteredChildren = entry.children ? filterFiles(entry.children, query) : []
          if (filteredChildren.length > 0 || entry.name.toLowerCase().includes(query)) {
            result.push({
              ...entry,
              children: filteredChildren.length > 0 ? filteredChildren : entry.children
                ? entry.children.filter(c => c.name.toLowerCase().includes(query))
                : undefined
            })
          }
        } else {
          if (entry.name.toLowerCase().includes(query)) {
            result.push(entry)
          }
        }
      }
      return result
    }
    ```
    - 优化过滤逻辑：如果目录名匹配则显示该目录下所有内容；如果子目录内有匹配项则显示该目录及匹配子项
    - 在 return 中导出 `filteredFiles`

    **2. 修改 `src/renderer/src/components/Sidebar.vue`** (per D-19, D-20)

    将 Sidebar 改为 `<script setup lang="ts">`（当前已是）并添加搜索框和收藏列表：

    a) 搜索框（per D-19）：
    ```html
    <div v-if="workspace.rootPath" class="search-box">
      <input
        type="text"
        class="search-input"
        placeholder="搜索文件..."
        v-model="workspace.filterText"
      />
      <button
        v-if="workspace.filterText"
        class="search-clear"
        @click="workspace.filterText = ''"
      >&times;</button>
    </div>
    ```

    b) 收藏目录列表（per D-20）：
    ```html
    <div v-if="workspace.favorites.length > 0" class="favorites-section">
      <div class="favorites-header">
        <span class="favorites-title">收藏</span>
      </div>
      <div
        v-for="fav in workspace.favorites"
        :key="fav"
        class="favorite-item"
        :class="{ active: workspace.rootPath === fav }"
        @click="workspace.openFavorite(fav)"
      >
        <span class="fav-icon">&#x2B50;</span>
        <span class="fav-name">{{ fav.split(/[\\/]/).pop() }}</span>
        <span class="fav-path">{{ fav }}</span>
        <button class="fav-remove" @click.stop="workspace.removeFavorite(fav)">&times;</button>
      </div>
    </div>
    ```

    c) 添加收藏按钮（在 workspace-name 旁边）：
    ```html
    <div class="workspace-actions">
      <span class="workspace-name">{{ workspace.rootPath.split(/[\\/]/).pop() }}</span>
      <button
        class="fav-add-btn"
        title="收藏此目录"
        @click="workspace.addFavorite(workspace.rootPath)"
      >&#x2B50;</button>
    </div>
    ```

    d) 将 FileTree 组件的模板绑定改为使用 `workspace.filteredFiles`（而非 `workspace.files`）。由于 FileTree.vue 直接访问 `workspace.files`，需要改为访问 `workspace.filteredFiles`：
    - 修改 FileTree.vue 的模板中 `workspace.files` 引用改为 `workspace.filteredFiles`

    e) 添加相关 CSS 样式：
    ```css
    .search-box {
      position: relative;
      padding: 6px 8px;
      border-bottom: 1px solid var(--border);
    }
    .search-input {
      width: 100%;
      background: var(--bg-tertiary);
      border: 1px solid var(--border);
      color: var(--text-primary);
      padding: 4px 8px;
      border-radius: 3px;
      font-size: 12px;
      outline: none;
    }
    .search-input:focus {
      border-color: var(--accent);
    }
    .search-input::placeholder {
      color: var(--text-muted);
    }
    .search-clear {
      position: absolute;
      right: 12px;
      top: 50%;
      transform: translateY(-50%);
      background: none;
      border: none;
      color: var(--text-muted);
      cursor: pointer;
      font-size: 14px;
      padding: 0 4px;
    }
    .favorites-section {
      border-bottom: 1px solid var(--border);
    }
    .favorites-header {
      padding: 6px 12px;
      font-size: 11px;
      text-transform: uppercase;
      color: var(--text-muted);
      letter-spacing: 0.5px;
    }
    .favorite-item {
      display: flex;
      align-items: center;
      gap: 6px;
      padding: 4px 12px;
      cursor: pointer;
      font-size: 12px;
      color: var(--text-secondary);
    }
    .favorite-item:hover {
      background: var(--bg-tertiary);
    }
    .favorite-item.active {
      color: var(--accent);
    }
    .fav-icon { font-size: 12px; flex-shrink: 0; }
    .fav-name {
      color: var(--text-primary);
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
    .fav-path {
      flex: 1;
      font-size: 10px;
      color: var(--text-muted);
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
    .fav-remove {
      background: none;
      border: none;
      color: var(--text-muted);
      cursor: pointer;
      font-size: 12px;
      padding: 0 2px;
      opacity: 0;
      transition: opacity 0.15s;
    }
    .favorite-item:hover .fav-remove { opacity: 1; }
    .fav-remove:hover { color: var(--error); }
    .workspace-actions {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 6px 12px;
    }
    .fav-add-btn {
      background: none;
      border: none;
      font-size: 12px;
      cursor: pointer;
      padding: 0 2px;
      opacity: 0.5;
    }
    .fav-add-btn:hover { opacity: 1; }
    ```

    **3. 修改 `src/renderer/src/components/FileTree.vue`** (per D-19)

    FileTree.vue 使用 Options API，需要修改模板中 `workspace.files` 的引用：
    - 将模板中 `v-for="entry in workspace.files"` 改为 `v-for="entry in workspace.filteredFiles"`
    - 如果 `filteredFiles` 为空且 `filterText` 非空，显示 "无匹配文件" 提示
    - 在 FileTree 根模板中添加空状态：
      ```html
      <div v-if="workspace.filterText && workspace.filteredFiles.length === 0" class="filter-empty">
        无匹配文件
      </div>
      ```
    - 添加 CSS：`.filter-empty { padding: 12px 16px; color: var(--text-muted); font-size: 12px; text-align: center; }`
  </action>
  <verify>
    <automated>cd "E:/work/projects/new-aitools" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `workspace.ts` 包含 `filteredFiles` computed 属性
    - `workspace.ts` 包含 `filterFiles` 辅助函数
    - `workspace.ts` 包含 `addFavorite` 和 `removeFavorite` 方法
    - `Sidebar.vue` 包含 `search-input` class 的 input 元素
    - `Sidebar.vue` 包含 `favorites-section` class 的 div
    - `Sidebar.vue` 包含 `fav-add-btn` 按钮
    - `FileTree.vue` 引用 `workspace.filteredFiles`（而非 `workspace.files`）
    - TypeScript 编译通过，0 error
  </acceptance_criteria>
  <done>
    侧边栏顶部有搜索框，输入文本实时过滤文件树（递归匹配文件/目录名）。FileTree 空匹配时显示提示。收藏列表显示在侧边栏，支持添加/删除/点击切换。workspace-name 旁有收藏按钮。TypeScript 编译通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 渲染进程→主进程 | 文件监听通过 IPC invoke 调用，路径参数来自 workspace store |
| 主进程→文件系统 | chokidar 读取文件系统事件，仅监听 allowedRoots 内的路径 |
| localStorage→渲染进程 | 侧边栏宽度和收藏路径从 localStorage 读取，纯本地数据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | E (Elevation) | fs:startWatch 路径注入 | mitigate | 在 file-watcher.ts 中校验 dirPath 是否在 allowedRoots 内（复用 filesystem.ts 的 addAllowedRoot/isPathAllowed） |
| T-03-09 | D (Denial of Service) | chokidar 监听大目录 | mitigate | 配置 ignored: /node_modules\|\.git/ 和 depth: 10 限制监听范围 |
| T-03-10 | I (Info Disclosure) | 收藏路径存 localStorage | accept | localStorage 仅本地存储，收藏路径不含敏感信息 |
</threat_model>

<verification>
1. TypeScript 编译通过: `npx tsc --noEmit`
2. file-watcher.ts 存在且包含 chokidar.watch 调用
3. preload 桥接文件监听 API 存在
4. workspace store 包含 startWatch/stopWatch/filterText/favorites
5. App.vue 包含 resize-handle 和 sidebarWidth
6. Sidebar.vue 包含搜索框和收藏列表
7. FileTree.vue 使用 filteredFiles
</verification>

<success_criteria>
- 侧边栏宽度可拖拽调整并持久化 (D-17)
- 文件系统变更自动刷新文件树 (D-18)
- 文件搜索实时过滤 (D-19)
- 收藏项目目录可添加/删除/切换 (D-20)
- TypeScript 0 error
</success_criteria>

<output>
After completion, create `.planning/phases/03-ux-enhancement/03-05-SUMMARY.md`
</output>

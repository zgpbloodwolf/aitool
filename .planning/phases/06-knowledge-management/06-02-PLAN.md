---
phase: 06-knowledge-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/stores/clipboard.ts
  - src/renderer/src/stores/snippet.ts
  - src/renderer/src/composables/useClipboard.ts
  - src/renderer/src/stores/__tests__/clipboard.test.ts
  - src/renderer/src/stores/__tests__/snippet.test.ts
autonomous: true
requirements:
  - UX-08

must_haves:
  truths:
    - "剪贴板历史记录在内存中存储最近 50 条文本内容"
    - "相同文本不重复记录，重复时更新时间戳并移到最前"
    - "关闭应用后历史记录清空"
    - "收藏片段通过 localStorage 持久化，重启应用后保留"
    - "收藏片段支持创建、编辑、删除操作"
    - "收藏片段可从剪贴板历史记录提升"
  artifacts:
    - path: "src/renderer/src/stores/clipboard.ts"
      provides: "剪贴板历史内存 store"
      exports: ["useClipboardStore"]
      contains: "MAX_ENTRIES"
    - path: "src/renderer/src/stores/snippet.ts"
      provides: "收藏片段 localStorage store"
      exports: ["useSnippetStore"]
      contains: "SNIPPETS_KEY"
    - path: "src/renderer/src/composables/useClipboard.ts"
      provides: "剪贴板读取与记录 composable"
      exports: ["useClipboard"]
    - path: "src/renderer/src/stores/__tests__/clipboard.test.ts"
      provides: "clipboard store 单元测试"
    - path: "src/renderer/src/stores/__tests__/snippet.test.ts"
      provides: "snippet store 单元测试"
  key_links:
    - from: "src/renderer/src/composables/useClipboard.ts"
      to: "src/renderer/src/stores/clipboard.ts"
      via: "调用 addEntry() 记录剪贴板内容"
      pattern: "useClipboardStore.*addEntry"
    - from: "src/renderer/src/stores/snippet.ts"
      to: "localStorage"
      via: "watch deep 自动写入"
      pattern: "localStorage.setItem.*SNIPPETS_KEY"
---

<objective>
创建剪贴板增强功能的数据层：内存版剪贴板历史 store（50 条上限）、localStorage 版收藏片段 store（CRUD + 持久化）、剪贴板读取 composable。

Purpose: 为剪贴板弹出面板提供完整的数据管理基础设施，面板组件只需调用 store 方法即可。
Output: 两个 Pinia store（clipboard、snippet）+ 一个 composable（useClipboard）+ 对应测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-knowledge-management/06-CONTEXT.md
@.planning/phases/06-knowledge-management/06-RESEARCH.md
@.planning/phases/06-knowledge-management/06-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/renderer/src/stores/settings.ts (Pinia composition API + localStorage 模式):
```typescript
import { defineStore } from 'pinia'
import { ref, computed, watch } from 'vue'

const SETTINGS_KEY = 'aitools-settings'

export const useSettingsStore = defineStore('settings', () => {
  const raw = localStorage.getItem(SETTINGS_KEY)
  const initial: SettingsState = raw ? { ...DEFAULTS, ...safeParse(raw) } : { ...DEFAULTS }
  const settings = ref<SettingsState>(initial)

  watch(settings, (newVal) => {
    localStorage.setItem(SETTINGS_KEY, JSON.stringify(newVal))
  }, { deep: true })

  function update(patch: Partial<SettingsState>): void {
    for (const [key, value] of Object.entries(patch)) {
      if (key in settings.value) {
        ;(settings.value as Record<string, unknown>)[key] = value
      }
    }
  }

  return { settings, update, /* ... */ }
})
```

From src/renderer/src/composables/useNotification.ts (composable 模式):
```typescript
export function useNotification(): void {
  window.api.onNotificationFocusTab?.((channelId: string) => {
    window.dispatchEvent(new CustomEvent('notification:focus-tab', { detail: channelId }))
  })
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 剪贴板历史 store + 收藏片段 store + useClipboard composable + 测试</name>
  <files>
    src/renderer/src/stores/clipboard.ts,
    src/renderer/src/stores/snippet.ts,
    src/renderer/src/composables/useClipboard.ts,
    src/renderer/src/stores/__tests__/clipboard.test.ts,
    src/renderer/src/stores/__tests__/snippet.test.ts
  </files>
  <read_first>
    src/renderer/src/stores/settings.ts
    src/renderer/src/composables/useNotification.ts
    .planning/phases/06-knowledge-management/06-CONTEXT.md
    .planning/phases/06-knowledge-management/06-RESEARCH.md
  </read_first>
  <behavior>
    **clipboard store 测试:**
    - Test 1: addEntry 添加文本到历史记录最前面
    - Test 2: addEntry 相同文本不重复，移到最前并更新时间戳
    - Test 3: addEntry 空文本不记录
    - Test 4: 超过 50 条时自动清除最早的记录
    - Test 5: removeEntry 按 ID 删除指定条目
    - Test 6: clearAll 清空所有历史

    **snippet store 测试:**
    - Test 1: addSnippet 添加片段到列表最前面
    - Test 2: promoteFromClipboard 从剪贴板文本创建片段
    - Test 3: updateSnippet 按 ID 更新标题/内容
    - Test 4: deleteSnippet 按 ID 删除片段
    - Test 5: localStorage 持久化 — 修改后自动写入
  </behavior>
  <action>
    **步骤 1: 创建剪贴板历史 store** (src/renderer/src/stores/clipboard.ts)

    Per D-05: 内存存储，关闭后清空。Per D-06: 最大 50 条。Per D-07: 仅文本。

    ```typescript
    import { defineStore } from 'pinia'
    import { ref } from 'vue'

    /** 剪贴板历史条目 */
    export interface ClipboardEntry {
      id: string
      text: string
      timestamp: number
    }

    /** D-06: 最大保留条目数 */
    const MAX_ENTRIES = 50

    /**
     * 剪贴板历史 store — 纯内存存储，关闭应用后清空 (per D-05)
     */
    export const useClipboardStore = defineStore('clipboard', () => {
      const history = ref<ClipboardEntry[]>([])

      /**
       * 添加条目 — 相同文本去重并移到最前 (per D-07: 仅文本)
       */
      function addEntry(text: string): void {
        if (!text.trim()) return
        // 去重：相同文本移除旧条目
        const existingIdx = history.value.findIndex(e => e.text === text)
        if (existingIdx !== -1) {
          history.value.splice(existingIdx, 1)
        }
        history.value.unshift({
          id: `clip_${Date.now()}_${Math.random().toString(36).slice(2)}`,
          text,
          timestamp: Date.now()
        })
        // D-06: 超出上限自动清除最早的
        if (history.value.length > MAX_ENTRIES) {
          history.value = history.value.slice(0, MAX_ENTRIES)
        }
      }

      function removeEntry(id: string): void {
        history.value = history.value.filter(e => e.id !== id)
      }

      function clearAll(): void {
        history.value = []
      }

      return { history, addEntry, removeEntry, clearAll }
    })
    ```

    **步骤 2: 创建收藏片段 store** (src/renderer/src/stores/snippet.ts)

    Per D-08: localStorage 存储。Per D-09: CRUD 管理。Per D-10: 从剪贴板历史提升。

    ```typescript
    import { defineStore } from 'pinia'
    import { ref, watch } from 'vue'

    /** 收藏片段 */
    export interface Snippet {
      id: string
      title: string
      content: string
      createdAt: number
      updatedAt: number
    }

    const SNIPPETS_KEY = 'aitools-snippets'

    function safeParse(raw: string): Snippet[] {
      try { return JSON.parse(raw) } catch { return [] }
    }

    /**
     * 收藏片段 store — localStorage 持久化 (per D-08)
     */
    export const useSnippetStore = defineStore('snippet', () => {
      const raw = localStorage.getItem(SNIPPETS_KEY)
      const snippets = ref<Snippet[]>(raw ? safeParse(raw) : [])

      // 自动持久化
      watch(snippets, (newVal) => {
        localStorage.setItem(SNIPPETS_KEY, JSON.stringify(newVal))
      }, { deep: true })

      function addSnippet(title: string, content: string): void {
        snippets.value.unshift({
          id: `snippet_${Date.now()}_${Math.random().toString(36).slice(2)}`,
          title,
          content,
          createdAt: Date.now(),
          updatedAt: Date.now()
        })
      }

      /** D-10: 从剪贴板历史提升 */
      function promoteFromClipboard(text: string, title?: string): void {
        addSnippet(title || text.slice(0, 30), text)
      }

      function updateSnippet(id: string, patch: Partial<Pick<Snippet, 'title' | 'content'>>): void {
        const idx = snippets.value.findIndex(s => s.id === id)
        if (idx === -1) return
        snippets.value[idx] = {
          ...snippets.value[idx],
          ...patch,
          updatedAt: Date.now()
        }
      }

      function deleteSnippet(id: string): void {
        snippets.value = snippets.value.filter(s => s.id !== id)
      }

      return { snippets, addSnippet, promoteFromClipboard, updateSnippet, deleteSnippet }
    })
    ```

    **步骤 3: 创建 useClipboard composable** (src/renderer/src/composables/useClipboard.ts)

    不做后台轮询（per RESEARCH Pitfall 2），在用户操作时记录:
    ```typescript
    import { useClipboardStore } from '../stores/clipboard'

    /**
     * 剪贴板操作 composable — 在用户操作时读取剪贴板并记录历史
     */
    export function useClipboard() {
      const clipboardStore = useClipboardStore()

      /**
       * 读取当前剪贴板内容并记录到历史
       * 必须在用户手势回调中调用（浏览器安全策略限制）
       */
      async function readAndRecord(): Promise<string | null> {
        try {
          const text = await navigator.clipboard.readText()
          if (text.trim()) {
            clipboardStore.addEntry(text)
          }
          return text
        } catch {
          return null
        }
      }

      /**
       * 将文本写入剪贴板并记录到历史
       */
      async function pasteText(text: string): Promise<boolean> {
        try {
          await navigator.clipboard.writeText(text)
          clipboardStore.addEntry(text)
          return true
        } catch {
          return false
        }
      }

      return { readAndRecord, pasteText }
    }
    ```

    **步骤 4: 创建测试文件**

    a) `src/renderer/src/stores/__tests__/clipboard.test.ts`:
    - 测试 addEntry 添加到最前
    - 测试相同文本去重
    - 测试空文本不记录
    - 测试 50 条上限
    - 测试 removeEntry 和 clearAll

    b) `src/renderer/src/stores/__tests__/snippet.test.ts`:
    - 测试 addSnippet
    - 测试 promoteFromClipboard
    - 测试 updateSnippet
    - 测试 deleteSnippet
    - 测试 localStorage 持久化（mock localStorage）
  </action>
  <verify>
    <automated>cd "E:/work/projects/new-aitools" && npx vitest run src/renderer/src/stores/__tests__/clipboard.test.ts src/renderer/src/stores/__tests__/snippet.test.ts</automated>
  </verify>
  <done>
    - `useClipboardStore` 导出，history 为空数组初始值，addEntry 添加条目，removeEntry 删除条目，clearAll 清空
    - 相同文本去重，空文本不记录，50 条上限自动截断
    - `useSnippetStore` 导出，snippets 从 localStorage 初始化，watch deep 自动持久化
    - addSnippet、promoteFromClipboard、updateSnippet、deleteSnippet 全部工作
    - `useClipboard` composable 导出 readAndRecord 和 pasteText 方法
    - 所有测试通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 剪贴板 -> 渲染进程 | navigator.clipboard 读取受浏览器安全策略限制 |
| localStorage | 收藏片段明文存储在用户设备本地 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | I | localStorage 片段 | accept | 片段数据为用户主动保存的文本，无敏感风险，且 localStorage 仅本机可访问 |
| T-06-05 | D | navigator.clipboard | accept | 剪贴板历史为内存数据，应用关闭即清空 |
</threat_model>

<verification>
1. `npx vitest run src/renderer/src/stores/__tests__/clipboard.test.ts src/renderer/src/stores/__tests__/snippet.test.ts` 所有测试通过
2. TypeScript 编译无错误: `npx tsc --noEmit`
3. ESLint 无新增警告
</verification>

<success_criteria>
- `useClipboardStore` 纯内存 store，50 条上限，去重，空文本过滤
- `useSnippetStore` localStorage 持久化，CRUD 完整
- `useClipboard` composable 提供安全剪贴板读取和写入
- 所有测试通过，TypeScript 编译无错误
</success_criteria>

<output>
After completion, create `.planning/phases/06-knowledge-management/06-02-SUMMARY.md`
</output>

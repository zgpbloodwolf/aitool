---
phase: 06-knowledge-management
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/renderer/src/components/ContextMenu.vue
  - src/renderer/src/components/ChatPanel.vue
  - src/main/index.ts
autonomous: true
requirements:
  - UX-07

must_haves:
  truths:
    - "用户右键点击标签可看到包含「导出对话」选项的菜单"
    - "点击「导出对话」后弹出系统保存对话框，默认文件名为「标题-日期.md」"
    - "选择保存位置后文件成功写入磁盘"
    - "导出完成后显示成功提示，失败显示错误提示"
  artifacts:
    - path: "src/renderer/src/components/ContextMenu.vue"
      provides: "通用右键菜单组件"
      contains: "ContextMenu"
    - path: "src/renderer/src/components/ChatPanel.vue"
      provides: "标签右键菜单集成 + 导出触发逻辑"
      contains: "showTabContextMenu"
  key_links:
    - from: "src/renderer/src/components/ChatPanel.vue"
      to: "src/renderer/src/components/ContextMenu.vue"
      via: "import ContextMenu + @contextmenu 事件"
      pattern: "ContextMenu"
    - from: "src/renderer/src/components/ChatPanel.vue"
      to: "window.api.exportSession"
      via: "IPC invoke 调用导出"
      pattern: "exportSession"
---

<objective>
实现对话导出的前端交互：通用右键菜单组件 ContextMenu、标签右键菜单集成、导出触发与进度反馈。

Purpose: 让用户通过右键标签触发对话导出，完成从 UI 到后端的完整链路。
Output: 可通过标签右键菜单导出当前对话为 Markdown 文件的完整交互流程。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-knowledge-management/06-CONTEXT.md
@.planning/phases/06-knowledge-management/06-RESEARCH.md
@.planning/phases/06-knowledge-management/06-PATTERNS.md
@.planning/phases/06-knowledge-management/06-01-SUMMARY.md

<interfaces>
<!-- 从 Plan 01 创建的接口 — executor 必须使用这些 -->

From src/preload/index.ts (Plan 01 新增):
```typescript
showSaveDialog: (options: { defaultPath: string; title: string }): Promise<{ canceled: boolean; filePath: string }>
getActiveSessionId: (channelId: string): Promise<string | null>
exportSession: (sessionId: string, title: string, savePath: string): Promise<{ success: boolean; error?: string }>
```

<!-- 现有接口 -->

From src/renderer/src/components/ChatPanel.vue (行 76-82):
```typescript
const channelToTab = new Map<string, string>()
// channelToTab 暴露到 window.__channelToTab 用于通知跳转
;(window as any).__channelToTab = channelToTab
```

From src/renderer/src/components/ChatPanel.vue (标签栏结构, 行 722-757):
```vue
<div class="tab-bar">
  <div class="tab-list">
    <div v-for="tab in tabs" :key="tab.id" class="tab-item"
      @click="switchTab(tab.id)" @mousedown.middle.prevent="closeTab(tab.id)">
      <span class="tab-status-dot" />
      <span class="tab-label">{{ tab.label }}</span>
      <span class="tab-close" @click.stop="closeTab(tab.id)">x</span>
    </div>
  </div>
  <button class="tab-history" @click="toggleSessionHistory" />
  <button class="tab-add" @click="addNewTab" />
</div>
```

From src/renderer/src/components/ConfirmDialog.vue (overlay 模式参考):
```vue
<script setup lang="ts">
defineProps<{ visible: boolean; title: string; /* ... */ }>()
const emit = defineEmits<{ (e: 'confirm'): void; (e: 'cancel'): void }>()
</script>
<template>
  <Transition name="dialog-fade">
    <div v-if="visible" class="dialog-overlay" @click.self="handleCancel">
      <!-- 内容 -->
    </div>
  </Transition>
</template>
<style scoped>
.dialog-overlay {
  position: fixed; inset: 0;
  background: var(--dialog-backdrop);
  z-index: 1000;
}
</style>
```

From src/renderer/src/components/ChatPanel.vue (showStatus 函数):
```typescript
function showStatus(msg: string, type: 'success' | 'error' | 'warning' | 'info', duration?: number): void
```

From src/main/index.ts (行 87-106 before-input-event):
```typescript
mainWindow.webContents.on('before-input-event', (event, input) => {
  if (!input.control && !input.meta) return
  const shortcuts: Record<string, string> = {
    'n': 'shortcut:new-tab', 'w': 'shortcut:close-tab',
    'b': 'shortcut:toggle-sidebar', ',': 'shortcut:open-settings',
    '0': 'shortcut:reset-zoom'
  }
  // ...
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContextMenu 通用组件 + ChatPanel 标签右键菜单 + 导出功能集成</name>
  <files>
    src/renderer/src/components/ContextMenu.vue,
    src/renderer/src/components/ChatPanel.vue
  </files>
  <read_first>
    src/renderer/src/components/ChatPanel.vue
    src/renderer/src/components/ConfirmDialog.vue
    src/renderer/src/assets/main.css
    .planning/phases/06-knowledge-management/06-CONTEXT.md
  </read_first>
  <action>
    **步骤 1: 创建 ContextMenu 组件** (src/renderer/src/components/ContextMenu.vue)

    Per D-11: 标签右键菜单触发。Per PATTERNS.md: 参考 ConfirmDialog.vue overlay 模式。

    组件结构:
    ```vue
    <script setup lang="ts">
    export interface MenuItem {
      label: string          // 显示文本
      action: string         // 动作标识
      icon?: string          // 可选图标
      disabled?: boolean     // 是否禁用
    }

    const props = defineProps<{
      visible: boolean
      x: number
      y: number
      items: MenuItem[]
    }>()

    const emit = defineEmits<{
      (e: 'select', action: string): void
      (e: 'close'): void
    }>()

    function handleClickOutside(): void {
      emit('close')
    }

    function handleSelect(action: string): void {
      emit('select', action)
      emit('close')
    }

    // 计算菜单位置，检查窗口边界防止溢出
    function getMenuStyle(): Record<string, string> {
      let left = props.x
      let top = props.y
      // 估算菜单尺寸（200px 宽，每项 36px 高）
      const estimatedWidth = 200
      const estimatedHeight = props.items.length * 36
      if (left + estimatedWidth > window.innerWidth) {
        left = window.innerWidth - estimatedWidth - 8
      }
      if (top + estimatedHeight > window.innerHeight) {
        top = window.innerHeight - estimatedHeight - 8
      }
      return {
        left: `${left}px`,
        top: `${top}px`
      }
    }
    </script>

    <template>
      <Teleport to="body">
        <Transition name="context-menu">
          <div v-if="visible" class="context-menu-overlay" @click="handleClickOutside">
            <div class="context-menu" :style="getMenuStyle()">
              <div
                v-for="item in items"
                :key="item.action"
                class="context-menu-item"
                :class="{ disabled: item.disabled }"
                @click="item.disabled ? undefined : handleSelect(item.action)"
              >
                <span v-if="item.icon" class="menu-icon">{{ item.icon }}</span>
                <span class="menu-label">{{ item.label }}</span>
              </div>
            </div>
          </div>
        </Transition>
      </Teleport>
    </template>

    <style scoped>
    .context-menu-overlay {
      position: fixed;
      inset: 0;
      z-index: 1100;
    }

    .context-menu {
      position: fixed;
      min-width: 160px;
      background: var(--bg-secondary);
      border: 1px solid var(--border);
      border-radius: 6px;
      padding: 4px 0;
      box-shadow: 0 4px 12px var(--shadow-color);
    }

    .context-menu-item {
      display: flex;
      align-items: center;
      gap: 8px;
      padding: 6px 12px;
      cursor: pointer;
      color: var(--text-primary);
      font-size: 13px;
    }

    .context-menu-item:hover:not(.disabled) {
      background: var(--bg-tertiary);
    }

    .context-menu-item.disabled {
      color: var(--text-muted);
      cursor: default;
    }

    .context-menu-enter-active,
    .context-menu-leave-active {
      transition: opacity 0.15s ease;
    }
    .context-menu-enter-from,
    .context-menu-leave-to {
      opacity: 0;
    }
    </style>
    ```

    **步骤 2: ChatPanel.vue 集成标签右键菜单和导出功能**

    在 ChatPanel.vue 中进行以下修改:

    a) 新增 import:
    ```typescript
    import ContextMenu, { type MenuItem } from './ContextMenu.vue'
    ```

    b) 新增状态变量（在 `const confirmVisible = ref(false)` 附近）:
    ```typescript
    // 标签右键菜单
    const contextMenuVisible = ref(false)
    const contextMenuX = ref(0)
    const contextMenuY = ref(0)
    const contextMenuTabId = ref<string | null>(null)
    const contextMenuItems: MenuItem[] = [
      { label: '导出对话', action: 'export', icon: '\u{1F4C4}' }
    ]
    ```

    c) 新增事件处理函数:
    ```typescript
    /** D-11: 标签右键菜单 */
    function showTabContextMenu(event: MouseEvent, tabId: string): void {
      event.preventDefault()
      contextMenuTabId.value = tabId
      contextMenuX.value = event.clientX
      contextMenuY.value = event.clientY
      contextMenuVisible.value = true
    }

    function handleContextMenuAction(action: string): void {
      if (action === 'export' && contextMenuTabId.value) {
        exportCurrentChat(contextMenuTabId.value)
      }
      contextMenuVisible.value = false
    }
    ```

    d) 新增导出函数:
    ```typescript
    /** D-11: 导出当前标签的对话为 Markdown 文件 */
    async function exportCurrentChat(tabId: string): Promise<void> {
      // 查找 tabId 对应的 channelId
      let channelId: string | null = null
      for (const [chId, tId] of channelToTab) {
        if (tId === tabId) { channelId = chId; break }
      }
      if (!channelId) {
        showStatus('无法定位当前频道', 'error')
        return
      }

      // 获取 sessionId
      const sessionId = await window.api.getActiveSessionId(channelId)
      if (!sessionId) {
        showStatus('当前无活跃会话，无法导出', 'warning')
        return
      }

      // 获取标签标题
      const tab = tabs.value.find(t => t.id === tabId)
      const title = tab?.label || '对话'
      const date = new Date().toISOString().split('T')[0]
      // D-04: 默认文件名为「会话标题+日期」
      const defaultName = `${title}-${date}.md`

      // 弹出保存对话框
      const dialogResult = await window.api.showSaveDialog({
        title: '导出对话',
        defaultPath: defaultName
      })
      if (dialogResult.canceled || !dialogResult.filePath) return

      showStatus('正在导出对话...', 'info')

      // 调用导出 IPC
      const result = await window.api.exportSession(sessionId, title, dialogResult.filePath)
      if (result.success) {
        showStatus('对话已导出', 'success', 3000)
      } else {
        showStatus('导出失败: ' + (result.error || '未知错误'), 'error')
      }
    }
    ```

    e) 修改模板 — 在 tab-item 上添加 `@contextmenu.prevent`:
    在 `tab-item` div 上添加:
    ```
    @contextmenu.prevent="showTabContextMenu($event, tab.id)"
    ```

    f) 在模板中添加 ContextMenu 组件（在 session-history overlay 之后）:
    ```vue
    <!-- 标签右键菜单 (UX-07) -->
    <ContextMenu
      :visible="contextMenuVisible"
      :x="contextMenuX"
      :y="contextMenuY"
      :items="contextMenuItems"
      @select="handleContextMenuAction"
      @close="contextMenuVisible = false"
    />
    ```

    注意: ContextMenu 使用 `<Teleport to="body">` 渲染到 body 层级，所以不受 ChatPanel 内部 z-index 限制。
  </action>
  <verify>
    <automated>cd "E:/work/projects/new-aitools" && npx tsc --noEmit</automated>
  </verify>
  <done>
    - `ContextMenu.vue` 组件创建，支持 `visible/x/y/items` props 和 `select/close` events
    - 菜单定位检查窗口边界防止溢出
    - ChatPanel 标签项添加 `@contextmenu.prevent` 事件
    - 右键标签显示「导出对话」菜单项
    - 点击「导出对话」弹出保存对话框，默认文件名「标题-日期.md」（per D-04）
    - 导出成功显示成功提示，失败显示错误提示
    - TypeScript 编译无错误
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户交互 -> 右键菜单 | DOM 事件坐标来自浏览器，可信 |
| 渲染进程 -> 主进程 IPC | exportSession 参数通过 IPC 传递 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | S | ContextMenu 坐标注入 | accept | 坐标值仅用于 CSS positioning，无注入风险 |
| T-06-07 | E | 导出文件路径 | mitigate | 使用 Electron dialog.showSaveDialog 返回的系统安全路径 |
</threat_model>

<verification>
1. TypeScript 编译无错误: `npx tsc --noEmit`
2. ESLint 无新增警告
3. 手动验证: 运行 `pnpm dev`，右键标签 → 看到菜单 → 点击导出 → 弹出保存对话框 → 保存成功
</verification>

<success_criteria>
- 右键标签显示包含「导出对话」的上下文菜单
- 点击导出后弹出系统保存对话框，默认文件名格式正确（per D-04）
- 导出成功/失败均有状态栏提示
- ContextMenu 组件通用可复用，使用 CSS 变量适配双主题
- TypeScript 编译无错误
</success_criteria>

<output>
After completion, create `.planning/phases/06-knowledge-management/06-03-SUMMARY.md`
</output>

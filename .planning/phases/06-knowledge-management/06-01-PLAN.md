---
phase: 06-knowledge-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/types.ts
  - src/main/ipc/dialog.ts
  - src/main/ipc/claude-webview.ts
  - src/main/claude/session-store.ts
  - src/preload/index.ts
  - src/preload/index.d.ts
autonomous: true
requirements:
  - UX-07

must_haves:
  truths:
    - "用户可通过标签右键菜单将当前对话导出为 Markdown 文件"
    - "导出的文件仅包含用户消息和助手回复，不含 system/tool_use 内容"
    - "导出的 Markdown 文件头部包含会话标题和导出日期"
    - "保存对话框允许用户选择保存位置和修改文件名"
  artifacts:
    - path: "src/shared/types.ts"
      provides: "ExportSessionResult 类型定义"
      contains: "ExportSessionResult"
    - path: "src/main/ipc/dialog.ts"
      provides: "dialog:showSaveDialog IPC handler"
      contains: "dialog:showSaveDialog"
    - path: "src/main/ipc/claude-webview.ts"
      provides: "claude:export-session IPC handler + claude:get-active-session-id IPC handler"
      contains: "claude:export-session"
    - path: "src/preload/index.ts"
      provides: "exportSession + showSaveDialog + getActiveSessionId preload API"
      contains: "exportSession"
  key_links:
    - from: "src/preload/index.ts"
      to: "src/main/ipc/claude-webview.ts"
      via: "ipcRenderer.invoke('claude:export-session')"
      pattern: "claude:export-session"
    - from: "src/main/ipc/claude-webview.ts"
      to: "src/main/claude/session-store.ts"
      via: "getSessionMessages() 读取 JSONL 消息"
      pattern: "getSessionMessages"
---

<objective>
实现对话导出为 Markdown 文件的后端管线：新增 `dialog:showSaveDialog` 和 `claude:export-session` IPC handler，在主进程完成消息读取、Markdown 格式化和文件写入。

Purpose: 提供完整的主进程导出能力，渲染进程只需触发 IPC 调用即可完成导出。
Output: 可被渲染进程通过 `window.api.exportSession()` 调用的完整导出管线。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-knowledge-management/06-CONTEXT.md
@.planning/phases/06-knowledge-management/06-RESEARCH.md
@.planning/phases/06-knowledge-management/06-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/shared/types.ts:
```typescript
export interface SessionInfo {
  id: string
  lastModified: number
  fileSize: number
  summary: string | undefined
  gitBranch: string | undefined
}
```

From src/main/ipc/dialog.ts:
```typescript
import { dialog, BrowserWindow, ipcMain } from 'electron'

export function registerDialogHandlers(): void {
  ipcMain.handle('dialog:selectFolder', async () => {
    const window = BrowserWindow.getFocusedWindow()
    if (!window) return null
    const result = await dialog.showOpenDialog(window, {
      properties: ['openDirectory']
    })
    return result.canceled ? null : result.filePaths[0] ?? null
  })
}
```

From src/main/claude/session-store.ts:
```typescript
export async function getSessionMessages(sessionId: string, cwd: string): Promise<unknown[]>
// channels Map 中的 Channel 类型有 lastSessionId: string | null
```

From src/main/ipc/claude-webview.ts (行 33-41):
```typescript
interface Channel {
  process: ClaudeCodeProcess
  webviewChannelId: string
  cwd: string
  permissionResolvers: Map<string, PermissionResolver>
  sentPermissionRequests: Set<string>
  totalInputTokens: number
  totalOutputTokens: number
  lastSessionId: string | null
}
const channels = new Map<string, Channel>()
let currentCwd = process.cwd()
```

From src/preload/index.ts:
```typescript
// IPC invoke 暴露模式示例:
claudeListSessions: (): Promise<SessionInfo[]> => ipcRenderer.invoke('claude:list-sessions'),
claudeDeleteSession: (sessionId: string): Promise<boolean> =>
  ipcRenderer.invoke('claude:delete-session', sessionId),
```

From src/preload/index.d.ts:
```typescript
// 类型声明模式示例:
claudeDeleteSession: (sessionId: string) => Promise<boolean>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 导出后端 IPC 管线 — 类型 + dialog handler + export handler + preload 桥接</name>
  <files>
    src/shared/types.ts,
    src/main/ipc/dialog.ts,
    src/main/ipc/claude-webview.ts,
    src/main/claude/session-store.ts,
    src/preload/index.ts,
    src/preload/index.d.ts,
    src/main/__tests__/export-session.test.ts
  </files>
  <read_first>
    src/shared/types.ts
    src/main/ipc/dialog.ts
    src/main/ipc/claude-webview.ts
    src/main/claude/session-store.ts
    src/preload/index.ts
    src/preload/index.d.ts
    .planning/phases/06-knowledge-management/06-CONTEXT.md
    .planning/phases/06-knowledge-management/06-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `formatMessagesAsMarkdown` 过滤 system 消息 — 给定包含 type='system' 和 type='user' 的消息数组，输出仅包含 user 消息内容
    - Test 2: `formatMessagesAsMarkdown` 过滤 tool_use/thinking block — 给定 assistant 消息包含 text + tool_use block，输出仅包含 text block 内容
    - Test 3: `formatMessagesAsMarkdown` 文件头包含标题和日期 — 输出第一行为 `# 标题`，第二行为 `> 导出于 YYYY-MM-DD`
    - Test 4: `formatMessagesAsMarkdown` 保留原始 Markdown 代码块 — 给定包含 ```python...``` 的 text block，输出保留代码块不变
    - Test 5: `formatMessagesAsMarkdown` 处理空 content 数组 — 不崩溃，输出仅包含文件头
  </behavior>
  <action>
    **步骤 1: 新增类型定义** (src/shared/types.ts)

    在文件末尾添加导出结果类型（per D-01~D-04）:
    ```typescript
    /** 对话导出结果 (UX-07) */
    export interface ExportSessionResult {
      success: boolean
      error?: string
    }
    ```

    **步骤 2: 新增 dialog:showSaveDialog handler** (src/main/ipc/dialog.ts)

    在 `registerDialogHandlers()` 函数内追加新的 IPC handler（参考现有 `dialog:selectFolder` 模式）:
    ```typescript
    ipcMain.handle('dialog:showSaveDialog', async (_event, options: {
      defaultPath: string
      title: string
    }) => {
      const window = BrowserWindow.getFocusedWindow()
      if (!window) return { canceled: true, filePath: '' }
      const result = await dialog.showSaveDialog(window, {
        title: options.title,
        defaultPath: options.defaultPath,
        filters: [{ name: 'Markdown', extensions: ['md'] }]
      })
      return { canceled: result.canceled, filePath: result.filePath || '' }
    })
    ```

    **步骤 3: 新增 Markdown 格式化工具函数** (src/main/claude/session-store.ts)

    在文件中新增导出函数（不修改现有函数）。导入 `writeFile` from `fs/promises`:
    ```typescript
    import { readFile, stat, readdir, open, unlink, writeFile } from 'fs/promises'
    ```

    新增 `formatMessagesAsMarkdown` 函数:
    - 参数: `messages: unknown[]`, `title: string`, `date: string`
    - 遍历消息，per D-01: 跳过 `type === 'system'` 和 `isSidechain/isMeta` 消息
    - user 消息: 添加 `## 用户` 标题 + 遍历 content 数组中 `type === 'text'` 的 block，提取 `block.text`
    - assistant 消息: 添加 `## 助手` 标题 + 同样只保留 `type === 'text'` block（per D-01，跳过 `tool_use`、`thinking`）
    - per D-02: 文件头为 `# ${title}\n\n> 导出于 ${date}\n\n`
    - per D-03: 保留原始 Markdown 代码块，不做任何转换
    - 返回拼接后的完整 Markdown 字符串

    新增 `exportSessionAsMarkdown` 函数:
    - 参数: `sessionId: string`, `title: string`, `savePath: string`, `cwd: string`
    - 调用 `getSessionMessages(sessionId, cwd)` 获取消息
    - 调用 `formatMessagesAsMarkdown(messages, title, new Date().toISOString().split('T')[0])` 格式化
    - 调用 `writeFile(savePath, markdown, 'utf8')` 写入文件
    - 使用 `safeLog` / `safeError` 记录日志
    - 返回 `{ success: true }` 或 `{ success: false, error: String(e) }`

    **步骤 4: 新增 IPC handler** (src/main/ipc/claude-webview.ts)

    在 `registerClaudeWebviewHandlers()` 函数末尾（`claude:recover-process` handler 之后、函数闭合 `}` 之前）追加两个新 handler:

    a) `claude:get-active-session-id` handler:
    ```typescript
    ipcMain.handle('claude:get-active-session-id', async (_event, channelId: string) => {
      const channel = channels.get(channelId)
      return channel?.lastSessionId || null
    })
    ```

    b) `claude:export-session` handler:
    ```typescript
    ipcMain.handle('claude:export-session', async (_event, sessionId: string, title: string, savePath: string) => {
      safeLog('[ClaudeIPC] 正在导出会话:', sessionId)
      return await exportSessionAsMarkdown(sessionId, title, savePath, currentCwd)
    })
    ```

    在文件顶部添加导入:
    ```typescript
    import { exportSessionAsMarkdown } from '../claude/session-store'
    ```

    **步骤 5: Preload 桥接** (src/preload/index.ts)

    在 `api` 对象中追加三个新方法（参考现有 `claudeDeleteSession` 等 invoke 模式）:
    ```typescript
    // 对话导出 (UX-07)
    showSaveDialog: (options: { defaultPath: string; title: string }): Promise<{ canceled: boolean; filePath: string }> =>
      ipcRenderer.invoke('dialog:showSaveDialog', options),
    getActiveSessionId: (channelId: string): Promise<string | null> =>
      ipcRenderer.invoke('claude:get-active-session-id', channelId),
    exportSession: (sessionId: string, title: string, savePath: string): Promise<{ success: boolean; error?: string }> =>
      ipcRenderer.invoke('claude:export-session', sessionId, title, savePath),
    ```

    **步骤 6: 类型声明同步** (src/preload/index.d.ts)

    在 `api` 接口中追加对应方法签名:
    ```typescript
    showSaveDialog: (options: { defaultPath: string; title: string }) => Promise<{ canceled: boolean; filePath: string }>
    getActiveSessionId: (channelId: string) => Promise<string | null>
    exportSession: (sessionId: string, title: string, savePath: string) => Promise<{ success: boolean; error?: string }>
    ```

    **步骤 7: 测试文件** (src/main/__tests__/export-session.test.ts)

    创建测试文件，测试 `formatMessagesAsMarkdown` 函数。从 `session-store.ts` 中提取或导出该函数进行测试:
    - 测试过滤 system 消息
    - 测试过滤 tool_use/thinking block
    - 测试文件头格式（标题 + 日期）
    - 测试保留原始 Markdown 代码块
    - 测试空 content 数组不崩溃
  </action>
  <verify>
    <automated>cd "E:/work/projects/new-aitools" && npx vitest run src/main/__tests__/export-session.test.ts</automated>
  </verify>
  <done>
    - `dialog:showSaveDialog` IPC handler 注册并可调用
    - `claude:export-session` IPC handler 注册并可调用
    - `claude:get-active-session-id` IPC handler 注册并可调用
    - `formatMessagesAsMarkdown` 正确过滤 system/tool_use/thinking 内容
    - 文件头包含标题和导出日期（per D-02）
    - 原始 Markdown 代码块保留不变（per D-03）
    - preload 桥接和类型声明同步更新
    - 所有测试通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 渲染进程 -> 主进程 IPC | `exportSession` 参数可能被篡改（sessionId 注入、savePath 路径遍历） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | S | `claude:get-active-session-id` | accept | 仅返回 sessionId 或 null，无敏感数据泄露风险 |
| T-06-02 | T | `claude:export-session` sessionId | mitigate | session-store 已有 UUID 正则验证（`UUID_RE`），在 `getSessionMessages` 前验证 sessionId 格式 |
| T-06-03 | E | `claude:export-session` savePath | mitigate | `dialog.showSaveDialog` 返回的用户选择路径，Electron 沙箱限制渲染进程无法指定任意路径 |
</threat_model>

<verification>
1. `npx vitest run src/main/__tests__/export-session.test.ts` 所有测试通过
2. TypeScript 编译无错误: `npx tsc --noEmit`
3. ESLint 无新增警告: `npx eslint src/shared/types.ts src/main/ipc/dialog.ts src/main/ipc/claude-webview.ts src/main/claude/session-store.ts src/preload/index.ts src/preload/index.d.ts`
</verification>

<success_criteria>
- `dialog:showSaveDialog` handler 注册成功，返回 `{ canceled, filePath }` 结构
- `claude:export-session` handler 注册成功，能将 JSONL 消息转为 Markdown 文件
- `formatMessagesAsMarkdown` 正确过滤 system/tool_use/thinking（per D-01），保留代码块（per D-03）
- 文件头包含 `# 标题` 和 `> 导出于 日期`（per D-02）
- Preload API 和类型声明与主进程 handler 一致
- 所有测试通过，TypeScript 编译无错误
</success_criteria>

<output>
After completion, create `.planning/phases/06-knowledge-management/06-01-SUMMARY.md`
</output>
